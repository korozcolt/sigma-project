---
phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv
plan: 06
subsystem: rename-cosmetico
tags: [rename, votante-apoyo, exports, console-commands, enums, public-registration, adr-006]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: Locked D-01 rename decision and ADR-006 interface rules (only user-facing strings renamed)
provides:
  - Export column headers (Leaders, Annotators, Coordinators, Survey CSV) using Apoyo terminology
  - UserRole and CampaignStatus enum getDescription() text using Apoyo terminology
  - Operator-visible console command output ($description, info(), warn()) using Apoyo terminology
  - Public voter-registration controller flash message and Blade view using Apoyo terminology
  - Updated PublicVoterRegistrationLinkTest assertion matching renamed page title
affects: [02.1-05, 04.1-reportes-avanzados-de-apoyos]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ADR-006 rename scope: only user-facing strings (headers, command $description/info()/warn(), enum descriptions, flash messages, Blade text) are renamed; class/method/route names, $signature, internal PHP comments, and Log:: context strings are left untouched"

key-files:
  created: []
  modified:
    - app/Exports/LeadersExport.php
    - app/Exports/AnnotatorsExport.php
    - app/Exports/CoordinatorsExport.php
    - app/Services/SurveyExportService.php
    - app/Enums/UserRole.php
    - app/Enums/CampaignStatus.php
    - app/Console/Commands/CreateVoterRecordsForUsers.php
    - app/Console/Commands/SendBirthdayMessages.php
    - app/Console/Commands/TestHablameSms.php
    - app/Http/Controllers/PublicVoterRegistrationController.php
    - resources/views/public/voter-registration.blade.php
    - tests/Feature/PublicVoterRegistrationLinkTest.php

key-decisions:
  - "Followed the plan's explicit interface rule literally: internal PHP comments and Log::error context strings referencing 'votante(s)' were intentionally left unchanged, even though the plan's top-level <verification> step (expecting a grep count of 1) undercounted these 4 intentional exceptions (3 comments + 1 log line = 5 total remaining matches, not 1)"

requirements-completed: [D-01]

# Metrics
duration: 9min
completed: 2026-07-23
---

# Phase 02.1 Plan 06: Rename Votante to Apoyo in Exports, Console Commands, Enums, and Public Registration Summary

**Renamed all remaining user-facing "Votante"/"votante" strings in 4 export/service files, 2 enums, 3 console commands, and the public voter-registration controller/Blade view to "Apoyo"/"apoyo", completing the D-01 rename outside the Filament admin panel (covered separately by plan 02.1-05).**

## Performance

- **Duration:** 9 min
- **Started:** 2026-07-23T04:35:00Z (approx)
- **Completed:** 2026-07-23T04:44:05Z
- **Tasks:** 3 completed
- **Files modified:** 12

## Accomplishments
- Export column headers (`LeadersExport`, `AnnotatorsExport`, `CoordinatorsExport`, `SurveyExportService`) now read "Apoyos Registrados" / "ID Apoyo" / "Nombre Apoyo"
- `UserRole::getDescription()` (LEADER, REVIEWER) and `CampaignStatus::getDescription()` (ACTIVE) now describe "apoyos" instead of "votantes"
- Console command operator-visible output (`$description`, `info()`, `warn()`) renamed in `CreateVoterRecordsForUsers`, `SendBirthdayMessages`, `TestHablameSms`, while command signatures (`users:create-voter-records`, etc.) and internal comments were left untouched per ADR-006
- Public voter-registration page (title, heading, subtext, helper texts) and its controller's success flash message now use "Apoyo"/"apoyo"; the internal `Log::error(...)` message was intentionally left as-is (not user-facing)
- `PublicVoterRegistrationLinkTest` updated to assert `'Registro de apoyos'`; full test file re-run and passes (3 passed, 13 assertions)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename in Exports and Enums** - `08228dc` (fix)
2. **Task 2: Rename in Console Commands** - `7fd41b3` (fix)
3. **Task 3: Rename in public registration controller/view, update PublicVoterRegistrationLinkTest.php** - `b9f5411` (fix)

**Plan metadata:** committed with this SUMMARY (see final commit below)

## Files Created/Modified
- `app/Exports/LeadersExport.php` - Header "Votantes Registrados" -> "Apoyos Registrados"
- `app/Exports/AnnotatorsExport.php` - Header "Votantes Registrados" -> "Apoyos Registrados"
- `app/Exports/CoordinatorsExport.php` - Header "Votantes Registrados" -> "Apoyos Registrados"
- `app/Services/SurveyExportService.php` - Headers "ID Votante"/"Nombre Votante" -> "ID Apoyo"/"Nombre Apoyo"
- `app/Enums/UserRole.php` - LEADER/REVIEWER `getDescription()` text renamed
- `app/Enums/CampaignStatus.php` - ACTIVE `getDescription()` text renamed
- `app/Console/Commands/CreateVoterRecordsForUsers.php` - `$description` and `info()` message renamed; signature untouched
- `app/Console/Commands/SendBirthdayMessages.php` - `$description` and 3 `info()`/`warn()` messages renamed; signature untouched
- `app/Console/Commands/TestHablameSms.php` - 2 `warn()`/`info()` messages renamed
- `app/Http/Controllers/PublicVoterRegistrationController.php` - Success flash message renamed; `Log::error` context string left as-is (not user-facing); Pint also reordered `use` imports alphabetically and collapsed the empty constructor body
- `resources/views/public/voter-registration.blade.php` - Page title, heading, subtext, and 2 helper texts renamed
- `tests/Feature/PublicVoterRegistrationLinkTest.php` - Assertion updated to `'Registro de apoyos'`

## Decisions Made
- Followed the plan's explicit `<interfaces>` and Task 2 `<action>` instructions literally: internal PHP comments (`app/Console/Commands/CreateVoterRecordsForUsers.php` L37/L66/L107 style comments, `app/Console/Commands/SendBirthdayMessages.php` L90-style comment) and the controller's `Log::error('Error en registro público de votantes', ...)` context string were deliberately left unchanged, since they are not operator-visible/user-facing output.

## Deviations from Plan

### Auto-fixed Issues
None - no bugs, missing functionality, or blocking issues were encountered; all string replacements matched the plan's `<action>` blocks exactly.

### Plan Self-Inconsistency (documented, not a code deviation)
The plan's own top-level `<verification>` step states the combined grep count across Exports/Console/Enums/Controller/Blade view should print **1** (only the intentional controller `Log::error` line). However, Task 2's `<action>` explicitly instructs "do not rename internal PHP comments" for the 3 console-command files, which contain such comments referencing "votante(s)". Following that explicit instruction (which is consistent with the plan's `<interfaces>` principle of renaming only user-facing strings) leaves 4 additional matches (3 comments + the 1 intentional log line = 5 total), not 1. No code was changed to chase the "1" figure, since doing so would have violated the explicit "do not rename comments" instruction in the same task. This is flagged here for visibility, not treated as a bug requiring a fix.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All export/console/enum/public-registration user-facing "Votante" strings are renamed to "Apoyo", completing the non-Filament half of the D-01 rename (Filament admin panel covered by plan 02.1-05)
- `PublicVoterRegistrationLinkTest` passes with the updated assertion
- No blockers for subsequent phases

---
*Phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv*
*Completed: 2026-07-23*

## Self-Check: PASSED

All 12 modified files confirmed present on disk. All 3 task commits (`08228dc`, `7fd41b3`, `b9f5411`) confirmed present in git history.

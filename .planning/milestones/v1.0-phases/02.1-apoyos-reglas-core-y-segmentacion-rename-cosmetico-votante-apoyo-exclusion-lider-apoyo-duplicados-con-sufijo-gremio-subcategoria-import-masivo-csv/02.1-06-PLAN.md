---
phase: 02.1
plan: 06
type: execute
wave: 1
depends_on: ["02.1-01"]
files_modified:
  - app/Exports/LeadersExport.php
  - app/Exports/AnnotatorsExport.php
  - app/Exports/CoordinatorsExport.php
  - app/Services/SurveyExportService.php
  - app/Enums/UserRole.php
  - app/Enums/CampaignStatus.php
  - app/Console/Commands/CreateVoterRecordsForUsers.php
  - app/Console/Commands/SendBirthdayMessages.php
  - app/Console/Commands/TestHablameSms.php
  - app/Http/Controllers/PublicVoterRegistrationController.php
  - resources/views/public/voter-registration.blade.php
  - tests/Feature/PublicVoterRegistrationLinkTest.php
autonomous: true
requirements: [D-01]

must_haves:
  truths:
    - "Export column headers, console command output, role/status descriptions, and the public voter-registration page all say Apoyo instead of Votante"
    - "The public registration page test still passes after the rename"
  artifacts:
    - path: "resources/views/public/voter-registration.blade.php"
      provides: "Public-facing registration form with Apoyo terminology"
  key_links:
    - from: "resources/views/public/voter-registration.blade.php"
      to: "tests/Feature/PublicVoterRegistrationLinkTest.php"
      via: "assertSee('Registro de apoyos')"
      pattern: "Registro de apoyos"
---

<objective>
Rename every remaining "Votante"/"votante" occurrence in Exports, Console Commands, Enum descriptions, the public voter-registration controller/flash-message, and its Blade view, to "Apoyo"/"apoyo", per the locked D-01 decision.

Purpose: D-01 requires the rename to cover reports, exports, and public-facing pages without exception, not just the Filament admin panel. This plan covers the backend/export/console/public surface that plan `02.1-05` (Filament admin panel) does not touch.
Output: 4 Export/Service files, 2 Enum files, 3 Console Command files, 1 Controller, 1 public Blade view renamed; 1 existing test updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv/02.1-CONTEXT.md
@docs/DECISIONES.md
</context>

<interfaces>
Per ADR-006, rename only user-facing strings (export column headers, command $description and $this->info()/warn() output shown to the operator running the command, enum getDescription()/getLabel() text, controller flash messages, Blade view text). Do NOT rename class names, method names, route names, namespaces, file names, or DB/log context keys (e.g. leave `Log::error('Error en registro público de votantes', [...])` log-context strings alone; that particular string is used for log searchability, not shown to any user — skip it).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename in Exports and Enums</name>
  <files>app/Exports/LeadersExport.php, app/Exports/AnnotatorsExport.php, app/Exports/CoordinatorsExport.php, app/Services/SurveyExportService.php, app/Enums/UserRole.php, app/Enums/CampaignStatus.php</files>
  <read_first>
    - app/Exports/LeadersExport.php (line around 61)
    - app/Exports/AnnotatorsExport.php (line around 48)
    - app/Exports/CoordinatorsExport.php (line around 62)
    - app/Services/SurveyExportService.php (lines around 35-36)
    - app/Enums/UserRole.php (lines around 57-58)
    - app/Enums/CampaignStatus.php (line around 51)
  </read_first>
  <action>
    Make these exact string replacements (old to new), one per file/line. Do not change anything else.

    app/Exports/LeadersExport.php L61: column header "Votantes Registrados" becomes "Apoyos Registrados"
    app/Exports/AnnotatorsExport.php L48: column header "Votantes Registrados" becomes "Apoyos Registrados"
    app/Exports/CoordinatorsExport.php L62: column header "Votantes Registrados" becomes "Apoyos Registrados"

    app/Services/SurveyExportService.php:
    - L35: column header "ID Votante" becomes "ID Apoyo"
    - L36: column header "Nombre Votante" becomes "Nombre Apoyo"

    app/Enums/UserRole.php (inside `getDescription()`):
    - L57: `self::LEADER => 'Registra y gestiona votantes en su zona.'` becomes `self::LEADER => 'Registra y gestiona apoyos en su zona.'`
    - L58: `self::REVIEWER => 'Valida votantes y realiza llamadas de verificación.'` becomes `self::REVIEWER => 'Valida apoyos y realiza llamadas de verificación.'`

    app/Enums/CampaignStatus.php L51: `self::ACTIVE => 'Campaña en curso, equipo activo registrando votantes.'` becomes `self::ACTIVE => 'Campaña en curso, equipo activo registrando apoyos.'`
  </action>
  <verify>
    <automated>grep -rEc "[Vv]otante" app/Exports/LeadersExport.php app/Exports/AnnotatorsExport.php app/Exports/CoordinatorsExport.php app/Services/SurveyExportService.php app/Enums/UserRole.php app/Enums/CampaignStatus.php | awk -F: '{s+=$2} END {print s+0}'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above prints 0
    - `grep -c "Apoyo" app/Enums/UserRole.php` returns at least 2
  </acceptance_criteria>
  <done>All 6 files renamed; role/status descriptions and export headers now use Apoyo/Apoyos.</done>
</task>

<task type="auto">
  <name>Task 2: Rename in Console Commands</name>
  <files>app/Console/Commands/CreateVoterRecordsForUsers.php, app/Console/Commands/SendBirthdayMessages.php, app/Console/Commands/TestHablameSms.php</files>
  <read_first>
    - app/Console/Commands/CreateVoterRecordsForUsers.php (lines around 25, 32)
    - app/Console/Commands/SendBirthdayMessages.php (lines around 21, 98, 103, 115)
    - app/Console/Commands/TestHablameSms.php (lines around 114, 121)
  </read_first>
  <action>
    Make these exact string replacements (old to new), one per file/line. Only rename operator-visible output ($description shown by `php artisan list`, and `$this->info()`/`$this->warn()` messages). Do not rename the command's `$signature` (the artisan command name itself stays `users:create-voter-records` etc, per ADR-006 code identifiers stay in English) and do not rename internal PHP comments.

    app/Console/Commands/CreateVoterRecordsForUsers.php:
    - L25: `$description = 'Crear registros de votantes para users existentes que no tienen uno'` becomes `'Crear registros de apoyos para users existentes que no tienen uno'`
    - L32: `$this->info('Buscando users sin registro de votante...')` becomes `$this->info('Buscando users sin registro de apoyo...')`

    app/Console/Commands/SendBirthdayMessages.php:
    - L21: `$description = 'Envía mensajes de cumpleaños a los votantes que cumplen años hoy'` becomes `'Envía mensajes de cumpleaños a los apoyos que cumplen años hoy'`
    - L98: `"No hay votantes que cumplan años hoy en {$campaign->name}"` becomes `"No hay apoyos que cumplan años hoy en {$campaign->name}"`
    - L103: `"{$voters->count()} votantes cumplen años hoy"` becomes `"{$voters->count()} apoyos cumplen años hoy"`
    - L115: `"Rate limit alcanzado para votante {$voter->full_name}"` becomes `"Rate limit alcanzado para apoyo {$voter->full_name}"`

    app/Console/Commands/TestHablameSms.php:
    - L114: `'Creando votante de prueba...'` becomes `'Creando apoyo de prueba...'`
    - L121: `"Votante creado: {$voter->full_name}"` becomes `"Apoyo creado: {$voter->full_name}"`
  </action>
  <verify>
    <automated>grep -rEc "[Vv]otante" app/Console/Commands/CreateVoterRecordsForUsers.php app/Console/Commands/SendBirthdayMessages.php app/Console/Commands/TestHablameSms.php | awk -F: '{s+=$2} END {print s+0}'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above prints 0
    - `grep -n "users:create-voter-records" app/Console/Commands/CreateVoterRecordsForUsers.php` still matches (command signature untouched)
  </acceptance_criteria>
  <done>All operator-visible console output uses Apoyo/apoyo; command signatures unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Rename in public registration controller/view, update PublicVoterRegistrationLinkTest.php</name>
  <files>app/Http/Controllers/PublicVoterRegistrationController.php, resources/views/public/voter-registration.blade.php, tests/Feature/PublicVoterRegistrationLinkTest.php</files>
  <read_first>
    - app/Http/Controllers/PublicVoterRegistrationController.php (line around 150; leave L154's Log::error message alone, it is not user-facing)
    - resources/views/public/voter-registration.blade.php (lines around 1, 36, 39, 99, 256)
    - tests/Feature/PublicVoterRegistrationLinkTest.php (line around 36)
  </read_first>
  <action>
    app/Http/Controllers/PublicVoterRegistrationController.php L150: flash message "Tu registro fue enviado correctamente. Si necesitas registrar otro votante, vuelve a abrir el enlace." becomes "... Si necesitas registrar otro apoyo, vuelve a abrir el enlace." Do NOT change L154 (`Log::error('Error en registro público de votantes', [...])`) — that is an internal log message, not user-facing.

    resources/views/public/voter-registration.blade.php:
    - L1: page title `'Registro de votantes'` becomes `'Registro de apoyos'`
    - L36: heading "Registro de votantes" becomes "Registro de apoyos"
    - L39: subtext "Completa el formulario. El votante quedará asociado al líder indicado por este enlace." becomes "... El apoyo quedará asociado al líder indicado por este enlace."
    - L99: helper text "Si necesitas registrar más de un votante, puedes volver a abrir este enlace y completar el formulario nuevamente." becomes "Si necesitas registrar más de un apoyo, ..."
    - L256: helper text "Selecciona el puesto de votación del votante (opcional)." becomes "Selecciona el puesto de votación del apoyo (opcional)."

    tests/Feature/PublicVoterRegistrationLinkTest.php L36: `$response->assertSee('Registro de votantes');` becomes `$response->assertSee('Registro de apoyos');`
  </action>
  <verify>
    <automated>php artisan test --filter=PublicVoterRegistrationLinkTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=PublicVoterRegistrationLinkTest` exits 0
    - `grep -rEc "[Vv]otante" resources/views/public/voter-registration.blade.php` prints 0
    - `grep -n "Log::error" app/Http/Controllers/PublicVoterRegistrationController.php` still contains "votantes" (log message intentionally left as-is, confirming it was not accidentally touched)
  </acceptance_criteria>
  <done>Public registration page and its user-visible flash message use Apoyo/apoyo; the test asserting on page text passes.</done>
</task>

</tasks>

<verification>
Run `php artisan test --filter=PublicVoterRegistrationLinkTest`. Run `grep -rEc "[Vv]otante" app/Exports app/Console/Commands app/Enums/UserRole.php app/Enums/CampaignStatus.php app/Http/Controllers/PublicVoterRegistrationController.php resources/views/public/voter-registration.blade.php | awk -F: '{s+=$2} END {print s+0}'` and confirm it prints 1 (only the intentionally-untouched Log::error line in the controller). Run `vendor/bin/pint --dirty`.
</verification>

<success_criteria>
- Export headers, console output, enum descriptions, and the public registration page use Apoyo/apoyo
- PublicVoterRegistrationLinkTest passes
- The one intentional exception (internal log message) is verified, not accidental
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv/02.1-06-SUMMARY.md`
</output>

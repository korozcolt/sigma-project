---
phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv
plan: 08
subsystem: database
tags: [laravel, filament, eloquent, migrations, validation-histories, rbac]

# Dependency graph
requires:
  - phase: 02.1-02
    provides: Gremio/Subcategoria global catalog models, migrations, and factories
  - phase: 02.1-03
    provides: voters.duplicate_sequence column, VoterStatus::DUPLICATE, VoterDuplicateAssignmentService
provides:
  - voters.gremio_id, voters.subcategoria_id, voters.lugar_expedicion_cedula, voters.placa (all nullable)
  - Voter::gremio() / Voter::subcategoria() BelongsTo relations
  - "Reasignar dueño de duplicado" Filament header action on EditVoter (admin_campaign/super_admin only, DUPLICATE-status only)
  - Mandatory-note-audited duplicate reassignment writes to validation_histories (validation_type=duplicate_reassignment)
affects: [02.1-import-masivo-csv, 04.1-reportes-avanzados-de-apoyos]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Filament header Action with ->visible() role+status gate, ->form([Textarea required]) for mandatory audit note, ->action() writing to ValidationHistory mirroring FinalizeElectionEvent job's audit-write pattern"

key-files:
  created:
    - database/migrations/2026_07_22_000004_add_classification_fields_to_voters_table.php
  modified:
    - app/Models/Voter.php
    - app/Filament/Resources/Voters/Pages/EditVoter.php
    - tests/Unit/Models/GremioSubcategoriaTest.php
    - tests/Feature/ApoyoDuplicateSequenceTest.php

key-decisions:
  - "Pint's no_unused_imports rule removed the explicit `use App\\Models\\Gremio;`/`use App\\Models\\Subcategoria;` statements from Voter.php since Gremio/Subcategoria already live in the same App\\Models namespace as Voter — bare class references (Gremio::class) resolve without an import. This is a tooling-enforced deviation from the plan's literal snippet, not a CLAUDE.md violation (no aliasing or inline `\\App\\Models\\...` paths were introduced)."

requirements-completed: [D-03, D-10]

# Metrics
duration: 15min
completed: 2026-07-23
---

# Phase 02.1 Plan 08: Voter Classification Fields + Duplicate Reassignment Summary

**Added nullable gremio/subcategoria/document columns to voters plus an admin-only, mandatory-note-audited "reassign duplicate owner" Filament action that never touches duplicate_sequence.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-07-23T04:56:12Z
- **Completed:** 2026-07-23T04:59:18Z
- **Tasks:** 2 completed
- **Files modified:** 5 (1 created, 4 modified)

## Accomplishments
- `voters` table now has `gremio_id`, `subcategoria_id`, `lugar_expedicion_cedula`, `placa` — all nullable per client's CSV column spec (D-05)
- `Voter` model exposes `gremio()`/`subcategoria()` BelongsTo relations, bridging the global Gremio/Subcategoria catalog (plan 02.1-02) onto individual voter records
- Admin-only "Reasignar dueño de duplicado" action added to the Voter edit page: visible only to `admin_campaign`/`super_admin` and only when `status === DUPLICATE`
- Reassignment requires a mandatory note, writes a fully audited `validation_histories` row (`validation_type = duplicate_reassignment`), and moves the voter back to `PENDING_REVIEW`
- Verified `duplicate_sequence` is never touched by the reassignment action (D-10 immutability) — confirmed both by code (no `duplicate_sequence` reference in `EditVoter.php`) and by test assertion

## Task Commits

Each task was committed atomically:

1. **Task 1: Migration for gremio/subcategoria/document fields + Voter model relations** - `b0ca971` (feat)
2. **Task 2: Reassign-duplicate-owner Filament action (D-03, D-10)** - `f794fac` (feat)

**Plan metadata:** _(this commit, docs: complete plan)_

## Files Created/Modified
- `database/migrations/2026_07_22_000004_add_classification_fields_to_voters_table.php` - Adds gremio_id/subcategoria_id (FKs, nullOnDelete) + lugar_expedicion_cedula/placa (nullable strings) to voters
- `app/Models/Voter.php` - Adds 4 fields to `$fillable`, adds `gremio()`/`subcategoria()` BelongsTo relations
- `app/Filament/Resources/Voters/Pages/EditVoter.php` - Adds `reassignDuplicateOwner` header Action with role/status visibility gate, mandatory-note form, and ValidationHistory audit write
- `tests/Unit/Models/GremioSubcategoriaTest.php` - Un-skips the previously-red Voter-optional-gremio/subcategoria test; adds relation-resolution and document-field-optional coverage
- `tests/Feature/ApoyoDuplicateSequenceTest.php` - Adds action-visibility tests (visible for admin+DUPLICATE, hidden for non-DUPLICATE, hidden for leader/coordinator); previously-scaffolded reassignment-audit and immutability tests now pass

## Decisions Made
- Followed the plan's exact migration/relation/action code as specified. The only deviation was Pint auto-removing the same-namespace `use` imports for `Gremio`/`Subcategoria` in `Voter.php` (see Deviations below) — functionally identical, just idiomatic for same-namespace classes.
- Added 3 extra behavior tests beyond what was pre-scaffolded (Voter gremio/subcategoria relation resolution, document fields optional, and action-visibility for status/role gates) to fully cover the plan's `<behavior>` specification under TDD, since only the reassignment-audit and immutability tests were already scaffolded in plan 02.1-01.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Tooling-enforced style fix] Pint removed same-namespace `use` imports in Voter.php**
- **Found during:** Task 1 (running `vendor/bin/pint --dirty` before finalizing)
- **Issue:** The plan's snippet added `use App\Models\Gremio;` and `use App\Models\Subcategoria;` to `Voter.php`. Since `Voter` itself lives in `App\Models`, these are same-namespace imports that PHP resolves automatically; Pint's `no_unused_imports` rule flags and removes them.
- **Fix:** Allowed Pint's auto-fix to stand — `Gremio::class`/`Subcategoria::class` still resolve correctly via same-namespace resolution, satisfying CLAUDE.md's ban on aliases/inline `\App\Models\...` paths (neither pattern was used).
- **Files modified:** app/Models/Voter.php
- **Verification:** `php artisan test --filter=GremioSubcategoriaTest` — all 8 tests pass with the relations intact.
- **Committed in:** b0ca971 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (tooling/style, no functional impact)
**Impact on plan:** No scope creep — same-namespace import removal is a cosmetic Pint convention, not a behavior change.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Voter classification fields (gremio/subcategoria/lugar_expedicion_cedula/placa) are now available for the CSV mass-import plan to populate.
- D-03 (mandatory-note audit for duplicate reassignment) and D-10 (duplicate_sequence immutability) are both fully implemented and test-covered.
- No blockers for subsequent plans in this phase or for Phase 04.1 (advanced reporting) which will read gremio/subcategoria data.

---
*Phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv*
*Completed: 2026-07-23*

## Self-Check: PASSED

All created/modified files confirmed present on disk; both task commits (b0ca971, f794fac) confirmed in git history.

---
phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv
plan: 01
subsystem: testing
tags: [pest, filament, imports, tdd-scaffolding, migrations]

requires: []
provides:
  - "notifications/imports/exports/failed_import_rows tables migrated (Filament ImportAction infrastructure)"
  - "RED test scaffolds for D-02/D-03/D-08/D-10 (tests/Feature/ApoyoDuplicateSequenceTest.php, tests/Feature/ApoyoLeaderExclusionTest.php)"
  - "RED test scaffolds for D-04/D-05/D-06/D-07/D-09 (tests/Feature/Filament/ApoyoImporterTest.php, tests/Unit/Models/GremioSubcategoriaTest.php)"
affects: ["02.1-02", "02.1-03", "02.1-04", "02.1-08", "02.1-09", "02.1-10"]

tech-stack:
  added: []
  patterns: ["Filament native ImportAction infrastructure (job batches + notifications + imports/exports/failed_import_rows tables)", "Wave 0 RED-test scaffolding: reference not-yet-existing classes/columns directly so tests fail/error for the right reason until later plans implement them"]

key-files:
  created:
    - database/migrations/2026_07_23_040727_create_notifications_table.php
    - database/migrations/2026_07_23_040731_create_imports_table.php
    - database/migrations/2026_07_23_040732_create_exports_table.php
    - database/migrations/2026_07_23_040733_create_failed_import_rows_table.php
    - tests/Feature/ApoyoDuplicateSequenceTest.php
    - tests/Feature/ApoyoLeaderExclusionTest.php
    - tests/Feature/Filament/ApoyoImporterTest.php
    - tests/Unit/Models/GremioSubcategoriaTest.php
  modified: []

key-decisions:
  - "job_batches migration was NOT created (already existed in 0001_01_01_000002_create_jobs_table.php) - only notifications + imports + exports + failed_import_rows were published/created, per the plan's explicit warning"
  - "notifications table uses morphs('notifiable') not uuidMorphs(), matching User's integer auto-increment PK - no edit needed, Filament's publish stub already generated the correct variant"
  - "Reassignment RED tests (ApoyoDuplicateSequenceTest) exercise the exact Livewire interaction shape (EditVoter Livewire page + callAction('reassignDuplicateOwner', ['notes' => ...])) that plan 02.1-08 is scoped to implement, so the scaffold requires no further behavioral redesign later, only the real action code"
  - "Leader-exclusion RED tests instantiate App\\Rules\\DocumentNotBelongsToLeaderOrCoordinator directly and call ->validate() with a closure-based $fail capture, matching plan 02.1-04's own interface spec verbatim"

patterns-established:
  - "RED scaffold tests intentionally reference future classes/columns (VoterStatus::DUPLICATE, App\\Rules\\DocumentNotBelongsToLeaderOrCoordinator, App\\Models\\Gremio/Subcategoria, App\\Filament\\Imports\\ApoyoImporter) so `php artisan test --filter=...` fails/errors for a documented, expected reason rather than 'class not found by accident'"

requirements-completed: [D-02, D-03, D-04, D-05, D-06, D-07, D-08]

duration: 45min
completed: 2026-07-22
---

# Phase 02.1 Plan 01: Wave 0 Test Scaffolding + Filament Import Infrastructure Summary

**Published Filament's ImportAction infrastructure tables (notifications/imports/exports/failed_import_rows) and scaffolded 4 RED test files (19 total test cases) describing every new behavior D-02 through D-10 introduce, ahead of their implementation in later Phase 02.1 plans.**

## Performance

- **Duration:** ~45 min (including worktree environment bootstrap: composer install, .env setup, sqlite DB creation, and syncing a stale worktree branch with 5 missing planning commits from `main`)
- **Completed:** 2026-07-22
- **Tasks:** 3/3 completed
- **Files modified:** 8 (4 migrations, 4 test files)

## Accomplishments
- `notifications`, `imports`, `exports`, `failed_import_rows` tables published and migrated; Filament's `ImportAction` will not throw "no such table" when `ApoyoImporter` is built in plan 02.1-10
- 4 new test files (19 test cases) exist describing exact expected behavior for D-02 (duplicate alert), D-03 (reassignment audit), D-04/D-05/D-09 (Gremio/Subcategoria catalog), D-06/D-07 (CSV import), D-08 (leader/coordinator exclusion) — all fail/error today for documented reasons, none accidentally
- Every later plan in Phase 02.1 can now reference an existing `<automated>` test command instead of `MISSING`

## Task Commits

Each task was committed atomically:

1. **Task 1: Publish Filament ImportAction infrastructure migrations** - `82b7cca` (chore)
2. **Task 2: Scaffold RED tests for duplicate sequence and leader-exclusion behavior** - `9d0e804` (test)
3. **Task 3: Scaffold RED tests for CSV import and Gremio/Subcategoria catalog** - `fd5a560` (test)

_No TDD red→green→refactor cycle applies here — this plan's entire purpose is to land RED-state scaffolds intentionally, not to make anything pass._

## Files Created/Modified
- `database/migrations/2026_07_23_040727_create_notifications_table.php` - Laravel database notifications table (morphs, integer PK)
- `database/migrations/2026_07_23_040731_create_imports_table.php` - Filament import batch tracking
- `database/migrations/2026_07_23_040732_create_exports_table.php` - Filament export batch tracking (unused this phase, but required by the same publish tag)
- `database/migrations/2026_07_23_040733_create_failed_import_rows_table.php` - Per-row rejection detail for downloadable failed-rows CSV
- `tests/Feature/ApoyoDuplicateSequenceTest.php` - 5 tests: duplicate_sequence assignment (0/1/2) + reassignment audit trail (D-02/D-03/D-10)
- `tests/Feature/ApoyoLeaderExclusionTest.php` - 4 tests: leader/coordinator exclusion scoped away from admin_campaign, UserObserver auto-voter flow proven unaffected (D-08)
- `tests/Feature/Filament/ApoyoImporterTest.php` - 5 tests: role-restricted import action visibility (D-07) + partial success/rejection reporting (D-06)
- `tests/Unit/Models/GremioSubcategoriaTest.php` - 5 tests: hierarchical global catalog with no campaign scoping (D-04/D-05/D-09)

## Decisions Made
- Followed the plan's explicit instruction not to run `make:queue-batches-table` since `job_batches` already exists in `0001_01_01_000002_create_jobs_table.php` — confirmed via `read_first` before running any command.
- Wrote the duplicate-reassignment and CSV-import RED tests using the exact Livewire/Filament interaction shapes that plans 02.1-08 and 02.1-10 are scoped to implement (rather than generic placeholder assertions), by reading those downstream plans' `<interfaces>`/`<action>` blocks first. This minimizes future rework: later plans mostly need to make these tests pass, not redesign them.
- `ApoyoImporterTest`'s CSV-import test bodies reference `App\Filament\Imports\ApoyoImporter::getColumns()` directly to guarantee a deterministic "class not found" error (since `SomeClass::class` alone doesn't trigger autoloading) rather than relying on a Livewire assertion that might fail for an unrelated reason.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Bootstrapped a git worktree missing the phase's own planning commits**
- **Found during:** Task load / pre-execution setup
- **Issue:** The plan file `.planning/phases/02.1-.../02.1-01-PLAN.md` referenced in this task did not exist in the assigned worktree. `gsd-tools init` resolved paths against the main repository checkout (which had the phase planning committed at `90c9407`), but the worktree branch (`worktree-agent-a0874f5ff5261965d`) was still at an older commit (`c47a4b2`) that predated the phase's planning commits. Without them, `02.1-01-PLAN.md`, `02.1-CONTEXT.md`, `02.1-RESEARCH.md`, `02.1-VALIDATION.md`, and the updated `ROADMAP.md` were all missing from the worktree.
- **Fix:** Verified `c47a4b2` (worktree tip) was a strict ancestor of `main`'s `90c9407` (pure fast-forward, no divergent commits), then ran `git merge --ff-only main` inside the worktree. This brought in only the 5 already-committed planning-doc commits with zero risk of conflicting with any worktree-local code changes.
- **Files modified:** None directly (planning docs only, pulled via fast-forward merge)
- **Verification:** `git log --oneline -3` showed the worktree at `90c9407` post-merge; the phase directory and all 10 plan files were then readable.
- **Committed in:** N/A (fast-forward merge, no new commit created; commits already existed on `main`)

**2. [Rule 3 - Blocking] Worktree had no `vendor/`, `node_modules/`, `.env`, or sqlite database**
- **Found during:** Task load / pre-execution setup
- **Issue:** The worktree is a fresh checkout with `vendor/` and `node_modules/` gitignored — neither existed, and there was no `.env` file, so `php artisan` commands (including the plan's own `migrate:status` verification) could not run at all.
- **Fix:** Ran `composer install --no-interaction --prefer-dist`, copied `.env.example` to `.env`, ran `php artisan key:generate`, created an empty `database/database.sqlite` file, and ran the baseline `php artisan migrate` (sqlite is the project's default local `DB_CONNECTION`).
- **Files modified:** `.env` (new, gitignored — not committed), `database/database.sqlite` (new, gitignored — not committed)
- **Verification:** `php artisan migrate` ran cleanly through all pre-existing migrations before this plan's Task 1 added the four new ones.
- **Committed in:** N/A (gitignored local environment files, correctly not committed)

---

**Total deviations:** 2 auto-fixed (both Rule 3 - blocking environment setup issues, zero scope creep into the plan's actual deliverables)
**Impact on plan:** Both fixes were prerequisite infrastructure work with no bearing on the plan's business logic. No application code behavior was altered by either fix.

## Issues Encountered
- The plan's own suggested verification command (`php artisan test --filter=ApoyoDuplicateSequenceTest --filter=ApoyoLeaderExclusionTest`) triggers `artisan test`'s `--filter` "cannot be used more than once" warning and silently only applies the last `--filter`. Worked around by running each filter separately in this execution; documenting here in case a later plan copies the same two-filter command verbatim.

## User Setup Required

None - no external service configuration required. (The worktree-local `.env`/sqlite setup above is standard local dev bootstrap, not external service configuration, and does not need to be repeated by a human — it is already committed to disk in this worktree for any subsequent plan in this phase.)

## Next Phase Readiness

- Plan 02.1-02 (Gremio/Subcategoria catalog) can now reference `tests/Unit/Models/GremioSubcategoriaTest.php` as an existing, failing test target.
- Plan 02.1-03 (duplicate-sequence schema) and 02.1-08 (reassignment action) can now reference `tests/Feature/ApoyoDuplicateSequenceTest.php`.
- Plan 02.1-04 (leader-exclusion rule) can now reference `tests/Feature/ApoyoLeaderExclusionTest.php`.
- Plan 02.1-10 (ApoyoImporter) can now reference `tests/Feature/Filament/ApoyoImporterTest.php`.
- `notifications`/`imports`/`exports`/`failed_import_rows` tables are migrated in this worktree's local sqlite database; no blockers for Filament's `ImportAction` in plan 02.1-10.
- No blockers identified for Wave 1 plans (02.1-02, 02.1-04) to begin.

---
*Phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv*
*Completed: 2026-07-22*

## Self-Check: PASSED

All 8 created files verified present on disk. All 3 task commits (`82b7cca`, `9d0e804`, `fd5a560`) verified present in git history.

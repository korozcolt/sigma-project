---
phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv
plan: 04
subsystem: validation
tags: [laravel, validation-rule, spatie-permission, apoyo, leader-exclusion]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: RED test scaffold tests/Feature/ApoyoLeaderExclusionTest.php with the exact rule interface later plans are scoped to implement
provides:
  - "App\\Rules\\DocumentNotBelongsToLeaderOrCoordinator, a reusable Illuminate ValidationRule"
affects: [02.1-09 (Voter/Apoyo Filament form), 02.1-10 (CSV importer)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Standalone ValidationRule classes in app/Rules mirroring App\\Rules\\MaxTablesForPollingPlace style (constructor promotion, guard clauses, $fail closure)"

key-files:
  created:
    - app/Rules/DocumentNotBelongsToLeaderOrCoordinator.php
  modified: []

key-decisions:
  - "Rule scoped strictly to leader/coordinator roles via User::query()->role([...]) — admin_campaign is intentionally excluded per D-08"
  - "Rule is never attached to Voter model events; it is form/importer-only so UserObserver::created()'s direct Voter::create() call remains unaffected by construction"
  - "Optional $excludeVoterId constructor param lets callers (future Voter edit forms) allow a leader/coordinator's own linked system voter record to pass validation"

patterns-established:
  - "New standalone validation rules should mirror app/Rules/MaxTablesForPollingPlace.php: readonly constructor-promoted params, early-return guard clauses, single $fail() call"

requirements-completed: [D-08]

# Metrics
duration: 8min
completed: 2026-07-23
---

# Phase 02.1 Plan 04: DocumentNotBelongsToLeaderOrCoordinator Validation Rule Summary

**Standalone Illuminate ValidationRule that blocks registering an Apoyo whose document_number belongs to an existing leader/coordinator user, scoped to exclude admin_campaign, and provably compatible with UserObserver's auto-voter-creation flow.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-22T23:13:11-05:00 (immediately after 02.1-01 scaffold commit)
- **Completed:** 2026-07-23T04:42:59Z
- **Tasks:** 2 (Task 1 code, Task 2 verification-only — no changes needed)
- **Files modified:** 1 created

## Accomplishments
- Created `App\Rules\DocumentNotBelongsToLeaderOrCoordinator`, mirroring the exact style of `App\Rules\MaxTablesForPollingPlace`
- Confirmed the rule correctly excludes `admin_campaign` users (scoped strictly to `leader`/`coordinator` per D-08)
- Confirmed via test that `UserObserver::created()`'s direct `Voter::create()` call is unaffected by this rule (it is never wired into model events)
- Verified `tests/Feature/ApoyoLeaderExclusionTest.php` (Wave 0 RED scaffold) now passes fully GREEN, with zero regression in `tests/Feature/UserVoterRelationTest.php`

## Task Commits

Each task was committed atomically:

1. **Task 1: DocumentNotBelongsToLeaderOrCoordinator validation rule** - `ae0dae4` (feat)
2. **Task 2: Flesh out ApoyoLeaderExclusionTest.php with real assertions** - no commit (no code changes required — see Deviations)

_Note: TDD RED state for this plan's tests was already committed in Wave 0 (`9d0e804`, plan 02.1-01); this plan only needed to supply the GREEN implementation._

## Files Created/Modified
- `app/Rules/DocumentNotBelongsToLeaderOrCoordinator.php` - Standalone `ValidationRule` rejecting a `document_number` matching an existing leader/coordinator user, with optional `$excludeVoterId` bypass for that user's own system voter record

## Decisions Made
- No new decisions beyond what was already resolved in RESEARCH.md (D-08); implementation followed the plan's provided interface exactly.

## Deviations from Plan

**Task 2 required no code changes.** The Wave 0 scaffold (`tests/Feature/ApoyoLeaderExclusionTest.php`, committed in `9d0e804` as part of plan 02.1-01) already contained the exact 4 real `it(...)` assertions described in this plan's Task 2 action (leader rejection, coordinator rejection, admin_campaign allowance, and the UserObserver non-interference check) — there were no `TODO`/placeholder bodies to replace. Verified by:
- `grep -n "TODO\|placeholder" tests/Feature/ApoyoLeaderExclusionTest.php` → no matches
- `git status --short tests/Feature/ApoyoLeaderExclusionTest.php` → no diff (file unchanged from the Wave 0 commit)
- `php artisan test --filter=ApoyoLeaderExclusionTest` → 4 passed, 8 assertions
- `php artisan test --filter=UserVoterRelationTest` → 20 passed, 43 assertions (no regression)

This is not a scope reduction — the plan's required test coverage and behavior are fully satisfied; the work was simply already done as part of the prior wave's scaffold quality. No commit was made for Task 2 since there was nothing to change.

**Parallel-execution note:** Running `vendor/bin/pint --dirty` (per plan verification step) reformatted several files outside this plan's scope that were mid-edit by sibling agents running concurrently in the same working tree (e.g. `app/Enums/UserRole.php`, `app/Filament/Widgets/*`, `resources/views/livewire/leader/*.blade.php`). These are purely cosmetic whitespace/brace-style changes (no logic altered). Per the parallel-execution safety rules, none of these files were staged or committed by this plan — they remain as uncommitted working-tree noise for their owning agents to handle. `app/Rules/DocumentNotBelongsToLeaderOrCoordinator.php` itself was Pint-clean and committed as-is.

---

**Total deviations:** 0 auto-fixes (Rules 1-4 not triggered)
**Impact on plan:** None — plan executed exactly as written, Task 2 required no additional work.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `App\Rules\DocumentNotBelongsToLeaderOrCoordinator` is ready to be wired into the Voter/Apoyo Filament form (plan 02.1-09) and the CSV importer (plan 02.1-10), both of which can pass `excludeVoterId` when editing an existing voter record.
- No blockers.

---
*Phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv*
*Completed: 2026-07-23*

## Self-Check: PASSED
- FOUND: app/Rules/DocumentNotBelongsToLeaderOrCoordinator.php
- FOUND: ae0dae4 (feat commit)

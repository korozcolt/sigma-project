---
phase: 04.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Feature/DashboardWidgetsTest.php
  - tests/Feature/Filament/TopCoordinatorsTableTest.php
  - tests/Feature/Filament/TopPollingPlacesTableTest.php
  - tests/Feature/Filament/RejectionsReportTableTest.php
  - tests/Feature/Filament/DuplicatesReportTableTest.php
  - tests/Feature/Filament/JurisdictionReportTableTest.php
  - tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php
  - tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php
  - database/factories/PollingPlaceFactory.php
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]
must_haves:
  truths:
    - "A failing (RED) test exists proving TopLeadersTable currently violates D-01 (counts DUPLICATE voters)"
    - "A failing (RED) test exists for every new widget/export this phase will create, so Wave 1 has a GREEN target"
  artifacts:
    - path: "tests/Feature/Filament/TopCoordinatorsTableTest.php"
      provides: "RED spec for D-05 team-rollup coordinator ranking + D-01 exclusion"
    - path: "tests/Feature/Filament/DuplicatesReportTableTest.php"
      provides: "RED spec for D-06 cross-campaign sibling visibility"
    - path: "tests/Feature/Filament/JurisdictionReportTableTest.php"
      provides: "RED spec for D-04 Nacional hide condition + dentro/fuera comparison"
    - path: "database/factories/PollingPlaceFactory.php"
      provides: "Missing factory required to seed PollingPlace test data"
  key_links:
    - from: "tests/Feature/Filament/*Test.php"
      to: "app/Filament/Widgets/*.php (not yet created)"
      via: "use App\\Filament\\Widgets\\{Class}; Livewire::test({Class}::class)"
      pattern: "use App\\\\Filament\\\\Widgets\\\\"
---

<objective>
Wave 0 (TDD RED): write every test this phase needs BEFORE any widget/export implementation exists, per this project's TDD convention (confirmed pattern from Phase 02.1's plan structure) and per `04.1-VALIDATION.md`'s explicit Wave 0 requirement list.

Purpose: lock in the exact expected behavior for D-01 (duplicate exclusion), D-02 (widget+export pattern), D-03 (combined CSV), D-04 (jurisdiction Nacional-hide), D-05 (coordinator team-rollup), and D-06 (duplicates cross-campaign exception) as executable specs, so Wave 1 plans implement against a fixed target instead of inventing behavior ad hoc.

Output: 8 test files (1 extended, 7 new) + 1 missing model factory, all RED (failing due to missing classes/behavior), committed as the RED half of this phase's TDD cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-reportes-avanzados-de-apoyos-ranking-lider-coordinador-puesto-votacion-informe-rechazos-informe-duplicados-export-csv-plano-informe-jurisdiccion-dentro-fuera/04.1-CONTEXT.md
@.planning/phases/04.1-reportes-avanzados-de-apoyos-ranking-lider-coordinador-puesto-votacion-informe-rechazos-informe-duplicados-export-csv-plano-informe-jurisdiccion-dentro-fuera/04.1-RESEARCH.md
@.planning/phases/04.1-reportes-avanzados-de-apoyos-ranking-lider-coordinador-puesto-votacion-informe-rechazos-informe-duplicados-export-csv-plano-informe-jurisdiccion-dentro-fuera/04.1-VALIDATION.md
@tests/Feature/DashboardWidgetsTest.php
@app/Filament/Widgets/TopLeadersTable.php
@app/Models/Voter.php
@app/Models/User.php
@app/Models/Campaign.php
@app/Models/PollingPlace.php
@app/Models/VerificationCall.php
@app/Enums/VoterStatus.php
@app/Enums/CallResult.php
@app/Enums/CampaignScope.php
@app/Enums/UserRole.php
@database/factories/VoterFactory.php
@database/factories/CampaignFactory.php
@database/factories/MunicipalityFactory.php
</context>

<interfaces>
Contracts the RED tests reference. These classes DO NOT EXIST YET on disk — that is expected;
tests must fail with a class-not-found error until Wave 1 (plans 04.1-02/03/04) creates them.

Widgets Wave 1 will create (tests reference by class name):
- App\Filament\Widgets\TopCoordinatorsTable       (plan 04.1-02)
- App\Filament\Widgets\TopPollingPlacesTable      (plan 04.1-02)
- App\Filament\Widgets\RejectionsReportTable      (plan 04.1-03)
- App\Filament\Widgets\DuplicatesReportTable      (plan 04.1-03)
- App\Filament\Widgets\JurisdictionReportTable    (plan 04.1-04)
- App\Filament\Widgets\ApoyosLideresCoordinadoresTable (plan 04.1-04)

Export Wave 1 will create:
- App\Exports\ApoyosLideresCoordinadoresExport(protected ?int $campaignId = null)
  - query(): Builder — Voter::query()->when($campaignId, ...)->with(['registeredBy.coordinator', ...])
  - map($voter): array

Existing service/model contracts the tests use directly:
- App\Services\VoterDuplicateAssignmentService::siblingsFor(string $documentNumber): Collection — bypasses campaign global scope
- App\Services\CampaignContext::currentCampaign(?User $user = null): ?Campaign
- App\Services\CampaignContext::currentCampaignId(?User $user = null): ?int
- App\Models\Voter fields/relations: campaign_id, document_number, duplicate_sequence, status (VoterStatus enum), registered_by, municipality_id, polling_place_id, gremio_id, subcategoria_id; registeredBy(): BelongsTo -> User; verificationCalls(): HasMany -> VerificationCall; municipality(): BelongsTo
- App\Models\User: coordinator(): BelongsTo (coordinator_user_id -> User); leaders(): HasMany (inverse of coordinator_user_id); registeredVoters(): HasMany (Voter::registered_by)
- App\Models\VerificationCall: call_result cast to App\Enums\CallResult; voter(): BelongsTo

Filament testing helpers used below (verified in vendor/filament/tables/src/Testing/):
- Livewire::test(WidgetClass::class)->assertOk()
- ->assertCanSeeTableRecords($models) / ->assertCanNotSeeTableRecords($models)
- ->assertCountTableRecords(int $count)
- ->assertTableColumnStateSet(string $column, $expected, $record)
- ->assertTableHeaderActionsExistInOrder(['export'])  — proves D-02's export button exists without invoking the actual download

Scaffolding note (per CLAUDE.md: "`php artisan make:` for all new files. Always `--no-interaction`."):
Every brand-new test file below is scaffolded via `php artisan make:test --pest {path} --no-interaction` (add `--unit` for tests/Unit) BEFORE being edited. `tests/Feature/DashboardWidgetsTest.php` already exists and is only being EXTENDED — no `make:` command for it. `database/factories/PollingPlaceFactory.php` is scaffolded via `php artisan make:factory PollingPlaceFactory --model=PollingPlace --no-interaction`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests — D-01 regression fix + coordinator/polling-place rankings</name>
  <files>tests/Feature/DashboardWidgetsTest.php, tests/Feature/Filament/TopCoordinatorsTableTest.php, tests/Feature/Filament/TopPollingPlacesTableTest.php, database/factories/PollingPlaceFactory.php</files>
  <read_first>
    tests/Feature/DashboardWidgetsTest.php
    app/Filament/Widgets/TopLeadersTable.php
    app/Models/PollingPlace.php
    database/factories/MunicipalityFactory.php
    database/factories/DepartmentFactory.php
  </read_first>
  <behavior>
    - Test 1 (extend DashboardWidgetsTest.php): a leader with 5 valid (PENDING_REVIEW) voters + 3 voters explicitly created with status => VoterStatus::DUPLICATE, duplicate_sequence => 1 in the SAME campaign must show registered_voters_count === 5 in TopLeadersTable, not 8. This is the direct regression test for the documented D-01 bug in app/Filament/Widgets/TopLeadersTable.php.
    - Test 2 (TopCoordinatorsTableTest.php): a coordinator with 2 leaders — leader A has 10 valid + 2 DUPLICATE voters, leader B has 5 valid voters — must show the coordinator's apoyos_equipo_count as exactly 15 (10 + 5, DUPLICATE excluded), and leaders_count as 2. Also test: a coordinator with 0 leaders/0 apoyos in the active campaign is still VISIBLE in the table (this is a coverage report, not a top-N ranking that hides zero rows — the D-05/item-1 "meta vs real" gap-visibility requirement, deliberately different from TopLeadersTable's zero-exclusion behavior). Also test: a coordinator belonging to a DIFFERENT campaign is not shown.
    - Test 3 (TopPollingPlacesTableTest.php): 3 voters at PollingPlace A (2 valid + 1 DUPLICATE), 2 voters at PollingPlace B (both valid) -> PollingPlace A shows apoyos_count = 2 (not 3), PollingPlace B shows apoyos_count = 2. Also test: a voter with polling_place_id = null does not appear under any polling place and does not break the widget (assertOk()).
  </behavior>
  <action>
    In tests/Feature/DashboardWidgetsTest.php (EXISTING file — extend it, no `make:` command needed), add (after the existing 'top leaders table does not show users without voters' test, using the existing beforeEach campaign/municipality fixtures already defined at the top of the file):

    test('top leaders table excludes DUPLICATE status voters from the ranking count (D-01)', function () {
        $leader = User::factory()->create(['municipality_id' => $this->municipality->id]);
        $leader->campaigns()->attach($this->campaign);

        Voter::factory()->count(5)->create([
            'campaign_id' => $this->campaign->id,
            'municipality_id' => $this->municipality->id,
            'registered_by' => $leader->id,
        ]);

        foreach (range(1, 3) as $i) {
            Voter::factory()->create([
                'campaign_id' => $this->campaign->id,
                'municipality_id' => $this->municipality->id,
                'registered_by' => $leader->id,
                'document_number' => "900000000{$i}",
                'status' => VoterStatus::DUPLICATE,
                'duplicate_sequence' => 1,
            ]);
        }

        Livewire::test(TopLeadersTable::class)
            ->assertCanSeeTableRecords([$leader])
            ->assertTableColumnStateSet('registered_voters_count', 5, $leader);
    });

    VoterStatus is already imported at the top of this file; no new imports needed for this test.

    Run `php artisan make:test --pest Filament/TopCoordinatorsTableTest --no-interaction` to scaffold the new tests/Feature/Filament/TopCoordinatorsTableTest.php file, then edit it to mirror the beforeEach structure of DashboardWidgetsTest.php: create SUPER_ADMIN role + user, act as them, create Department/Municipality, create an active Campaign, put it in session as campaign_context.campaign_id / campaign_context.mode = 'single'. Import App\Filament\Widgets\TopCoordinatorsTable. Write the 3 scenarios from behavior above using Livewire::test(TopCoordinatorsTable::class) and assertTableColumnStateSet('apoyos_equipo_count', 15, $coordinator) / assertTableColumnStateSet('leaders_count', 2, $coordinator). Use User::factory()->create() + ->assignRole(UserRole::COORDINATOR->value) for coordinators, and set coordinator_user_id on leader Users to link them (User::factory()->create(['coordinator_user_id' => $coordinator->id])). End with assertTableHeaderActionsExistInOrder(['export']).

    First run `php artisan make:factory PollingPlaceFactory --model=PollingPlace --no-interaction` to scaffold database/factories/PollingPlaceFactory.php (confirmed missing — no file matches database/factories/*Polling*), then edit the generated class modeled on MunicipalityFactory/DepartmentFactory, with fields: department_id (Department::factory()), municipality_id (Municipality::factory()), dane_department_code (fake()->numberBetween(1,99)), dane_municipality_code (fake()->numberBetween(1,999)), zone_code (fake()->numberBetween(1,9)), place_code (fake()->unique()->numberBetween(1,999)), name (fake()->company().' - Puesto de Votacion'), address (fake()->address()), commune (null), max_tables (fake()->numberBetween(1,20)).

    Then run `php artisan make:test --pest Filament/TopPollingPlacesTableTest --no-interaction` to scaffold tests/Feature/Filament/TopPollingPlacesTableTest.php (same fixture pattern as TopCoordinatorsTableTest.php), then write the polling-place ranking test using PollingPlace::factory()->create(['municipality_id' => $this->municipality->id]) and voters with matching polling_place_id, asserting apoyos_count via assertTableColumnStateSet, and one voter with polling_place_id => null asserting assertOk() doesn't error.
  </action>
  <verify>
    <automated>php artisan test --filter="excludes DUPLICATE status voters" ; php artisan test tests/Feature/Filament/TopCoordinatorsTableTest.php ; php artisan test tests/Feature/Filament/TopPollingPlacesTableTest.php</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "excludes DUPLICATE status voters" tests/Feature/DashboardWidgetsTest.php returns a match
    - tests/Feature/Filament/TopCoordinatorsTableTest.php exists and contains "use App\Filament\Widgets\TopCoordinatorsTable;"
    - tests/Feature/Filament/TopPollingPlacesTableTest.php exists and contains "use App\Filament\Widgets\TopPollingPlacesTable;"
    - database/factories/PollingPlaceFactory.php exists and extends Illuminate\Database\Eloquent\Factories\Factory
    - Running php artisan test tests/Feature/Filament/TopCoordinatorsTableTest.php FAILS with a class-not-found/undefined-class error (RED — expected, TopCoordinatorsTable doesn't exist until Wave 1)
    - Running php artisan test --filter="excludes DUPLICATE status voters" FAILS (RED — TopLeadersTable is not yet fixed)
  </acceptance_criteria>
  <done>3 RED test files exist (1 extended, 2 new) plus the missing PollingPlaceFactory; all fail for the expected reason (missing class / unfixed bug), none fail due to test-authoring mistakes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests — rejections, duplicates (D-06), jurisdiction (D-04) report widgets</name>
  <files>tests/Feature/Filament/RejectionsReportTableTest.php, tests/Feature/Filament/DuplicatesReportTableTest.php, tests/Feature/Filament/JurisdictionReportTableTest.php</files>
  <read_first>
    tests/Feature/ApoyoDuplicateSequenceTest.php
    app/Services/VoterDuplicateAssignmentService.php
    app/Models/VerificationCall.php
    app/Enums/CallResult.php
    app/Models/Campaign.php
    app/Enums/CampaignScope.php
    app/Enums/ElectionType.php
  </read_first>
  <behavior>
    - RejectionsReportTableTest: voter A has status = REJECTED_CENSUS, no calls -> appears once. Voter B has status = PENDING_REVIEW but has a VerificationCall with call_result = CallResult::REJECTED -> appears once (proves the call-based inclusion path). Voter C has BOTH status = REJECTED_CENSUS AND a VerificationCall with call_result = CallResult::NOT_INTERESTED -> appears EXACTLY ONCE, not twice (no double-counting). Voter D has status = CONFIRMED and no rejection calls -> excluded. Assert total record count via assertCountTableRecords(3).
    - DuplicatesReportTableTest (D-06 — the ONE intentional cross-campaign exception in this phase): Campaign A is active (session context). Create Voter X in Campaign A with document_number = '5551111111', duplicate_sequence = 0. Create Voter Y with the SAME document_number but registered under Campaign B — to do this, temporarily switch Session::put('campaign_context.campaign_id', $campaignB->id) before creating Voter Y with duplicate_sequence = 1, status = VoterStatus::DUPLICATE, then switch the session back to Campaign A before mounting the widget. Assert the widget (mounted while Campaign A is active) shows BOTH Voter X and Voter Y — proving cross-campaign sibling visibility per D-06 (this is the ONE report in the entire phase allowed to do this; every other widget in this phase must stay campaign-scoped). Also test: an entirely separate duplicate pair that exists ONLY inside Campaign C (unrelated to the active Campaign A) does NOT appear (the report is not "show me every duplicate in the system", only groups touching the active campaign). Also test: a normal non-disputed voter (duplicate_sequence = 0, no siblings) in Campaign A does NOT appear (not an actual duplicate group).
    - JurisdictionReportTableTest (D-04): (a) Campaign with election_type forcing scope = CampaignScope::Nacional (Campaign::factory()->create(['election_type' => App\Enums\ElectionType::PRESIDENT]); confirmed via Campaign::boot()'s saving() hook this yields scope = Nacional with municipality_id/department_id both null) -> assert JurisdictionReportTable::canView() returns false. (b) Municipal-scope campaign (ElectionType::MAYOR) with municipality_id set: create 3 voters in that same municipality (expect "Dentro"), 2 voters in a different municipality (expect "Fuera") -> widget renders and assertSee('Dentro') and assertSee('Fuera'). (c) Departmental-scope campaign (ElectionType::GOVERNOR): create a voter whose municipality shares the campaign's department_id (Dentro) vs. one whose municipality's department differs (Fuera).
  </behavior>
  <action>
    Run `php artisan make:test --pest Filament/RejectionsReportTableTest --no-interaction`, `php artisan make:test --pest Filament/DuplicatesReportTableTest --no-interaction`, and `php artisan make:test --pest Filament/JurisdictionReportTableTest --no-interaction` to scaffold the 3 new test files under tests/Feature/Filament/. Then edit each: import the not-yet-existing widget class (RejectionsReportTable, DuplicatesReportTable, JurisdictionReportTable respectively from App\Filament\Widgets) and follow the same beforeEach scaffold pattern used in Task 1 (SUPER_ADMIN role + user + active Campaign in session).

    For DuplicatesReportTableTest.php, the cross-campaign fixture MUST look like this (do not simplify away the session-switch — it is the only way to legitimately create a Voter under a non-active campaign given CampaignContext::enforceCampaignId() always overwrites campaign_id to the currently active context when one is set):

    $campaignB = Campaign::factory()->create(['status' => 'active']);

    Session::put('campaign_context.campaign_id', $campaignB->id);
    Session::put('campaign_context.mode', 'single');

    $voterY = Voter::factory()->create([
        'campaign_id' => $campaignB->id,
        'municipality_id' => $this->municipality->id,
        'document_number' => '5551111111',
        'duplicate_sequence' => 1,
        'status' => VoterStatus::DUPLICATE,
    ]);

    Session::put('campaign_context.campaign_id', $this->campaign->id);
    Session::put('campaign_context.mode', 'single');

    Livewire::test(DuplicatesReportTable::class)
        ->assertCanSeeTableRecords([$voterX, $voterY]);

    For JurisdictionReportTableTest.php's Nacional-scope test, DO NOT use Livewire::test() for the hide assertion — canView() is a static gate consulted by the panel/dashboard registration layer, not by the widget's own render method, so mounting it directly would still render. Assert directly instead:

    $nacional = Campaign::factory()->create(['election_type' => \App\Enums\ElectionType::PRESIDENT, 'status' => 'active']);
    Session::put('campaign_context.campaign_id', $nacional->id);
    Session::put('campaign_context.mode', 'single');

    expect(\App\Filament\Widgets\JurisdictionReportTable::canView())->toBeFalse();

    Every test file ends with an export-button existence assertion: ->assertTableHeaderActionsExistInOrder(['export']) (skip this specific assertion only for the static canView() Nacional test, since that scenario never mounts the widget).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Filament/RejectionsReportTableTest.php tests/Feature/Filament/DuplicatesReportTableTest.php tests/Feature/Filament/JurisdictionReportTableTest.php</automated>
  </verify>
  <acceptance_criteria>
    - tests/Feature/Filament/RejectionsReportTableTest.php contains "use App\Filament\Widgets\RejectionsReportTable;" and an assertCountTableRecords(3) assertion
    - tests/Feature/Filament/DuplicatesReportTableTest.php contains "use App\Filament\Widgets\DuplicatesReportTable;" and both a cross-campaign session-switch block and an assertCanSeeTableRecords([$voterX, $voterY]) assertion
    - tests/Feature/Filament/JurisdictionReportTableTest.php contains "use App\Filament\Widgets\JurisdictionReportTable;" and a `JurisdictionReportTable::canView()` assertion for the Nacional case
    - All 3 test files FAIL with class-not-found errors when run (RED — expected, widgets don't exist until Wave 1)
  </acceptance_criteria>
  <done>3 new RED test files exist, each encoding the exact behavior (including the D-06 cross-campaign exception and D-04 Nacional-hide) that Wave 1 must satisfy to go GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED tests — combined CSV export (D-03) + its dashboard widget</name>
  <files>tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php, tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php</files>
  <read_first>
    tests/Unit/Exports/VotersExportTest.php
    app/Exports/VotersExport.php
    app/Models/User.php
    app/Enums/UserRole.php
  </read_first>
  <behavior>
    - ApoyosLideresCoordinadoresExportTest.php (mirrors VotersExportTest.php's style — inspect ->query()->toBase()->wheres, and call ->map() directly on a real model instance rather than downloading a file): (a) constructing `new ApoyosLideresCoordinadoresExport($campaignId)` produces a query filtered to `campaign_id = $campaignId` (assert via the same `collect($builder->wheres)->first(...)` pattern VotersExportTest.php uses). (b) `map()` on a Voter whose `registeredBy` is a plain LEADER with a coordinator assigned returns the leader's name/phone/email in the "lider" columns and the leader's coordinator's name/phone/email in the "coordinador" columns. (c) `map()` on a Voter whose `registeredBy` is a COORDINATOR who registered the Apoyo directly (no leader in between, `coordinator_user_id` irrelevant) returns the coordinator's own name/phone/email in the "coordinador" columns and 'N/A' (or equivalent blank marker) in the "lider" columns — this is the edge case documented in 04.1-RESEARCH.md's Code Examples section.
    - ApoyosLideresCoordinadoresTableTest.php: widget renders (assertOk()), shows a Voter row with líder+coordinador columns populated per the same two scenarios as above, and has a working export header action (assertTableHeaderActionsExistInOrder(['export'])).
  </behavior>
  <action>
    Run `php artisan make:test --pest --unit Exports/ApoyosLideresCoordinadoresExportTest --no-interaction` to scaffold tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php, then edit it to follow the exact structure of tests/Unit/Exports/VotersExportTest.php (`uses(Tests\TestCase::class, Illuminate\Foundation\Testing\RefreshDatabase::class);`, `it(...)` blocks). Import `App\Exports\ApoyosLideresCoordinadoresExport`, `App\Models\Campaign`, `App\Models\User`, `App\Models\Voter`, `App\Enums\UserRole`. Build the query-filter test using `(new ApoyosLideresCoordinadoresExport($campaign->id))->query()->toBase()` and assert a `where` entry for `campaign_id`. Build the two `map()` tests by constructing real `User`/`Voter` model instances (with roles assigned via `assignRole()`) and calling `(new ApoyosLideresCoordinadoresExport())->map($voter)`, asserting on array offsets/values (e.g. `expect($row[11])->toBe($coordinator->name);` — pick offsets matching whatever column order you define in the `<action>` you write for the real export class in plan 04.1-04, and state that exact order as a code comment at the top of the test file so plan 04.1-04's implementation must match it exactly).

    Run `php artisan make:test --pest Filament/ApoyosLideresCoordinadoresTableTest --no-interaction` to scaffold tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php, then edit it using the standard beforeEach scaffold (SUPER_ADMIN + active Campaign in session). Import `App\Filament\Widgets\ApoyosLideresCoordinadoresTable`. Assert `assertOk()`, `assertCanSeeTableRecords([$voter])` for a voter in the active campaign, `assertCanNotSeeTableRecords([$otherVoter])` for a voter in a different campaign (this widget IS campaign-scoped, unlike DuplicatesReportTable — D-03's CSV plano is explicitly NOT one of the phase's cross-campaign exceptions per D-06), and `assertTableHeaderActionsExistInOrder(['export'])`.
  </action>
  <verify>
    <automated>php artisan test tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php</automated>
  </verify>
  <acceptance_criteria>
    - tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php exists, contains "use App\Exports\ApoyosLideresCoordinadoresExport;", and contains at least 3 `it(...)` blocks (query filter, leader-with-coordinator map, coordinator-registered-directly map)
    - tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php exists and contains "use App\Filament\Widgets\ApoyosLideresCoordinadoresTable;"
    - A code comment documenting the exact `map()` column order exists at the top of tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php
    - Both test files FAIL with class-not-found errors when run (RED — expected)
  </acceptance_criteria>
  <done>The D-03 combined CSV export and its dashboard widget both have RED specs, including the documented column-order contract Wave 1 (plan 04.1-04) must implement against exactly.</done>
</task>

</tasks>

<verification>
Run the full new-test surface together and confirm every file fails only for the expected reason (missing class), not for fixture/syntax mistakes:
`php artisan test tests/Feature/DashboardWidgetsTest.php tests/Feature/Filament/TopCoordinatorsTableTest.php tests/Feature/Filament/TopPollingPlacesTableTest.php tests/Feature/Filament/RejectionsReportTableTest.php tests/Feature/Filament/DuplicatesReportTableTest.php tests/Feature/Filament/JurisdictionReportTableTest.php tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php`
Existing DashboardWidgetsTest.php tests unrelated to the new D-01 test must remain green (no regression from the edit).
</verification>

<success_criteria>
- 8 test files exist (1 extended, 7 new) plus database/factories/PollingPlaceFactory.php
- Every new/extended test targeting not-yet-created classes fails with a class-not-found error (true RED, not a fixture bug)
- The D-01 regression test on the EXISTING TopLeadersTable fails because the bug is still present (true RED)
- `vendor/bin/pint --dirty` produces no violations on the new files
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-reportes-avanzados-de-apoyos-ranking-lider-coordinador-puesto-votacion-informe-rechazos-informe-duplicados-export-csv-plano-informe-jurisdiccion-dentro-fuera/04.1-01-SUMMARY.md`
</output>

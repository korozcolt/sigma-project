---
phase: 04.1-reportes-avanzados-de-apoyos-ranking-lider-coordinador-puesto-votacion-informe-rechazos-informe-duplicados-export-csv-plano-informe-jurisdiccion-dentro-fuera
plan: 01
subsystem: testing
tags: [pest, livewire, filament-tables, tdd-red, dashboard-widgets, exports]

# Dependency graph
requires:
  - phase: 02.1-apoyos-reglas-core-y-segmentacion
    provides: Apoyo duplicate-cedula tracking (duplicate_sequence, VoterStatus::DUPLICATE), VoterDuplicateAssignmentService::siblingsFor()
provides:
  - 8 RED Pest test files (1 extended, 7 new) locking in exact expected behavior for D-01 through D-06
  - Missing PollingPlaceFactory required to seed PollingPlace test data
  - Documented 15-column map() contract for the D-03 combined CSV export
affects: [04.1-02, 04.1-03, 04.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RED-first phase: all widget/export test specs written before any implementation exists, so Wave 1 plans implement against a fixed target"
    - "D-06 cross-campaign sibling visibility fixture: session-switch to a non-active campaign to legitimately create a Voter row under it, then switch back before mounting the widget"
    - "canView() static-gate assertion tested directly (not via Livewire::test()) since it is consulted by the panel registration layer, not the widget's own render"

key-files:
  created:
    - tests/Feature/Filament/TopCoordinatorsTableTest.php
    - tests/Feature/Filament/TopPollingPlacesTableTest.php
    - tests/Feature/Filament/RejectionsReportTableTest.php
    - tests/Feature/Filament/DuplicatesReportTableTest.php
    - tests/Feature/Filament/JurisdictionReportTableTest.php
    - tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php
    - tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php
    - database/factories/PollingPlaceFactory.php
  modified:
    - tests/Feature/DashboardWidgetsTest.php

key-decisions:
  - "Defined the D-03 combined CSV export's 15-column map() order as a code comment at the top of ApoyosLideresCoordinadoresExportTest.php (id, document_number, full_name, phone, email, municipality, address, status, registered_at, lider_nombre/telefono/email, coordinador_nombre/telefono/email) so plan 04.1-04 has a fixed implementation target"
  - "Extended the export-button existence assertion (assertTableHeaderActionsExistInOrder(['export'])) to TopPollingPlacesTable RED tests as well, for consistency with D-02's widget+export pattern across all ranking widgets"

patterns-established:
  - "Every new Filament table-widget RED test follows the same beforeEach scaffold: SUPER_ADMIN role + user, Department/Municipality, active Campaign in session (campaign_context.campaign_id / mode=single)"

requirements-completed: []  # D-01..D-06 are RED specs only in this plan; Wave 1 plans (04.1-02/03/04) close them out to GREEN

# Metrics
duration: 20min
completed: 2026-07-23
---

# Phase 04.1 Plan 01: RED Tests for Advanced Apoyo Reporting Summary

**8 Pest RED test files (1 extended, 7 new) plus a missing PollingPlaceFactory, encoding the exact expected behavior for D-01 through D-06 before any widget/export implementation exists.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-07-23T14:47:00Z
- **Completed:** 2026-07-23T14:53:00Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments
- Locked in the D-01 regression spec proving `TopLeadersTable` currently counts `VoterStatus::DUPLICATE` voters in `registered_voters_count` (confirmed still-failing RED against the existing, unfixed widget)
- Wrote RED specs for all 6 not-yet-created widgets (`TopCoordinatorsTable`, `TopPollingPlacesTable`, `RejectionsReportTable`, `DuplicatesReportTable`, `JurisdictionReportTable`, `ApoyosLideresCoordinadoresTable`) and the `ApoyosLideresCoordinadoresExport` class, all failing with class-not-found errors
- Encoded the D-06 cross-campaign sibling-visibility exception (the only report in the phase allowed to show voters across campaigns) via a session-switch fixture pattern
- Encoded the D-04 Nacional-scope hide behavior as a direct static `canView()` assertion (not a Livewire mount) since that's how the panel registration layer actually consults it
- Documented the exact 15-column order the combined CSV export's `map()` must produce, as a code comment at the top of its test file

## Task Commits

Each task was committed atomically:

1. **Task 1: RED tests — D-01 regression fix + coordinator/polling-place rankings** - `7065765` (test)
2. **Task 2: RED tests — rejections, duplicates (D-06), jurisdiction (D-04) report widgets** - `9a8b5ce` (test)
3. **Task 3: RED tests — combined CSV export (D-03) + its dashboard widget** - `d2b391f` (test)

_Note: This is a pure-RED TDD plan (Wave 0) — no GREEN/REFACTOR steps are expected here; Wave 1 plans (04.1-02/03/04) will add the implementation commits that turn these RED specs GREEN._

## Files Created/Modified
- `tests/Feature/DashboardWidgetsTest.php` - Added D-01 regression test (`registered_voters_count` must exclude DUPLICATE status)
- `tests/Feature/Filament/TopCoordinatorsTableTest.php` - D-05 team-rollup coordinator ranking RED spec
- `tests/Feature/Filament/TopPollingPlacesTableTest.php` - Polling-place ranking RED spec with D-01 exclusion + null-safety
- `tests/Feature/Filament/RejectionsReportTableTest.php` - Status-based + call-based rejection merge without double-counting
- `tests/Feature/Filament/DuplicatesReportTableTest.php` - D-06 cross-campaign sibling visibility RED spec
- `tests/Feature/Filament/JurisdictionReportTableTest.php` - D-04 Nacional-hide + dentro/fuera comparison RED spec
- `tests/Feature/Filament/ApoyosLideresCoordinadoresTableTest.php` - Campaign-scoped widget + export-button RED spec
- `tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php` - D-03 combined CSV export query-filter + map() column-order RED spec
- `database/factories/PollingPlaceFactory.php` - Missing factory scaffolded and filled in (was blocking any PollingPlace-based test fixture)

## Decisions Made
- Chose a concrete 15-column order for the D-03 export's `map()` output (see code comment in `ApoyosLideresCoordinadoresExportTest.php`) rather than leaving it undefined, so plan 04.1-04 has an unambiguous implementation target instead of inventing column order ad hoc
- Extended the `assertTableHeaderActionsExistInOrder(['export'])` assertion to `TopPollingPlacesTable` (not explicitly called out in the plan's action text for that file, but consistent with D-02's widget+export pattern applying to every ranking widget in this phase)

## Deviations from Plan

None - plan executed exactly as written. The one addition noted above (export-button assertion on `TopPollingPlacesTable`) is a direct, same-pattern extension of an assertion the plan already specifies for the sibling `TopCoordinatorsTable` test in the same task, not a scope change.

## Issues Encountered
None. All 8 test files (1 extended, 7 new) fail exactly as expected:
- The D-01 regression test fails because `TopLeadersTable` still has the bug (asserting `registered_voters_count === 5` against an actual value of `null`/8)
- All 7 new test files targeting not-yet-created widget/export classes fail with `ComponentNotFoundException` or `Class ... not found` errors
- All pre-existing `DashboardWidgetsTest.php` tests remain green (no regression from the edit)
- `vendor/bin/pint --dirty` reported zero style violations on the final state of all new/modified files (one auto-fix applied and re-verified during Task 3: `new ClassName()` → `new ClassName` per project Pint config, and unused `use` imports removed to match `VotersExportTest.php`'s existing convention)

## Next Phase Readiness
- Wave 1 plans (04.1-02, 04.1-03, 04.1-04) can now implement `TopCoordinatorsTable`, `TopPollingPlacesTable`, `RejectionsReportTable`, `DuplicatesReportTable`, `JurisdictionReportTable`, `ApoyosLideresCoordinadoresTable`, and `ApoyosLideresCoordinadoresExport` against these fixed RED specs, plus fix the `TopLeadersTable` D-01 bug
- No blockers. The D-03 export's column order is now an explicit contract Wave 1 must match exactly (see code comment in `tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php`)

---
*Phase: 04.1-reportes-avanzados-de-apoyos-ranking-lider-coordinador-puesto-votacion-informe-rechazos-informe-duplicados-export-csv-plano-informe-jurisdiccion-dentro-fuera*
*Completed: 2026-07-23*

## Self-Check: PASSED

All 9 created/modified files verified present on disk; all 3 task commits (`7065765`, `9a8b5ce`, `d2b391f`) verified present in git history.

---
phase: 05.1-cross-phase-hardening-closure
plan: 07
subsystem: ui
tags: [filament, livewire, dashboard, widgets, drill-through]

# Dependency graph
requires:
  - phase: 05.1-06
    provides: coordinator-ownership scoping already applied to TopLeadersTable.php
provides:
  - New FollowUpBacklogOverview stats widget (pending census validation + pending call assignments)
  - Drill-through URL from the backlog stat to VoterResource's filtered index
  - Drill-through recordUrl from TopLeadersTable rows to that leader's filtered voter list
affects: [reporting, dashboard, voters]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Filament StatsOverviewWidget Stat::make()->url() drill-through to a Resource's filtered index using tableFilters[filter][values]"
    - "Filament TableWidget ->recordUrl() drill-through wiring a ranking row to a filtered Resource index"

key-files:
  created:
    - app/Filament/Widgets/FollowUpBacklogOverview.php
    - tests/Feature/FollowUpBacklogOverviewTest.php
    - tests/Feature/WidgetDrillThroughTest.php
  modified:
    - app/Providers/Filament/AdminPanelProvider.php
    - app/Filament/Widgets/TopLeadersTable.php

key-decisions:
  - "FollowUpBacklogOverview registered admin-panel-only, right after CampaignStatsOverview, consistent with existing admin-only widget precedent"
  - "Used ReflectionMethod to invoke the protected getStats() method directly in tests, since Livewire's public API doesn't expose the underlying Stat objects for URL assertions"

patterns-established:
  - "Drill-through URLs use VoterResource::getUrl('index', ['tableFilters' => [...]]) with the 'values' key (not 'value') for any ->multiple() SelectFilter"

requirements-completed: [REPT-02, REPT-04]

# Metrics
duration: 25min
completed: 2026-07-23
---

# Phase 05.1 Plan 07: Follow-Up Backlog Widget & Dashboard Drill-Through Summary

**New FollowUpBacklogOverview stats widget surfaces pending-validation/pending-call backlog counts, and both that stat and the leader-ranking table now drill through via URL to VoterResource's filtered voter list.**

## Performance

- **Started:** 2026-07-23T20:34:00Z
- **Completed:** 2026-07-23T20:41:37Z
- **Duration:** ~25 min (including worktree sync: fast-forward merge to pick up main's 05.1-06 changes, plus a first-time `composer install` + `npm install && npm run build` for this parallel-executor worktree)
- **Tasks:** 2 completed
- **Files modified:** 5 (2 created source, 1 modified source, 2 test files)

## Accomplishments
- Admin dashboard now shows a real follow-up/validation backlog indicator (REPT-02) — previously nonexistent
- The backlog stat and the leader-ranking table both drill through to a filtered `VoterResource` list (REPT-04) — previously every aggregate widget was a dead end
- Confirmed the `values` (not `value`) URL state key for `->multiple()` SelectFilters by asserting on the generated URL string directly

## Task Commits

Each task was committed atomically:

1. **Task 1: FollowUpBacklogOverview widget (REPT-02)** - `6a12f5a` (feat)
2. **Task 2: Drill-through from backlog stat and leader ranking to filtered voter lists (REPT-04)** - `968f3ef` (feat)

_Both tasks were TDD (`tdd="true"`); tests were written and passing as part of the same commit as the corresponding implementation, following the plan's action blocks which specified the widget code and test coverage together._

## Files Created/Modified
- `app/Filament/Widgets/FollowUpBacklogOverview.php` - New StatsOverviewWidget: "Apoyos Pendientes de Validar" (with drill-through URL to VoterResource) and "Llamadas Pendientes" stats, campaign-scoped
- `app/Providers/Filament/AdminPanelProvider.php` - Registers `FollowUpBacklogOverview::class` right after `CampaignStatsOverview::class`
- `app/Filament/Widgets/TopLeadersTable.php` - Adds `->recordUrl()` so each leader row links to that leader's filtered voter list via `registered_by`
- `tests/Feature/FollowUpBacklogOverviewTest.php` - Covers both non-zero and zero-backlog scenarios
- `tests/Feature/WidgetDrillThroughTest.php` - Covers both drill-through URLs (backlog stat and leader-ranking recordUrl)

## Decisions Made
- Registered `FollowUpBacklogOverview` admin-panel-only (matching all other report/dashboard widgets in this codebase, not exposed to coordinator/leader panels)
- Used `ReflectionMethod` in tests to invoke the protected `getStats()` method directly for URL assertions, since Livewire's testing API doesn't otherwise expose a way to inspect an individual `Stat`'s resolved URL

## Deviations from Plan

None — plan executed exactly as written. The plan's provided code for `FollowUpBacklogOverview.php` and the `->recordUrl()` addition to `TopLeadersTable.php` were used verbatim (after confirming, per the plan's own instruction, that the post-05.1-06 state of `TopLeadersTable.php` had no existing `recordUrl`/row-click wiring to conflict with).

## Issues Encountered
- This worktree's branch was several commits behind `main` (missing all of Phase 05.1 Wave 1's merged work, including 05.1-06's coordinator-scoping change this plan depends on). Since the worktree's HEAD was a strict ancestor of `main` with no divergence, fast-forwarded the worktree branch to `main` (`git merge --ff-only main`) before starting, per the plan's explicit dependency note.
- This worktree had no `vendor/`, `node_modules/`, `.env`, or `APP_KEY` yet (first execution in this worktree). Ran `composer install`, copied `.env.example` to `.env`, ran `php artisan key:generate`, and ran `npm install && npm run build` (needed to generate the Vite manifest, otherwise ~27 unrelated tests referencing built assets fail) to get a genuinely working test environment. This mirrors the setup note already logged in STATE.md from Phase 05.1 Plan 04.
- Full `composer test` run: 870 passed, 1 failed (`Tests/Feature/Filament/UserResourceTest > can update user campaigns`) — this is the pre-existing intermittent flake already documented in STATE.md/deferred-items.md from Phase 04.1, unrelated to this plan's changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- REPT-02 and REPT-04 are closed; the admin dashboard now has a working backlog indicator and two aggregate→list drill-through paths
- No blockers for the remaining Wave 2 plans (05.1-02, 05.1-03, 05.1-09)

---
*Phase: 05.1-cross-phase-hardening-closure*
*Completed: 2026-07-23*

## Self-Check: PASSED

All created files and both task commits verified present.

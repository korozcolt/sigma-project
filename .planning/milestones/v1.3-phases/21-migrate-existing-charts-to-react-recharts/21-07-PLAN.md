---
phase: 21-migrate-existing-charts-to-react-recharts
plan: 07
type: execute
wave: 4
depends_on: ["21-03", "21-04", "21-05", "21-06"]
files_modified:
  - app/Filament/Widgets/ReactIslandPocWidget.php
  - tests/Browser/ReactIslandPocWidgetTest.php
  - resources/js/charts/components/ChartCard.jsx
  - app/Providers/Filament/AdminPanelProvider.php
  - app/Providers/Filament/ReportsPanelProvider.php
  - app/Providers/Filament/CoordinatorPanelProvider.php
  - app/Providers/Filament/AreaCoordinatorPanelProvider.php
  - app/Providers/Filament/LeaderPanelProvider.php
autonomous: false
requirements: [MIGR-01, MIGR-02]
must_haves:
  truths:
    - "The throwaway Phase 20 ReactIslandPocWidget and its transitional 'poc' ChartCard shim no longer exist anywhere in the codebase"
    - "All 6 migrated/new widgets from this phase (3 big charts + 3 sparklines) render correctly together on the same real panels with zero console errors, verified in an actual browser"
    - "The full Pest test suite (Feature + Browser) passes with zero regressions after the PoC removal"
  artifacts: []
  key_links:
    - from: "app/Providers/Filament/AdminPanelProvider.php"
      to: "app/Filament/Widgets/ReactIslandPocWidget.php"
      via: "ReactIslandPocWidget::class registration line removed"
      pattern: "ReactIslandPocWidget"
---

<objective>
Decommission Phase 20's throwaway `ReactIslandPocWidget` (deleting the class, its Browser test, and its registration on all 5 panels, plus the transitional `'poc'` compatibility shim in `ChartCard.jsx`) now that this phase's 6 real widgets have proven the same infra end-to-end — then run a full regression and a human browser checkpoint confirming the whole migrated dashboard looks and behaves correctly together.

Purpose: Closes out Phase 21 cleanly — no dangling throwaway PoC code left in the codebase once real widgets have taken over proving the pipeline, per `ReactIslandPocWidget`'s own docblock ("safe to delete once this phase's infra is verified and the first real widget migrates").
Output: `ReactIslandPocWidget.php` and its test deleted; `ChartCard.jsx`'s `'poc'` branch removed; all 5 `PanelProvider`s no longer reference it; full regression + human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-migrate-existing-charts-to-react-recharts/21-CONTEXT.md
@.planning/phases/20-react-island-infrastructure/20-CONTEXT.md
@resources/js/charts/components/ChartCard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ReactIslandPocWidget, its Browser test, and the transitional 'poc' shim in ChartCard.jsx</name>
  <files>app/Filament/Widgets/ReactIslandPocWidget.php, tests/Browser/ReactIslandPocWidgetTest.php, resources/js/charts/components/ChartCard.jsx</files>
  <read_first>
    - app/Filament/Widgets/ReactIslandPocWidget.php (confirm no other file imports/extends this class before deleting — repo-wide grep for `ReactIslandPocWidget` first)
    - resources/js/charts/components/ChartCard.jsx (the `if (kind === 'poc') { ... }` branch added in Plan 21-02 — this task removes exactly that branch)
  </read_first>
  <action>
    Run `grep -rln "ReactIslandPocWidget" app resources tests` first to confirm every remaining reference before deleting anything.

    Delete `app/Filament/Widgets/ReactIslandPocWidget.php`.
    Delete `tests/Browser/ReactIslandPocWidgetTest.php`.

    In `resources/js/charts/components/ChartCard.jsx`, remove the entire `if (kind === 'poc') { ... }` block added in Plan 21-02 (the block rendering `data-testid="react-chart-poc"`/`data-testid="react-chart-poc-value"`) — after removal, `ChartCard` only has the `hasError` branch and the generic empty/`ChartRouter` branch. Also remove the now-unused `'poc': SparklineChartKind` alias entry from `resources/js/charts/ChartRouter.jsx`'s `KIND_COMPONENTS` map (leave `line`/`bar`/`pie`/`sparkline` untouched).

    Then run `npm run build`.
  </action>
  <verify>
    <automated>cd "/Volumes/NAS(MAC)/Data/Herd/sigma-project" && test ! -f app/Filament/Widgets/ReactIslandPocWidget.php && test ! -f tests/Browser/ReactIslandPocWidgetTest.php && grep -c "kind === 'poc'" resources/js/charts/components/ChartCard.jsx; grep -c "poc:" resources/js/charts/ChartRouter.jsx; npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Both files no longer exist on disk; ChartCard.jsx has zero references to kind 'poc'; ChartRouter.jsx's KIND_COMPONENTS map has zero 'poc' entries; npm run build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ReactIslandPocWidget's registration from all 5 PanelProviders</name>
  <files>app/Providers/Filament/AdminPanelProvider.php, app/Providers/Filament/ReportsPanelProvider.php, app/Providers/Filament/CoordinatorPanelProvider.php, app/Providers/Filament/AreaCoordinatorPanelProvider.php, app/Providers/Filament/LeaderPanelProvider.php</files>
  <read_first>
    - app/Providers/Filament/AdminPanelProvider.php (ReactIslandPocWidget::class line inside ->widgets([...]), plus its use import — both to remove)
  </read_first>
  <action>
    In each of the 5 `*PanelProvider.php` files, remove:
    - the `use App\Filament\Widgets\ReactIslandPocWidget;` import line, and
    - the `ReactIslandPocWidget::class,     // Fase 20 PoC — infra React/Recharts/Motion (temporal, ver ReactIslandPocWidget)` line from inside `->widgets([...])`.

    Do not touch any other line in any of these 5 files (in particular, leave the `CampaignVotersSparklineWidget::class`/`SurveyResponsesSparklineWidget::class` lines Plan 21-05 added completely untouched).
  </action>
  <verify>
    <automated>cd "/Volumes/NAS(MAC)/Data/Herd/sigma-project" && grep -rc "ReactIslandPocWidget" app/Providers/Filament/*.php; vendor/bin/pint --dirty --test app/Providers/Filament/ 2>&1 | tail -20</automated>
  </verify>
  <done>Zero remaining references to ReactIslandPocWidget in any PanelProvider file (grep count 0 for each); Pint reports no style violations on the dirty files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human checkpoint: verify all 6 migrated/new chart widgets render correctly together after PoC removal</name>
  <files>none — verification only, no files modified by this task</files>
  <what-built>
    Phase 21 is code-complete: all 3 existing big `ChartWidget`s (`ValidationProgressChart`, `TerritorialDistributionChart`, `SurveyResultsWidget`) and all 3 new dedicated sparkline widgets (`CampaignVotersSparklineWidget`, `CallCenterCallsSparklineWidget`, `SurveyResponsesSparklineWidget`) now render through the React/Recharts pipeline with full MonoCharts visual composition (D-02) and the monochrome palette (D-03), and the throwaway Phase 20 PoC widget has been fully removed.
  </what-built>
  <action>
    Perform the manual browser verification steps listed in `<how-to-verify>` below and report the outcome via `<resume-signal>`. No code changes — this task is pure human verification of Tasks 1-2's automated work, gated here per the project's standing "browser-verify before prod" convention (established Phase 20 D-04).
  </action>
  <how-to-verify>
    1. Run the full automated suite first and confirm it is green: `php artisan test` (Feature + Browser + E2E) — paste the final pass/fail summary into the session before pausing for manual review.
    2. In a real Chrome browser, log in as a `super_admin` and visit `/admin` (the main dashboard). Confirm:
       - `ValidationProgressChart` renders a 2-line chart (accent "Validados" line, muted zinc "Total Apoyos" line) with a visible legend and tooltip on hover.
       - `TerritorialDistributionChart` renders a ranked bar chart, tallest bar in accent orange, remaining bars in descending zinc opacity — not the old 10-color rainbow.
       - `CampaignVotersSparklineWidget` renders next to the main stats row as a small trend line with a large current-value callout.
       - `SurveyStatsOverview`'s row shows `SurveyResponsesSparklineWidget` next to it, similarly styled.
       - No widget shows a broken/blank box or the literal error text "No se pudo cargar la gráfica." under normal conditions.
       - Open DevTools console — confirm zero JavaScript errors.
    3. Visit `/admin/surveys/{any-survey}/edit` — confirm each question shows its own results chart (pie for Sí/No questions, bar for others) in the page footer.
    4. Visit `/admin/verification-calls` — confirm `CallCenterStatsWidget`'s stats row and `CallCenterCallsSparklineWidget`'s trend line both render.
    5. Navigate away from `/admin` to any other page via the sidebar (Livewire SPA navigation) and back — confirm no duplicate-root console errors and charts still render correctly (React root cleanup still works, per Phase 20 INFRA-03).
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 checks pass, or describe any issue found (which chart, what's wrong).</resume-signal>
  <verify>Manual only — user must type "approved" (or describe issues) per resume-signal; no automated command applies to a human visual/console confirmation.</verify>
  <done>User has explicitly confirmed (via the resume-signal) that all 6 migrated/new widgets render correctly with real MonoCharts visual composition, zero console errors, and correct pie/bar/line/sparkline kinds on their respective real pages, and that React root cleanup on SPA navigation still works.</done>
</task>

</tasks>

<verification>
Full regression: `php artisan test` (all suites). `git grep -c "ReactIslandPocWidget"` across the whole repo (excluding `.planning/` and `.git/`) returns zero hits. `npm run build` exits 0.
</verification>

<success_criteria>
- `ReactIslandPocWidget.php`, its Browser test, and its `ChartCard.jsx`/`ChartRouter.jsx` transitional references are fully removed.
- All 5 `PanelProvider`s no longer reference the deleted widget.
- Full Pest suite (Feature + Browser + E2E) passes with zero regressions.
- Human browser checkpoint approved, confirming all 6 migrated/new widgets render correctly together with real MonoCharts visual composition and zero console errors.
</success_criteria>

<output>
After completion, create `.planning/phases/21-migrate-existing-charts-to-react-recharts/21-07-SUMMARY.md`
</output>

---
phase: 20-react-island-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified: []
autonomous: false
requirements: [INFRA-03]

must_haves:
  truths:
    - "A human has visually confirmed the PoC widget's chart renders and updates live across a real poll cycle in a real browser, on all 5 panels (Admin, Coordinator, AreaCoordinator, Leader, Reports)"
    - "A human has visually confirmed navigating away from a panel hosting the widget and back does not leak a React root or produce console warnings, repeated on all 5 panels — not just Admin"
    - "A human has visually confirmed the PoC widget's presence does not disrupt other pre-existing Livewire widgets' own rendering/DOM diffing on the same Admin dashboard"
    - "Phase 20 is not marked done on automated tests alone — per D-04, a human checkpoint gates phase completion"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 20 with the human browser checkpoint D-04 explicitly requires. 20-01 and 20-02 already prove the bridge automatically (Pest 4 Browser test in 20-02, Admin panel only); this plan is the mandatory manual confirmation step before the phase is considered done — matching the user's established preference of browser-verifying UI changes before treating them as shipped, and directly satisfying the "verified on all 5 panels" language in INFRA-03/ROADMAP.md's Phase 20 success criterion 3 via a full manual check (render + poll update + navigate-away-and-back unmount check) repeated on every one of the 5 panels, since the automated test only exercises Admin.

Purpose: D-04 is explicit — "do not close out Phase 20 on automated tests alone."
Output: User confirmation that the poll cycle, cross-panel unmount-on-navigation, non-disruption of other widgets, and (if deliberately triggered) the D-03 error state all behave correctly in a real browser, on all 5 panels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-react-island-infrastructure/20-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human checkpoint: verify the React island PoC across all 5 panels, poll cycle, and navigation</name>
  <files>none — verification only, no files modified by this task</files>
  <what-built>
    A throwaway `ReactIslandPocWidget` (Filament `ChartWidget` subclass) rendering a small Recharts bar + Motion fade-in card via a React island bridged through Alpine/`wire:ignore`. It is registered on all 5 panels (Admin, Reports, Coordinator, AreaCoordinator, Leader) and polls every 10 seconds. An automated Pest 4 Browser test (20-02) already proves the poll-refresh mechanism works headlessly on the Admin panel — this checkpoint is the real-browser, human confirmation D-04 requires before the phase is considered done, and is the only place INFRA-03's "verified on all 5 panels" unmount-on-navigation requirement is actually checked.
  </what-built>
  <action>
    Perform the manual browser verification steps listed in `<how-to-verify>` below and report the outcome via `<resume-signal>`. No code changes — this task is pure human verification, automated as far as possible in 20-01/20-02, gated here per D-04.
  </action>
  <how-to-verify>
    1. Run `npm run build` if not already built for this environment, then log in to the Admin panel (`/admin`) as a user with `super_admin` or `admin_campaign` role.
    2. Locate the "React Island PoC" card on the dashboard (bottom of the widget list). Confirm it shows a small bar chart with a numeric value — not blank, not an error message. Also confirm the existing dashboard widgets above it (e.g. the campaign stats KPI cards, Validation Progress chart, Territorial Distribution map) are still rendering their real content normally, side by side with the new PoC card — this confirms the React island's mount does not disrupt other pre-existing Livewire widgets' own DOM diffing/polling on the same page.
    3. Leave the tab open and watch the card for at least 15-20 seconds (the widget polls every 10s). Confirm the displayed value changes on its own, with a smooth fade rather than a jarring flash/reset, and without the page reloading.
    4. Open the browser DevTools console. Confirm there is no warning containing "already been passed to createRoot()" (would indicate a duplicate root / leaked mount).
    5. Click to another page within the Admin panel (e.g. a resource list) and back to the Dashboard. Confirm the chart card reappears correctly with no console errors and no duplicate-root warning.
    6. Repeat the FULL check on ALL 4 remaining panels — Coordinator (`/coordinator`), AreaCoordinator (`/articulador`), Leader (`/leader`), and Reports (`/reports`) — logging in as a user with the corresponding role for each panel. On every one of these panels: (a) confirm the card renders real content and updates after a poll tick, same as steps 2-3, AND (b) repeat the exact navigate-away-and-back + DevTools console/duplicate-root check from steps 4-5. Do not skip the navigation/unmount check on any panel — INFRA-03 requires unmount-on-navigation to be verified on all 5 panels, not just Admin, and not just a render spot-check on the other 4.
    7. Optional (D-03 spot-check): temporarily rename `resources/js/charts/main.jsx` to simulate a missing bundle, reload a panel with the widget, and confirm the "No se pudo cargar la gráfica." fallback message appears within ~5 seconds instead of an indefinite blank/spinner — then rename the file back and rebuild.
  </how-to-verify>
  <resume-signal>Type "approved" once, for ALL 5 panels, the poll-cycle update and navigate-away-and-back check (no leaked root, no console errors) have been observed, plus confirmation that other Admin dashboard widgets are unaffected — or describe what's wrong (which panel, which step) so it can be fixed before Phase 20 is marked complete.</resume-signal>
  <verify>Manual only — user must type "approved" (or describe issues) per resume-signal; no automated command applies to a human visual/console confirmation.</verify>
  <done>User has explicitly confirmed (via the resume-signal) that the poll-cycle update and navigate-away-and-back check (no leaked root, clean console) were directly observed on all 5 panels — Admin, Coordinator, AreaCoordinator, Leader, and Reports — and that other pre-existing Admin dashboard widgets continue to render/update normally alongside the PoC widget.</done>
</task>

</tasks>

<verification>
This plan has no automated verification of its own — 20-02's Pest 4 Browser test already covers the automated proof (Admin panel only). This plan exists solely to satisfy D-04's explicit requirement for a human-in-the-browser confirmation gating phase completion, and is the only place INFRA-03's all-5-panels unmount-on-navigation requirement is actually exercised.
</verification>

<success_criteria>
- User has typed "approved" (or equivalent affirmative) confirming: poll-cycle live update works, no leaked React root across panel navigation, and no console errors — observed directly in a real browser, on all 5 panels (Admin, Coordinator, AreaCoordinator, Leader, Reports).
- User has confirmed other pre-existing Admin dashboard widgets continue to render/update normally with the PoC widget present.
</success_criteria>

<output>
After completion, create `.planning/phases/20-react-island-infrastructure/20-03-SUMMARY.md`
</output>

---
phase: 10-operator-provenance-fallback-controls
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified: []
autonomous: false
requirements: [SRC-01, SRC-04, SRC-05]

must_haves:
  truths:
    - "A human has visually confirmed the source badge, resolved-at timestamp, table filter, form display, role-gated action, and dashboard widget all render correctly together in the running application"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of the three automated plans (10-01, 10-02, 10-03) working together in the actual running Filament admin panel — the badge/timestamp across all three surfaces (table, view, edit form), the new table filter, the role-gated force-refresh action, and the new dashboard widget. All underlying logic and behavior is already proven by Pest tests in the prior plans; this plan exists solely to catch anything a screenshot-blind test suite cannot (visual layout, color contrast, real click-through feel).

Purpose: Close the loop on the phase's "UI hint: yes" flag from ROADMAP.md with an actual human look at the rendered UI before considering Phase 10 done.
Output: A confirmed-working (or issue-flagged) operator experience across all of SRC-01, SRC-04, and SRC-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-operator-provenance-fallback-controls/10-CONTEXT.md
@.planning/phases/10-operator-provenance-fallback-controls/10-01-SUMMARY.md
@.planning/phases/10-operator-provenance-fallback-controls/10-02-SUMMARY.md
@.planning/phases/10-operator-provenance-fallback-controls/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visually confirm provenance badges, filter, role gate, and widget in the running app</name>
  <files>None — this task modifies no files, it verifies Plans 10-01/10-02/10-03's already-committed changes in the running application.</files>
  <what-built>
    - Voters table: a colored `polling_place_source` badge column and a `polling_place_resolved_at` date column (Plan 10-01).
    - Voters table: a new "Fuente del Puesto de Votación" multi-select filter (Plan 10-01).
    - Voter view page: the same badge + timestamp in the infolist (Plan 10-01).
    - Voter edit form: a read-only source badge display near the polling-place fields (Plan 10-02).
    - Voter edit form: the "Actualizar datos desde Registraduría" button now only appears for admin_campaign/coordinator/super_admin users, on a voter that already has a polling place resolved (Plan 10-02).
    - Dashboard: a new "Procedencia del Puesto de Votación" widget showing the campaign's fallback-sourced voter count, linking to the filtered table (Plan 10-03).
  </what-built>
  <action>
    No implementation action — this is a verification-only checkpoint. Follow `<how-to-verify>` below and report the outcome.
  </action>
  <how-to-verify>
    1. As a super_admin, open the Voters list (`/admin/voters`). Confirm a colored badge appears in a "Fuente del Puesto de Votación" column (or toggle it visible via the column-visibility control if hidden by default) for voters that have a `polling_place_source` set, and that voters without one show an empty/placeholder state.
    2. Open the table filters and confirm "Fuente del Puesto de Votación" appears as a multi-select filter; select one or more sources and confirm the list narrows correctly.
    3. Open any voter's view page (`/admin/voters/{id}`) and confirm the same badge + "Actualizado el" timestamp render in the infolist.
    4. Open that same voter's edit page and confirm the read-only source badge appears near the polling-place fields (Ubicación section).
    5. On a voter with a polling place already set, confirm the "Actualizar datos desde Registraduría" (gray, refresh-icon) suffix button next to "Número de Documento" is visible while acting as super_admin.
    6. Log in as (or temporarily assign) a leader- or reviewer-only user and open the same voter's edit page — confirm the "Actualizar datos desde Registraduría" button is now HIDDEN, while the primary "Consultar Registraduría" (magnifying-glass) button remains visible.
    7. Return to the dashboard (`/admin`) and confirm the new "Procedencia del Puesto de Votación" widget shows a fallback-source count for the active campaign, and clicking its stat navigates to the voters table pre-filtered to the three fallback sources.
  </how-to-verify>
  <verify>
    <automated>MISSING — this task is human-verification-only by design (visual/UX confirmation across three already-tested plans); no automated command applies. All underlying behavior is already covered by the automated `<verify>` commands in Plans 10-01, 10-02, and 10-03.</automated>
  </verify>
  <done>Human has confirmed all seven checks in `<how-to-verify>` render and behave correctly, or has reported specific issues to address as gap-closure follow-up.</done>
  <resume-signal>Type "approved" if all seven checks look correct, or describe any visual/behavioral issue found.</resume-signal>
</task>

</tasks>

<verification>
This plan has no automated verification of its own — it is a human confirmation layer on top of the fully-tested behavior delivered by Plans 10-01, 10-02, and 10-03. If issues are found, they should be captured as gap-closure follow-up work rather than blocking those plans' already-passing test suites.
</verification>

<success_criteria>
A human operator has confirmed, by direct interaction with the running application, that SRC-01 (visible source everywhere), SRC-04 (manual re-check preserved for every role via the original action), and SRC-05 (filter + widget triage) all work together as intended.
</success_criteria>

<output>
After completion, create `.planning/phases/10-operator-provenance-fallback-controls/10-04-SUMMARY.md`
</output>

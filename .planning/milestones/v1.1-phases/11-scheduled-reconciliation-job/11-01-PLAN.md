---
phase: 11-scheduled-reconciliation-job
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/RegistraduriaService.php
  - config/services.php
  - .env
  - tests/Feature/Services/RegistraduriaServiceReachabilityTest.php
  - tests/fixtures/registraduria/consulta-sample.html
autonomous: true
requirements: [RECON-01]

must_haves:
  truths:
    - "isReachable() returns true against the real, live wsp.registraduria.gov.co endpoint when it is up (it currently always returns false because of a dead probe URL + wrong HTTP verb)"
    - "A full, untruncated real wsp success HTML response is captured to a committed fixture file, ready for Plan 11-02's parser task"
  artifacts:
    - path: "app/Services/RegistraduriaService.php"
      provides: "GET-based isReachable() against the corrected wsp probe URL"
    - path: "config/services.php"
      provides: "Corrected registraduria.probe_url fallback default"
    - path: "tests/fixtures/registraduria/consulta-sample.html"
      provides: "Real, untruncated captured wsp #consulta success HTML (spends exactly one 2captcha lookup)"
  key_links:
    - from: "app/Services/RegistraduriaService.php::isReachable()"
      to: "wsp.registraduria.gov.co"
      via: "Http::get() (not ->head())"
      pattern: "->get\\(config\\('services.registraduria.probe_url'\\)\\)"
---

<objective>
Fix the reachability gap (D-01) that currently makes `RegistraduriaService::isReachable()` always return `false`, and capture one real, untruncated wsp success HTML response (D-02 prerequisite) so Plan 11-02 can write its HTML parser against real data instead of a guess.

Purpose: Without this plan, `PollingPlaceResolver::attemptLiveAutomated()` always believes the live source is down and Phase 11's entire reconciliation job (RECON-01) would silently no-op forever, falling through to snapshot on every attempt.
Output: A working `isReachable()` against the real `wsp.registraduria.gov.co` endpoint, plus a real captured HTML fixture for the next plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-scheduled-reconciliation-job/11-CONTEXT.md
@.planning/phases/11-scheduled-reconciliation-job/11-RESEARCH.md
@app/Services/RegistraduriaService.php
@config/services.php
@tests/Feature/Services/RegistraduriaServiceReachabilityTest.php
</context>

<interfaces>
<!-- Existing RegistraduriaService.php contract this plan modifies (isReachable() only — getResult()/startLookup() untouched by this plan) -->

```php
// app/Services/RegistraduriaService.php (current, to be modified)
public function isReachable(): bool
{
    if (! config('services.registraduria.live_enabled')) {
        return false;
    }

    try {
        $response = Http::connectTimeout(2)->timeout(3)->withoutRedirecting()->head(config('services.registraduria.probe_url'));

        return $response->successful() || $response->redirect();
    } catch (ConnectionException) {
        return false;
    }
}
```

```php
// config/services.php (current, to be modified)
'registraduria' => [
    'url' => env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757'),
    'live_enabled' => env('REGISTRADURIA_LIVE_ENABLED', true),
    'probe_url' => env('REGISTRADURIA_PROBE_URL', 'https://apiweb-eleccionescolombia.infovotantes.com'),
],
```

Verified this session (2026-07-26 research pass, reproduced): `HEAD https://wsp.registraduria.gov.co/censo/consultar/` -> HTTP 500 every time; `GET` to the same URL -> HTTP 200 every time with the real form page.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix isReachable() to GET the corrected wsp probe URL (D-01)</name>
  <files>app/Services/RegistraduriaService.php, config/services.php, .env, tests/Feature/Services/RegistraduriaServiceReachabilityTest.php</files>
  <read_first>
    - app/Services/RegistraduriaService.php (isReachable(), lines ~80-93)
    - config/services.php (registraduria block, lines ~45-49)
    - tests/Feature/Services/RegistraduriaServiceReachabilityTest.php (existing Http::fake-based tests — note none of them assert HTTP verb, which is why this bug was never caught)
  </read_first>
  <behavior>
    - New test: "isReachable() sends a GET request (not HEAD) to the configured probe URL" — asserts via `Http::assertSent(fn ($request) => $request->method() === 'GET')`.
    - Existing 4 tests (kill-switch off, probe 200, probe redirect, probe connection failure) must still pass unchanged.
  </behavior>
  <action>
    In `app/Services/RegistraduriaService.php`'s `isReachable()`, change `->head(config('services.registraduria.probe_url'))` to `->get(config('services.registraduria.probe_url'))`. Keep `connectTimeout(2)->timeout(3)->withoutRedirecting()` unchanged — verified real response times are ~0.15-0.20s, well within these limits.

    In `config/services.php`, change the `probe_url` fallback default from `'https://apiweb-eleccionescolombia.infovotantes.com'` to `'https://wsp.registraduria.gov.co/censo/consultar/'`.

    In `.env`, change `REGISTRADURIA_PROBE_URL=https://apiweb-eleccionescolombia.infovotantes.com` to `REGISTRADURIA_PROBE_URL=https://wsp.registraduria.gov.co/censo/consultar/` (this file is gitignored — edit the live local value so the capture task in Task 2 and any manual verification use the corrected URL).

    In `tests/Feature/Services/RegistraduriaServiceReachabilityTest.php`, add a new test asserting the request method is GET (see behavior above), placed after the existing "returns true when the probe responds successfully" test. Do not remove or rewrite the 4 existing tests.
  </action>
  <acceptance_criteria>
    - `grep -n "->get(config('services.registraduria.probe_url'))" app/Services/RegistraduriaService.php` returns exactly one match
    - `grep -n "->head(" app/Services/RegistraduriaService.php` returns zero matches
    - `grep -n "wsp.registraduria.gov.co/censo/consultar" config/services.php` returns one match
    - `grep -n "wsp.registraduria.gov.co/censo/consultar" .env` returns one match
    - `php artisan test --filter=RegistraduriaServiceReachabilityTest` passes (5 tests: 4 existing + 1 new)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=RegistraduriaServiceReachabilityTest</automated>
  </verify>
  <done>isReachable() issues a GET (not HEAD) against the corrected wsp.registraduria.gov.co probe URL; the reachability test suite (5 tests) passes, including a new test that locks in the GET-vs-HEAD behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Capture one real, untruncated wsp success HTML response (D-02 prerequisite)</name>
  <files>tests/fixtures/registraduria/consulta-sample.html</files>
  <read_first>
    - registraduria-service/app.py (the /lookup and /result/&lt;session_id&gt; routes, and the `success` outcome branch that sets `data={"raw_message_html": pp_html}`)
    - .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md (known-good real cédulas: 1102812122, 1102815878, 64552231 — all confirmed `success` in Phase 9's 30-attempt spike)
  </read_first>
  <action>
    This task spends exactly ONE real 2captcha-budgeted lookup, per 11-CONTEXT.md D-02 and 11-RESEARCH.md's explicit recommendation — do not skip or simulate it, and do not write more than one real lookup unless the first attempt's outcome is not `success`.

    1. Confirm the Python service is reachable: `curl -s -o /dev/null -w "%{http_code}" http://localhost:5757/result/nonexistent` (expect `404`, confirming the Flask app is up). If it does not respond, start it: `cd registraduria-service && source venv/bin/activate && nohup python app.py > service.log 2>&1 &` then wait ~2 seconds and retry the health check.
    2. Start a lookup: `curl -s -X POST http://localhost:5757/lookup -H "Content-Type: application/json" -d '{"cedula":"1102812122"}'` — capture the returned `session_id`.
    3. Poll `curl -s http://localhost:5757/result/{session_id}` every 5 seconds (up to ~150 seconds / 30 polls) until the JSON's `status` field is `"done"`.
    4. If the resulting `outcome` is NOT `"success"` (e.g. `denied_by_score`, `not_found`, `session_expired`), retry the same steps with the next known-good cédula (`1102815878`, then `64552231` if needed) — up to 3 total real attempts. Phase 9's spike showed a 96.7% success rate, so a retry should rarely be needed.
    5. Once a `success` result is obtained, extract the FULL, untruncated string at `.data.raw_message_html` from the JSON response (use `python3 -c "import json,sys; print(json.load(sys.stdin)['data']['raw_message_html'])"` piped from the curl output, or an equivalent full-fidelity extraction — do NOT manually copy/paste from truncated terminal output).
    6. Write that exact, complete HTML string to a new file at `tests/fixtures/registraduria/consulta-sample.html` (create the `tests/fixtures/registraduria/` directory if it does not exist).
    7. Log a one-line confirmation to stdout: the byte length of the captured file and confirmation it contains a closing `</table>` tag (proving it is not truncated like Phase 9's ~130-200 char samples).
  </action>
  <acceptance_criteria>
    - `test -s tests/fixtures/registraduria/consulta-sample.html` (file exists and is non-empty)
    - `grep -c "id='consulta'" tests/fixtures/registraduria/consulta-sample.html` returns >= 1
    - `grep -c "</table>" tests/fixtures/registraduria/consulta-sample.html` returns >= 1 (proves the capture is NOT truncated like Phase 9's ~130-200 char samples, which cut off mid-`<th>`)
    - `wc -c tests/fixtures/registraduria/consulta-sample.html` reports a byte count meaningfully larger than 200 (Phase 9's truncated samples topped out around 200 bytes)
  </acceptance_criteria>
  <verify>
    <automated>test -s tests/fixtures/registraduria/consulta-sample.html && grep -q "</table>" tests/fixtures/registraduria/consulta-sample.html && echo "CAPTURE_OK"</automated>
  </verify>
  <done>tests/fixtures/registraduria/consulta-sample.html contains one real, complete, untruncated wsp `#consulta` success HTML response, ready for Plan 11-02's parser task to inspect and target.</done>
</task>

</tasks>

<verification>
- `php artisan test --filter=RegistraduriaServiceReachabilityTest` passes (5 tests).
- `tests/fixtures/registraduria/consulta-sample.html` exists, is non-truncated (contains a closing `</table>`), and was captured from a real live lookup (not fabricated).
</verification>

<success_criteria>
- isReachable() correctly reports the real wsp.registraduria.gov.co endpoint's up/down state via GET, not HEAD.
- A real, complete HTML sample is committed for Plan 11-02 to parse against — no guessed schema.
</success_criteria>

<output>
After completion, create `.planning/phases/11-scheduled-reconciliation-job/11-01-SUMMARY.md`
</output>

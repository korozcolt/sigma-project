# Quick Task 260723-f26: Botón secundario "Actualizar datos Registraduría" + pruebas E2E - Context

**Gathered:** 2026-07-23
**Status:** Ready for planning — session was paused before planner ran, all findings below are solid ground to resume from directly (no need to re-investigate).

<domain>
## Task Boundary

Agregar un botón secundario/discreto de "Actualizar datos desde Registraduría" en el formulario de Voter (Apoyo), y validar todo el flujo de consulta a Registraduría end-to-end con 2 cédulas reales de prueba (aún pendientes de que el usuario las dé).

</domain>

<decisions>
## Implementation Decisions

### Botón secundario de actualización
- Debe ser visualmente discreto (ícono pequeño gris o link de texto pequeño) — NO debe competir con el botón principal "Consultar Registraduría" (`suffixAction` `consultar_registraduria` en `app/Filament/Resources/Voters/Schemas/VoterForm.php:201-209`).
- Solo tiene sentido cuando ya hay un `polling_place_id` resuelto — es una acción de "actualizar lo que ya existe", no la acción de primera consulta.
- Debe llamar directo a la Capa 3 (2captcha), bypasseando Redis y la reconstrucción desde BD.
- Al completarse: mismo guardado que ya existe en `handleRegistraduriaResult()` (sobrescribe caché Redis + upsert de `CensusRecord`), solo que sin el chequeo previo de caché/BD.

### Alcance
- NO tocar el redeploy de producción — el usuario dijo explícitamente que eso se hará eventualmente, no es urgente ahora. Esto es solo código + pruebas locales.

### Claude's Discretion
- Icono/label exacto del botón secundario (ej. `heroicon-o-arrow-path` con label vacío o tooltip "Actualizar datos" — mantenerlo visualmente menor que el botón principal).
- Nombre exacto del nuevo método en `HasRegistraduriaPolling` (ej. `forceRefreshFromRegistraduria`).

</decisions>

<specifics>
## Specific Ideas

Ninguna referencia visual específica dada por el usuario más allá de "poco visible, pero se debe ver, de manera intuitiva, secundario".

</specifics>

<canonical_refs>
## Canonical References

### Lógica de caché ya existente (NO reinventar, ya cumple la regla "1 consulta por Registraduría, luego desde BD")
- `app/Filament/Resources/Voters/Concerns/HasRegistraduriaPolling.php` — trait completo:
  - `openRegistraduriaBrowser(string $cedula)` (líneas 37-89): Capa 1 Redis (30 días TTL) → Capa 2 reconstrucción desde `CensusRecord`+`PollingPlace` (líneas 97-140) → Capa 3 2captcha vía `RegistraduriaService::startLookup()` (solo si las dos anteriores fallan).
  - `handleRegistraduriaResult(array $data)` (líneas 148-187): listener del evento Livewire `registraduria-result` disparado desde Alpine.js. Guarda en Redis (`Cache::put`, líneas 172-180) y llama a `fillPollingPlaceFields()`.
  - `fillPollingPlaceFields(array $data)` (líneas 194-273): resuelve municipality/department/polling_place, llena el form data bag, y hace `CensusRecord::updateOrCreate()` (líneas 259-271) para acumular el censo real.
- `app/Filament/Resources/Voters/Schemas/VoterForm.php` líneas 195-224: campo `document_number` con el `suffixAction` actual (`consultar_registraduria`, ícono `heroicon-o-magnifying-glass`, línea 201-209). El nuevo botón secundario va junto a este, mismo campo.
- `app/Services/RegistraduriaService.php` — `startLookup($cedula)`, llama al microservicio Flask.

### Seguridad — YA RESUELTO en esta sesión (no repetir)
- API key vieja de 2captcha (ya rotada e inválida — ver commit `ac1dd5a` para el fix, no repetir el valor aquí) estaba hardcodeada en `registraduria-service/app.py` y expuesta en el historial de git de un repo PÚBLICO (`korozcolt/sigma-project`, confirmado público vía `gh repo view`).
- Usuario ya rotó la key en 2captcha y la configuró como variable de entorno `TWO_CAPTCHA_KEY` en Dokploy para el contenedor `sigma-registraduria` en producción (servidor `korserver`, gestionado con Dokploy).
- Código corregido y pusheado: commit `ac1dd5a` — `registraduria-service/app.py` ahora usa `TWO_CAPTCHA_KEY = os.environ["TWO_CAPTCHA_KEY"]` (falla rápido si no está seteada, sin fallback hardcodeado). Ya está en `origin/main`.
- Pendiente (no urgente, según el usuario): redeploy del contenedor en producción para que tome el código nuevo + la env var nueva — el contenedor sigue corriendo la versión vieja hardcodeada hasta que se haga "Deploy" en Dokploy. El proyecto aún no está en uso real, así que no hay apuro.
- La NUEVA key (`TWO_CAPTCHA_KEY`) fue mostrada por el usuario en una captura de pantalla de Dokploy durante esta sesión — **ya fue guardada localmente en `registraduria-service/.env`** (gitignored, ver Session Handoff abajo) para poder correr el microservicio Flask localmente y hacer las pruebas E2E sin volver a pedirla.

### Arquitectura del microservicio (para contexto, no cambiar)
- `registraduria-service/app.py` — Flask, usa 2captcha + Playwright headless para resolver el reCAPTCHA de `eleccionescolombia.registraduria.gov.co` y llamar a `apiweb-eleccionescolombia.infovotantes.com/api/v1/citizen/get-information`.
- Corre en el servidor de producción como contenedor Docker `sigma-registraduria` (Ubuntu + Dokploy, host `korserver` vía SSH). Localmente se corre con `python app.py` o similar (revisar README del microservicio para el comando exacto de arranque si existe).

</canonical_refs>

<pending_from_user>
## Pendiente del usuario (bloqueante para las pruebas E2E)

- **2 cédulas reales habilitadas en Registraduría** para pruebas — el usuario ofreció darlas pero aún no las compartió en el chat. Sin esto no se puede validar el flujo completo (solo consulta real, caché, y el botón de actualización forzada).
- Confirmar si el usuario quiere que se corra el microservicio Flask localmente (`registraduria-service/`) apuntando a la Registraduría real, o si prefiere apuntar las pruebas contra el servicio ya desplegado en producción (`sigma-registraduria` en `korserver`) — este último sigue con la key vieja hasta que se redeploye, así que fallaría hasta ese momento.

</pending_from_user>

---

*Quick task: 260723-f26*
*Context gathered: 2026-07-23*

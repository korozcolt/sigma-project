---
phase: quick/260804-um4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Jobs/DispatchCensusRevalidation.php
  - app/Jobs/ReconcileFallbackPollingPlaces.php
  - app/Services/PollingPlaceResolver.php
autonomous: true
requirements: [FIX-CRITICO-PHP82-TYPED-CONSTANTS]

must_haves:
  truths:
    - "DispatchCensusRevalidation.php parses successfully under PHP 8.2 (no typed class constant syntax)"
    - "ReconcileFallbackPollingPlaces.php parses successfully under PHP 8.2 (no typed class constant syntax)"
    - "PollingPlaceResolver.php parses successfully under PHP 8.2 (no typed class constant syntax)"
    - "The 'Revalidar apoyos de un líder' button works in production because the job class autoloads without a ParseError"
  artifacts:
    - path: "app/Jobs/DispatchCensusRevalidation.php"
      provides: "MAX_VOTERS_PER_RUN and MAX_ATTEMPTS_BEFORE_EXHAUSTION declared as untyped private const"
    - path: "app/Jobs/ReconcileFallbackPollingPlaces.php"
      provides: "MAX_VOTERS_PER_RUN and MAX_ATTEMPTS_BEFORE_EXHAUSTION declared as untyped private const"
    - path: "app/Services/PollingPlaceResolver.php"
      provides: "LIVE_POLL_ATTEMPTS and LIVE_POLL_INTERVAL_MS declared as untyped private const"
  key_links:
    - from: "app/Jobs/DispatchCensusRevalidation.php"
      to: "self::MAX_VOTERS_PER_RUN / self::MAX_ATTEMPTS_BEFORE_EXHAUSTION usages"
      via: "unchanged self:: reference (PHP does not require repeating the type at use sites)"
      pattern: "self::MAX_VOTERS_PER_RUN"
    - from: "app/Services/PollingPlaceResolver.php"
      to: "self::LIVE_POLL_ATTEMPTS / self::LIVE_POLL_INTERVAL_MS usages"
      via: "unchanged self:: reference"
      pattern: "self::LIVE_POLL_ATTEMPTS"
---

<objective>
Remove PHP 8.3-only typed class constant syntax (`private const int NAME = value;`) from 3 production files, replacing it with untyped `private const NAME = value;` so the code parses correctly under production's actual PHP 8.2.27 runtime (matching `composer.json`'s declared `"php": "^8.2"`).

Purpose: This is a live production incident fix. The typed-constant syntax (PHP 8.3 RFC) currently causes a `ParseError` at autoload time in production, which broke the "Revalidar apoyos de un líder" button — `DispatchCensusRevalidation` can't even be loaded, so clicking the button silently does nothing.

Output: 3 files with 6 total occurrences fixed (type removed, value and visibility untouched), verified via `php -l` and the existing Pest suite for these files — zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/Jobs/DispatchCensusRevalidation.php
@app/Jobs/ReconcileFallbackPollingPlaces.php
@app/Services/PollingPlaceResolver.php

This is a pure syntax-compatibility fix, already diagnosed and confirmed on the production server (`php -l` inside the real container produced the exact ParseError). No new investigation needed — just apply the 6 known edits below.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove PHP 8.3 typed-constant syntax from all 3 files (6 occurrences)</name>
  <files>
    app/Jobs/DispatchCensusRevalidation.php
    app/Jobs/ReconcileFallbackPollingPlaces.php
    app/Services/PollingPlaceResolver.php
  </files>
  <action>
    Edit each of the 6 exact lines below to remove ONLY the `int` type from the class constant declaration. Keep the visibility modifier (`private`), the constant name, and the value exactly as-is. Do not touch any other line — no other code in these files references the type at the point of use, since PHP does not require (or allow) repeating the type when referencing a constant via `self::NAME`.

    1. `app/Jobs/DispatchCensusRevalidation.php:19`
       `private const int MAX_VOTERS_PER_RUN = 50;` -> `private const MAX_VOTERS_PER_RUN = 50;`
    2. `app/Jobs/DispatchCensusRevalidation.php:21`
       `private const int MAX_ATTEMPTS_BEFORE_EXHAUSTION = 5;` -> `private const MAX_ATTEMPTS_BEFORE_EXHAUSTION = 5;`
    3. `app/Jobs/ReconcileFallbackPollingPlaces.php:18`
       `private const int MAX_VOTERS_PER_RUN = 50;` -> `private const MAX_VOTERS_PER_RUN = 50;`
    4. `app/Jobs/ReconcileFallbackPollingPlaces.php:20`
       `private const int MAX_ATTEMPTS_BEFORE_EXHAUSTION = 5;` -> `private const MAX_ATTEMPTS_BEFORE_EXHAUSTION = 5;`
    5. `app/Services/PollingPlaceResolver.php:19`
       `private const int LIVE_POLL_ATTEMPTS = 9;` -> `private const LIVE_POLL_ATTEMPTS = 9;`
    6. `app/Services/PollingPlaceResolver.php:21`
       `private const int LIVE_POLL_INTERVAL_MS = 5000;` -> `private const LIVE_POLL_INTERVAL_MS = 5000;`

    After editing, run `vendor/bin/pint --dirty` to confirm formatting stays clean (no other changes expected — this is a whitespace-neutral edit).
  </action>
  <verify>
    <automated>php -l app/Jobs/DispatchCensusRevalidation.php && php -l app/Jobs/ReconcileFallbackPollingPlaces.php && php -l app/Services/PollingPlaceResolver.php</automated>
  </verify>
  <done>All 3 files report "No syntax errors detected"; `grep -n "const int" app/Jobs/DispatchCensusRevalidation.php app/Jobs/ReconcileFallbackPollingPlaces.php app/Services/PollingPlaceResolver.php` returns no matches; `vendor/bin/pint --dirty` reports no style violations.</done>
</task>

<task type="auto">
  <name>Task 2: Run existing Pest coverage for all 4 affected test files to confirm zero behavior change</name>
  <files>
    tests/Feature/Jobs/DispatchCensusRevalidationTest.php
    tests/Feature/Jobs/ReconcileFallbackPollingPlacesTest.php
    tests/Feature/Services/PollingPlaceResolverTest.php
    tests/Feature/Services/PollingPlaceResolverPriorityTest.php
  </files>
  <action>
    Locate the exact paths of the 4 test files (`DispatchCensusRevalidationTest`, `ReconcileFallbackPollingPlacesTest`, `PollingPlaceResolverTest`, `PollingPlaceResolverPriorityTest` — use `find`/`grep` if paths differ from the guessed locations above, since prior SUMMARYs reference these classes but exact directories weren't confirmed in this plan). Run all 4 together with `php artisan test --filter` or by passing the file paths directly to `php artisan test`. All must pass unmodified — no test file should need any edit, since this fix does not change runtime behavior, only constant-declaration syntax.
  </action>
  <verify>
    <automated>php artisan test --filter="DispatchCensusRevalidationTest|ReconcileFallbackPollingPlacesTest|PollingPlaceResolverTest|PollingPlaceResolverPriorityTest"</automated>
  </verify>
  <done>All tests across the 4 affected test files pass with no failures and no modifications to any test file.</done>
</task>

</tasks>

<verification>
1. `php -l` on all 3 modified files reports "No syntax errors detected".
2. `grep -rn "const int"` across `app/Jobs/DispatchCensusRevalidation.php`, `app/Jobs/ReconcileFallbackPollingPlaces.php`, `app/Services/PollingPlaceResolver.php` returns zero matches — confirms all 6 occurrences were fixed.
3. Full Pest suite for the 4 affected test files (`DispatchCensusRevalidationTest`, `ReconcileFallbackPollingPlacesTest`, `PollingPlaceResolverTest`, `PollingPlaceResolverPriorityTest`) passes unchanged.
4. `vendor/bin/pint --dirty` passes clean.
5. `git diff` on the 3 files shows ONLY the removal of ` int` from each of the 6 `private const` declarations — no other line changed.
</verification>

<success_criteria>
- All 6 typed class constant declarations across the 3 files are now untyped (`private const NAME = value;`), matching PHP 8.2 compatibility per `composer.json`'s `"php": "^8.2"`.
- No other code changed — constant values, visibility, and every usage site (`self::CONST_NAME`) are untouched.
- Existing Pest tests for these 3 files pass without modification.
- This fix directly resolves the production ParseError blocking `DispatchCensusRevalidation` autoload, unblocking the "Revalidar apoyos de un líder" button in sigma-betha.
</success_criteria>

<output>
After completion, create `.planning/quick/260804-um4-fix-critico-constantes-de-clase-tipadas-/260804-um4-SUMMARY.md`
</output>

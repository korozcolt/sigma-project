---
phase: quick
plan: 260730-g0h
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Filament/Resources/Leaders/Tables/LeadersTable.php
  - app/Filament/Resources/Voters/Tables/VotersTable.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "LeadersTable columns are toggleable; Correo and Creado start hidden by default"
    - "VotersTable's Campaña column starts hidden by default (was toggleable-visible before)"
  artifacts:
    - path: "app/Filament/Resources/Leaders/Tables/LeadersTable.php"
      provides: "All 5 columns toggleable; email and created_at hidden by default"
    - path: "app/Filament/Resources/Voters/Tables/VotersTable.php"
      provides: "campaign.name column changed to isToggledHiddenByDefault: true"
  key_links: []
---

<objective>
Two related column-visibility changes, same pattern, in two Filament tables — pure column-config
changes, no other logic change.
</objective>

<context>
@app/Filament/Resources/Leaders/Tables/LeadersTable.php
@app/Filament/Resources/Voters/Tables/VotersTable.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add column toggling to LeadersTable, hide Correo/Creado by default</name>
  <files>
    app/Filament/Resources/Leaders/Tables/LeadersTable.php
  </files>
  <read_first>
    app/Filament/Resources/Leaders/Tables/LeadersTable.php
    app/Filament/Resources/Voters/Tables/VotersTable.php (reference pattern — ->toggleable() / ->toggleable(isToggledHiddenByDefault: true) usage, no other config needed for the "Alternar columnas" button to appear)
  </read_first>
  <action>
In `LeadersTable::configure()`, add `->toggleable()` to all 5 columns:
- `name` (Nombre): `->toggleable()` — visible by default
- `email` (Correo): `->toggleable(isToggledHiddenByDefault: true)` — hidden by default
- `coordinator.name` (Coordinador): `->toggleable()` — visible by default
- `registered_voters_count` (Apoyos): `->toggleable()` — visible by default
- `created_at` (Creado): `->toggleable(isToggledHiddenByDefault: true)` — hidden by default

No other change to the file (searchable/sortable/copyable/etc. all stay as-is).

Run `vendor/bin/pint --dirty`.
  </action>
  <verify>
    <automated>grep -c "toggleable" app/Filament/Resources/Leaders/Tables/LeadersTable.php</automated>
  </verify>
  <done>5 ->toggleable() calls present; email and created_at use isToggledHiddenByDefault: true.</done>
</task>

<task type="auto">
  <name>Task 2: Hide VotersTable's Campaña column by default</name>
  <files>
    app/Filament/Resources/Voters/Tables/VotersTable.php
  </files>
  <read_first>
    app/Filament/Resources/Voters/Tables/VotersTable.php (campaign.name column, ~lines 110-114)
  </read_first>
  <action>
Change the `campaign.name` column's `->toggleable()` (no args, visible by default) to
`->toggleable(isToggledHiddenByDefault: true)`. Do not touch any other column in this file —
every other column's current toggle state stays exactly as-is.

Run `vendor/bin/pint --dirty`.
  </action>
  <verify>
    <automated>grep -n -A 5 "campaign.name" app/Filament/Resources/Voters/Tables/VotersTable.php | grep "isToggledHiddenByDefault"</automated>
  </verify>
  <done>campaign.name column has isToggledHiddenByDefault: true; no other column changed.</done>
</task>

</tasks>

<verification>
- `grep -c "toggleable" app/Filament/Resources/Leaders/Tables/LeadersTable.php` returns 5
- `grep -A 5 "campaign.name" app/Filament/Resources/Voters/Tables/VotersTable.php` shows `isToggledHiddenByDefault: true`
- `vendor/bin/pint --dirty` clean
- `git diff app/Filament/Resources/Voters/Tables/VotersTable.php` shows exactly one changed column block
</verification>

<success_criteria>
- Líderes list: Correo and Creado start hidden, all columns toggleable via "Alternar columnas"
- Apoyos list: Campaña starts hidden by default, same as before for every other column
</success_criteria>

<output>
After completion, create `.planning/quick/260730-g0h-add-column-toggle-to-leaderstable-hide-c/260730-g0h-SUMMARY.md`
</output>

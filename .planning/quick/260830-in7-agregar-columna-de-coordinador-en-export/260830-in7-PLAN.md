---
phase: quick
plan: 260830-in7
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Exports/TopLeadersExport.php
  - app/Exports/TopCoordinatorsExport.php
  - app/Filament/Widgets/TopLeadersTable.php
  - app/Filament/Widgets/TopCoordinatorsTable.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "Al exportar el ranking de líderes a Excel, cada fila muestra el nombre del coordinador del líder"
    - "Al exportar el ranking de coordinadores a Excel, cada fila muestra el nombre del articulador del coordinador"
    - "La tabla en pantalla de Ranking de Líderes muestra una columna Coordinador"
    - "La tabla en pantalla de Cobertura y Ranking de Coordinadores muestra una columna Articulador"
  artifacts:
    - path: "app/Exports/TopLeadersExport.php"
      provides: "Columna Coordinador en export Excel de líderes"
      contains: "coordinator?->name"
    - path: "app/Exports/TopCoordinatorsExport.php"
      provides: "Columna Articulador en export Excel de coordinadores"
      contains: "areaCoordinator?->name"
    - path: "app/Filament/Widgets/TopLeadersTable.php"
      provides: "Columna Coordinador en tabla en pantalla"
      contains: "coordinator.name"
    - path: "app/Filament/Widgets/TopCoordinatorsTable.php"
      provides: "Columna Articulador en tabla en pantalla"
      contains: "areaCoordinator.name"
  key_links:
    - from: "app/Exports/TopLeadersExport.php"
      to: "App\\Models\\User::coordinator()"
      via: "eager load ->with('coordinator') + map()"
      pattern: "with\\('coordinator'\\)"
    - from: "app/Exports/TopCoordinatorsExport.php"
      to: "App\\Models\\User::areaCoordinator()"
      via: "eager load ->with('areaCoordinator') + map()"
      pattern: "with\\('areaCoordinator'\\)"
---

<objective>
Mostrar en los exports (Excel) y en las tablas en pantalla de ranking de líderes y coordinadores a qué nivel superior pertenece cada fila: columna "Coordinador" en el ranking de líderes, columna "Articulador" en el ranking de coordinadores.

Purpose: Los coordinadores/articuladores necesitan ver rápidamente, sin buscar en otra pantalla, a qué líder/coordinador está asociado cada registro cuando exportan o revisan el ranking.
Output: `TopLeadersExport`/`TopLeadersTable` con columna Coordinador; `TopCoordinatorsExport`/`TopCoordinatorsTable` con columna Articulador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/Exports/TopLeadersExport.php
@app/Exports/TopCoordinatorsExport.php
@app/Filament/Widgets/TopLeadersTable.php
@app/Filament/Widgets/TopCoordinatorsTable.php

<interfaces>
From app/Models/User.php (relations already exist, do not rename):

```php
public function coordinator(): BelongsTo       // líder -> su coordinador
public function areaCoordinator(): BelongsTo   // coordinador -> su articulador
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar columna Coordinador al export y a la tabla en pantalla de líderes</name>
  <files>app/Exports/TopLeadersExport.php, app/Filament/Widgets/TopLeadersTable.php</files>
  <action>
    En `app/Exports/TopLeadersExport.php`:
    - En `query()`, agregar `->with('coordinator')` a la query devuelta (encadenar junto al `when(...)` existente, sin alterar el filtrado/orden actual).
    - En `headings()`, agregar `'Coordinador'` después de `'Líder'` (antes de `'Email'`).
    - En `map()`, agregar `$leader->coordinator?->name ?? 'N/A'` en la misma posición (después del nombre, antes del email).

    En `app/Filament/Widgets/TopLeadersTable.php`:
    - En la closure de `->query(...)`, agregar `->with('coordinator')` al query builder (encadenar junto al `when(...)` existente).
    - En `->columns([...])`, agregar una nueva `TextColumn::make('coordinator.name')->label('Coordinador')->toggleable()` justo después de la columna `name` (Líder) y antes de `email`.

    No modificar la lógica de filtrado por rol/campaña ni el orden de las demás columnas.
  </action>
  <verify>
    <automated>cd "/Volumes/NAS(MAC)/Data/Herd/sigma-project" && php artisan tinker --execute="echo (new App\Exports\TopLeadersExport())->headings()[1];"</automated>
  </verify>
  <done>headings() incluye 'Coordinador' en la posición 2; map() devuelve el nombre del coordinador o 'N/A'; TopLeadersTable renderiza la columna Coordinador con eager load (sin N+1).</done>
</task>

<task type="auto">
  <name>Task 2: Agregar columna Articulador al export y a la tabla en pantalla de coordinadores</name>
  <files>app/Exports/TopCoordinatorsExport.php, app/Filament/Widgets/TopCoordinatorsTable.php</files>
  <action>
    En `app/Exports/TopCoordinatorsExport.php`:
    - En `query()`, agregar `->with('areaCoordinator')` a la query devuelta.
    - En `headings()`, agregar `'Articulador'` después de `'Coordinador'` (antes de `'Email'`).
    - En `map()`, agregar `$coordinator->areaCoordinator?->name ?? 'N/A'` en la misma posición.

    En `app/Filament/Widgets/TopCoordinatorsTable.php`:
    - En la closure de `->query(...)`, agregar `->with('areaCoordinator')` al query builder.
    - En `->columns([...])`, agregar una nueva `TextColumn::make('areaCoordinator.name')->label('Articulador')->toggleable()` justo después de la columna `name` (Coordinador) y antes de `email`.

    No modificar la lógica de filtrado por rol/campaña, el `apoyos_equipo_count` custom, ni el orden de las demás columnas.
  </action>
  <verify>
    <automated>cd "/Volumes/NAS(MAC)/Data/Herd/sigma-project" && php artisan tinker --execute="echo (new App\Exports\TopCoordinatorsExport())->headings()[1];"</automated>
  </verify>
  <done>headings() incluye 'Articulador' en la posición 2; map() devuelve el nombre del articulador o 'N/A'; TopCoordinatorsTable renderiza la columna Articulador con eager load (sin N+1).</done>
</task>

</tasks>

<verification>
Ejecutar `vendor/bin/pint --dirty` sobre los 4 archivos modificados.

Confirmar en un navegador real (per standing browser-verify-before-prod preference):
- Descargar el export de Ranking de Líderes y confirmar la columna Coordinador con el nombre correcto.
- Descargar el export de Ranking de Coordinadores y confirmar la columna Articulador con el nombre correcto.
- Ver en pantalla ambas tablas (dashboard) y confirmar que las nuevas columnas aparecen junto al nombre.
</verification>

<success_criteria>
- Ambos exports Excel incluyen la nueva columna con el nombre correcto del nivel superior (o 'N/A' si no tiene).
- Ambas tablas en pantalla muestran la nueva columna, sin errores N+1 (relaciones eager-loaded).
- No se modifican los nombres de relación existentes (`coordinator`, `areaCoordinator`) ni la lógica de filtrado/scoping por campaña o rol.
- `vendor/bin/pint --dirty` pasa limpio.
</success_criteria>

<output>
After completion, create `.planning/quick/260830-in7-agregar-columna-de-coordinador-en-export/260830-in7-SUMMARY.md`
</output>

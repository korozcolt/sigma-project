---
phase: quick-260730-fi4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Filament/Resources/Voters/Pages/ListVoters.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "The Apoyos list page's cross-cédula CSV action no longer displays a label/heading that can be confused with the Dashboard's internal-duplicates widget"
    - "The unrelated Dashboard DuplicatesReportTable widget ('Informe de Duplicados') is untouched and still renders exactly as before"
  artifacts:
    - path: "app/Filament/Resources/Voters/Pages/ListVoters.php"
      provides: "Action::make('duplicatesReport') with updated label and modalHeading"
  key_links:
    - from: "app/Filament/Resources/Voters/Pages/ListVoters.php"
      to: "Action::make('duplicatesReport')"
      via: "->label() and ->modalHeading() calls"
      pattern: "Cruzar Cédulas Externas \\(CSV\\)"
---

<objective>
Rename the Apoyos list page's "duplicatesReport" action label and modal heading so it no longer reads "Reporte de Duplicados" — a name that collides conceptually with the unrelated Dashboard "Informe de Duplicados" widget (`App\Filament\Widgets\DuplicatesReportTable`, which reports duplicate/disputed cédula records already in the database). This action is actually a "cross external CSV cédulas against registered Apoyos" tool, unrelated in purpose to that widget.

Purpose: Eliminate user-facing naming confusion between two distinct features that happen to share similar Spanish wording.
Output: Updated label/modalHeading strings only — no logic, query, form, or action-callback changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/Filament/Resources/Voters/Pages/ListVoters.php

<!-- Do NOT touch — separate, correct, unrelated feature confirmed by grep/read during planning -->
<!-- app/Filament/Widgets/DuplicatesReportTable.php — heading "Informe de Duplicados" stays as-is -->

<interfaces>
Current block in app/Filament/Resources/Voters/Pages/ListVoters.php (lines ~144-148):
```php
Action::make('duplicatesReport')
    ->label('Reporte de Duplicados')
    ->icon('heroicon-o-document-magnifying-glass')
    ->color('warning')
    ->modalHeading('Reporte de duplicados por cédula')
    ->modalSubmitActionLabel('Generar')
```
Only `->label()` and `->modalHeading()` string arguments change. The action `->form()`, `->action()` callback, icon, color, and action key `'duplicatesReport'` all stay exactly as-is.

Confirmed during planning: `grep -rln "Reporte de Duplicados\|Reporte de duplicados por cédula\|duplicatesReport" tests/` returned no matches — no test file asserts these strings, so no test files need updating.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename duplicatesReport action label and modal heading</name>
  <files>app/Filament/Resources/Voters/Pages/ListVoters.php</files>
  <action>
    In the `Action::make('duplicatesReport')` block:
    - Change `->label('Reporte de Duplicados')` to `->label('Cruzar Cédulas Externas (CSV)')`
    - Change `->modalHeading('Reporte de duplicados por cédula')` to `->modalHeading('Cruzar cédulas externas contra Apoyos registrados')`

    Do not change anything else in this action block (icon, color, form fields, action callback, notifications, streamDownload logic, or the `'duplicatesReport'` action key itself remain untouched).

    Do not touch app/Filament/Widgets/DuplicatesReportTable.php or its 'Informe de Duplicados' heading — confirmed unrelated feature.

    Run `vendor/bin/pint --dirty` after the edit.
  </action>
  <verify>
    <automated>grep -n "Cruzar Cédulas Externas (CSV)" app/Filament/Resources/Voters/Pages/ListVoters.php && grep -n "Cruzar cédulas externas contra Apoyos registrados" app/Filament/Resources/Voters/Pages/ListVoters.php && ! grep -n "Reporte de Duplicados\|Reporte de duplicados por cédula" app/Filament/Resources/Voters/Pages/ListVoters.php</automated>
  </verify>
  <done>The action's label and modalHeading read "Cruzar Cédulas Externas (CSV)" / "Cruzar cédulas externas contra Apoyos registrados" respectively; no other line in the file changed; app/Filament/Widgets/DuplicatesReportTable.php is unmodified.</done>
</task>

</tasks>

<verification>
- `grep -rn "Reporte de Duplicados\|Reporte de duplicados por cédula" app/` returns no matches inside ListVoters.php (DuplicatesReportTable.php's distinct "Informe de Duplicados" string is unaffected and expected to remain).
- `git diff app/Filament/Resources/Voters/Pages/ListVoters.php` shows only the two string changes.
- `git status` confirms app/Filament/Widgets/DuplicatesReportTable.php has no changes.
</verification>

<success_criteria>
The Apoyos list page's CSV cross-reference action displays "Cruzar Cédulas Externas (CSV)" as its button label and "Cruzar cédulas externas contra Apoyos registrados" as its modal heading, with zero behavior change and zero impact on the unrelated Dashboard duplicates widget.
</success_criteria>

<output>
After completion, create `.planning/quick/260730-fi4-rename-reporte-de-duplicados-apoyos-acti/260730-fi4-SUMMARY.md`
</output>

---
phase: quick
plan: 260726-eu3
type: execute
wave: 1
depends_on: []
files_modified:
  - registraduria-service/app.py
  - app/Services/InfovotantesService.php
  - app/Providers/AppServiceProvider.php
  - config/services.php
  - .env.example
  - tests/Feature/Services/InfovotantesServiceTest.php
  - tests/Feature/Services/PollingPlaceResolverPriorityTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "When the infovotantes adapter reports reachable, PollingPlaceResolver's automated cascade tries it BEFORE the wsp (RegistraduriaService) adapter and never calls wsp"
    - "When the infovotantes adapter reports unreachable, PollingPlaceResolver's cascade falls through to the wsp adapter exactly as it does today"
    - "The Python microservice serves both the wsp flow and the infovotantes flow from the same Flask process on port 5757, without one route affecting the other"
    - "RegistraduriaService's existing behavior (wsp lookup, HTML parsing, reachability probe) and its existing Pest coverage are completely unchanged"
    - "The infovotantes adapter has its own independent isReachable() check against the eleccionescolombia/infovotantes domain, separate from wsp's probe"
  artifacts:
    - path: "registraduria-service/app.py"
      provides: "New POST /lookup/infovotantes route running the restored 2captcha + Playwright-fetch flow against apiweb-eleccionescolombia.infovotantes.com, sharing the existing /result/<session_id> route and sessions dict. Existing /lookup (wsp) route and its functions untouched."
    - path: "app/Services/InfovotantesService.php"
      provides: "New LiveSourceAdapter implementation: startLookup() posts to /lookup/infovotantes, getResult() passes through already-structured fields (no HTML parsing needed), isReachable() probes the infovotantes/eleccionescolombia domain independently."
    - path: "app/Providers/AppServiceProvider.php"
      provides: "liveAdapters bound as [InfovotantesService, RegistraduriaService] — infovotantes first, wsp second, per user's priority decision."
    - path: "config/services.php"
      provides: "New 'infovotantes' config block (url, live_enabled, probe_url) independent from the existing 'registraduria' block."
    - path: "tests/Feature/Services/InfovotantesServiceTest.php"
      provides: "Coverage for InfovotantesService's isReachable()/startLookup()/getResult(), mirroring RegistraduriaServiceReachabilityTest's structure."
    - path: "tests/Feature/Services/PollingPlaceResolverPriorityTest.php"
      provides: "Coverage proving the container-resolved PollingPlaceResolver's automated cascade tries InfovotantesService first and falls back to RegistraduriaService only when infovotantes is unreachable, plus a binding-order regression test."
  key_links:
    - from: "app/Providers/AppServiceProvider.php liveAdapters array"
      to: "app/Services/InfovotantesService.php + app/Services/RegistraduriaService.php"
      via: "[$app->make(InfovotantesService::class), $app->make(RegistraduriaService::class)] — order is the priority"
    - from: "app/Services/InfovotantesService.php startLookup()"
      to: "registraduria-service/app.py POST /lookup/infovotantes"
      via: "Http::post baseUrl + /lookup/infovotantes with {cedula}"
    - from: "app/Services/InfovotantesService.php isReachable()"
      to: "config('services.infovotantes.probe_url')"
      via: "Http::connectTimeout(2)->timeout(3)->withoutRedirecting()->get(...)"
    - from: "app/Services/PollingPlaceResolver.php resolveAutomated()/attemptLiveAutomated()/isLiveReachable()/startLiveLookup()"
      to: "the ordered $liveAdapters iterable"
      via: "foreach ($this->liveAdapters as $adapter) — unchanged, no resolver code modified"
---

<objective>
Restore the pre-Phase-9 "infovotantes" live-lookup flow (2captcha -> eleccionescolombia.registraduria.gov.co sitekey -> apiweb-eleccionescolombia.infovotantes.com structured API) as a SECOND, coexisting LiveSourceAdapter, registered ahead of the current wsp adapter (RegistraduriaService) in PollingPlaceResolver's priority-ordered liveAdapters list. This is purely additive: wsp's Phase 9/11 behavior (HTML parsing, reachability probe, reconciliation, tests) must not change in any way.

Purpose: the user trusts infovotantes's underlying data source more than wsp's, and both domains are expected to eventually come back online (currently DNS-dead). When infovotantes is back, it should be tried first; wsp remains the fallback/support adapter, exactly matching what Phase 8's multi-adapter LiveSourceAdapter/PollingPlaceResolver architecture was built to support.

Output:
- registraduria-service/app.py gains a second route (POST /lookup/infovotantes) running the restored flow, sharing the existing /result/<session_id> route and sessions dict. The existing /lookup route (wsp) and its functions are byte-for-byte untouched.
- app/Services/InfovotantesService.php — new LiveSourceAdapter implementation.
- app/Providers/AppServiceProvider.php — liveAdapters: [InfovotantesService, RegistraduriaService] (infovotantes first).
- New services.infovotantes config block + .env.example entries.
- Pest coverage for the new adapter and for cascade priority ordering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Key contracts extracted from the codebase. Use directly, no exploration needed.

From app/Services/LiveSourceAdapter.php (the contract InfovotantesService must implement, unchanged):
- startLookup(string $cedula): string — throws Exception if unreachable/error
- getResult(string $sessionId): array{status, data, error}
- isReachable(): bool — cheap check, no captcha cost

From app/Services/RegistraduriaService.php (the wsp adapter, pattern to mirror, DO NOT MODIFY THIS FILE):
- protected string $baseUrl set from config('services.registraduria.url')
- startLookup(): POST {baseUrl}/lookup {cedula} -> session_id, throws on failure
- getResult(): GET {baseUrl}/result/{sessionId}; if data.raw_message_html present, parses HTML into the 7 fields
- isReachable(): gated by config('services.registraduria.live_enabled'); GET config('services.registraduria.probe_url') with connectTimeout(2)/timeout(3)/withoutRedirecting(); returns successful() || redirect()

From app/Providers/AppServiceProvider.php (current binding, to be changed):
  register() currently does:
    $this->app->bind(PollingPlaceResolver::class, fn ($app) => new PollingPlaceResolver(
        liveAdapters: [$app->make(RegistraduriaService::class)],
    ));

From config/services.php (current registraduria block, DO NOT MODIFY, add a sibling infovotantes block instead):
  'registraduria' => [
      'url' => env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757'),
      'live_enabled' => env('REGISTRADURIA_LIVE_ENABLED', true),
      'probe_url' => env('REGISTRADURIA_PROBE_URL', 'https://wsp.registraduria.gov.co/censo/consultar/'),
  ],

From app/Services/PollingPlaceResolver.php (NOT modified by this plan, confirms the cascade already iterates $liveAdapters in order everywhere: isLiveReachable(), startLiveLookup(), attemptLiveAutomated()/resolveAutomated() all use foreach ($this->liveAdapters as $adapter). attemptLiveAutomated() is the one that gates on $adapter->isReachable() before trying startLookup() and moves to the next adapter on any give-up — that is the fallback mechanism this plan's tests must exercise. startLiveLookup() intentionally always uses the FIRST adapter unconditionally (pre-existing behavior, confirmed by PollingPlaceResolverTest.php Test 6, not something this plan changes or needs to re-test). Registering [InfovotantesService, RegistraduriaService] in that order is the entire priority mechanism, no resolver code changes needed.)

From the pre-Phase-9 registraduria-service/app.py (git commit ac1dd5a, restore this flow's logic into new functions/route, do not touch existing wsp route/functions):
  Constants: TWO_CAPTCHA_KEY (already loaded via load_env(), already present in registraduria-service/.env)
    INFOVOTANTES_SITEKEY = "6Lc9DmgrAAAAAJAjWVhjDy1KSgqzqJikY5z7I9SV"
    INFOVOTANTES_PAGE_URL = "https://eleccionescolombia.registraduria.gov.co/identificacion"
    INFOVOTANTES_API = "https://apiweb-eleccionescolombia.infovotantes.com/api/v1/citizen/get-information"

  _lookup_infovotantes_async(session_id, cedula) flow:
    1. POST https://2captcha.com/in.php (method=userrecaptcha, googlekey=INFOVOTANTES_SITEKEY, pageurl=INFOVOTANTES_PAGE_URL, invisible=0, json=1) via aiohttp -> captcha_id
    2. Poll https://2captcha.com/res.php every 5s up to 30x -> token
    3. Playwright headless chromium, new_context(user_agent=desktop UA, ignore_https_errors=True), page.request.fetch(INFOVOTANTES_API, method=POST,
       headers={Authorization: Bearer {token}, Content-Type, Origin/Referer: eleccionescolombia.../identificacion, User-Agent, Accept: */*, Sec-Fetch-Site: cross-site, Sec-Fetch-Mode: cors, Sec-Fetch-Dest: empty},
       data=json({identification: cedula, identification_type: "CC", election_code: "presidencia", platform: "web", module: "polling_place"}), timeout=20000)
    4. result = await api_response.json(); if not result.get("status"): raise RuntimeError(f"API error: {result}")
    5. pp = result["data"]["polling_place"]; addr = pp["place_address"]
       data = {puesto_nombre: f"{pp['stand_code']} - {pp['stand']}".strip(" -"), puesto_codigo: pp['stand_code'], zona_codigo: addr['zone'],
               mesa_numero: str(pp['table']), departamento: addr['state'], municipio: addr['town'], direccion: addr['address']}
    6. _set(session_id, status="done", data=data)
  Uses the SAME sessions dict / sessions_lock / _set() helper already defined in current app.py for the wsp flow.
  On any exception, the sync wrapper catches Exception and calls _set(session_id, status="error", error=str(exc).split(chr(10))[0]) — same shape as the existing wsp _run().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore infovotantes flow as a second Flask route (registraduria-service/app.py)</name>
  <files>registraduria-service/app.py</files>
  <action>
Add the pre-Phase-9 infovotantes flow (from git commit ac1dd5a, see interfaces above) back into registraduria-service/app.py as an ADDITIONAL route, without modifying a single existing line related to the wsp flow (WSP_PAGE_URL, _lookup_async, _run, the /lookup route, the /result/session_id route all stay byte-for-byte identical — confirm with git diff before finishing that only additions appear, no deletions/changes to existing lines).

Steps:
1. Add new module-level constants directly below the existing WSP_PAGE_URL constant: INFOVOTANTES_SITEKEY, INFOVOTANTES_PAGE_URL, INFOVOTANTES_API (exact values in interfaces above).
2. Add a new async function _lookup_infovotantes_async(session_id: str, cedula: str) -> None, ported from ac1dd5a's _lookup_async: 2captcha solve against INFOVOTANTES_SITEKEY/INFOVOTANTES_PAGE_URL via aiohttp, then Playwright headless chromium + page.request.fetch() against INFOVOTANTES_API with the Bearer token and header/body shape documented above, then map result into the same 7-field shape (puesto_nombre, puesto_codigo, zona_codigo, mesa_numero, departamento, municipio, direccion). Reuse the existing _set() helper and the existing shared sessions dict / sessions_lock — do not create a second dict or a second lock.
3. On any failure (2captcha submit/poll error, no token, API error) raise a RuntimeError with a descriptive message, matching the old flow's error-handling shape.
4. Add _run_infovotantes(session_id: str, cedula: str) -> None mirroring the existing _run(): wraps asyncio.run(_lookup_infovotantes_async(session_id, cedula)) in try/except, calls _set(session_id, status="error", error=str(exc).split(chr(10))[0]) on exception.
5. Add a new Flask route:
   @app.route("/lookup/infovotantes", methods=["POST"])
   def lookup_infovotantes():
       body = request.get_json(silent=True) or {}
       cedula = str(body.get("cedula", "")).strip()
       if not cedula:
           return jsonify({"error": "El campo 'cedula' es requerido."}), 400
       session_id = str(uuid.uuid4())
       with sessions_lock:
           sessions[session_id] = {"status": "pending", "data": None, "error": None}
       threading.Thread(target=_run_infovotantes, args=(session_id, cedula), daemon=True).start()
       return jsonify({"session_id": session_id}), 200
6. Do NOT add a second /result/session_id route — the existing one already reads from the shared sessions dict by session_id regardless of which flow populated it, so it serves both flows unmodified.
7. No new pip dependencies required — aiohttp, flask, playwright are already imported and used by the current wsp flow.

Update the module docstring at the top of the file to add one short sentence noting both flows now coexist on this process (wsp at /lookup, infovotantes at /lookup/infovotantes), without removing or altering the existing Phase 9 wsp documentation block.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('registraduria-service/app.py').read()); print('syntax OK')" && grep -c "WSP_PAGE_URL" registraduria-service/app.py && grep -c "/lookup/infovotantes" registraduria-service/app.py && git diff --stat registraduria-service/app.py</automated>
  </verify>
  <done>
    registraduria-service/app.py has a working /lookup/infovotantes route producing sessions with the same 7-field structured data shape wsp uses after HTML parsing. python3 ast.parse confirms syntactic validity. git diff shows only additions, no line belonging to the wsp flow is modified or removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: InfovotantesService adapter + config + priority-ordered registration</name>
  <files>
    app/Services/InfovotantesService.php
    config/services.php
    .env.example
    app/Providers/AppServiceProvider.php
  </files>
  <action>
Create app/Services/InfovotantesService.php implementing LiveSourceAdapter, mirroring RegistraduriaService's structure exactly except for the base endpoint and the fact that infovotantes' Python flow already returns fully-structured fields (no HTML parsing step needed):

- Constructor sets protected string $baseUrl from config('services.infovotantes.url', 'http://localhost:5757').
- startLookup(string $cedula): string — POST {baseUrl}/lookup/infovotantes with ['cedula' => $cedula], timeout(10). On non-successful response, Log::error('InfovotantesService: lookup failed', [...]) then throw new \Exception('El servicio de infovotantes no está disponible. Inicia el servicio Python primero.'). Read session_id via $response->json('session_id'); throw new \Exception('El servicio de infovotantes no devolvió un session_id válido.') if missing. Return the session_id.
- getResult(string $sessionId): array — GET {baseUrl}/result/{sessionId}, timeout(5). 404 -> ['status' => 'error', 'data' => null, 'error' => 'Sesión no encontrada']. Non-successful -> ['status' => 'error', 'data' => null, 'error' => 'Error comunicándose con el servicio']. Otherwise return $response->json() directly (no HTML parsing — document this difference from RegistraduriaService in a docblock).
- isReachable(): bool — if !config('services.infovotantes.live_enabled') return false. Otherwise try Http::connectTimeout(2)->timeout(3)->withoutRedirecting()->get(config('services.infovotantes.probe_url')), return $response->successful() || $response->redirect(), catch ConnectionException and return false. Add a docblock noting this probes the eleccionescolombia/infovotantes host independently of RegistraduriaService's wsp probe.
- Use declare(strict_types=1) and explicit use statements (Illuminate\Http\Client\ConnectionException, Illuminate\Support\Facades\Http, Illuminate\Support\Facades\Log) per CLAUDE.md's import rule — no aliases, no inline paths.

In config/services.php, add a sibling 'infovotantes' block directly after the existing 'registraduria' block (do not modify the 'registraduria' block itself):
  'infovotantes' => [
      'url' => env('INFOVOTANTES_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757')),
      'live_enabled' => env('INFOVOTANTES_LIVE_ENABLED', true),
      'probe_url' => env('INFOVOTANTES_PROBE_URL', 'https://eleccionescolombia.registraduria.gov.co/identificacion'),
  ],

In .env.example, add three lines directly after the existing REGISTRADURIA_PROBE_URL line:
  INFOVOTANTES_SERVICE_URL=http://localhost:5757
  INFOVOTANTES_LIVE_ENABLED=true
  INFOVOTANTES_PROBE_URL=https://eleccionescolombia.registraduria.gov.co/identificacion

In app/Providers/AppServiceProvider.php, add "use App\Services\InfovotantesService;" alphabetically ordered with the other use App\Services\* import, before "use App\Services\RegistraduriaService;". Change the liveAdapters binding to register infovotantes FIRST, wsp SECOND:
  $this->app->bind(PollingPlaceResolver::class, fn ($app) => new PollingPlaceResolver(
      liveAdapters: [
          $app->make(InfovotantesService::class),
          $app->make(RegistraduriaService::class),
      ],
  ));
Do not touch anything else in this file (boot() and User::observe() stay exactly as-is).
  </action>
  <verify>
    <automated>php -l app/Services/InfovotantesService.php && php -l app/Providers/AppServiceProvider.php && php artisan config:clear && php artisan tinker --execute="echo config('services.infovotantes.probe_url');"</automated>
  </verify>
  <done>
    app/Services/InfovotantesService.php exists, implements LiveSourceAdapter, php -l passes on both modified PHP files. config('services.infovotantes.*') resolves the new url/live_enabled/probe_url keys. AppServiceProvider's liveAdapters array is [InfovotantesService, RegistraduriaService] in that order.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pest coverage — adapter behavior + resolver priority ordering</name>
  <files>
    tests/Feature/Services/InfovotantesServiceTest.php
    tests/Feature/Services/PollingPlaceResolverPriorityTest.php
  </files>
  <action>
Create tests/Feature/Services/InfovotantesServiceTest.php, mirroring the structure of tests/Feature/Services/RegistraduriaServiceReachabilityTest.php plus getResult/startLookup coverage (no HTML-parsing test needed here, since infovotantes never produces raw_message_html):

- "returns false with no HTTP call when the live kill switch is off" — config(['services.infovotantes.live_enabled' => false]), Http::fake(), expect isReachable() toBeFalse, Http::assertNothingSent().
- "returns true when the probe responds successfully" — config live_enabled true, Http::fake([config('services.infovotantes.probe_url').'*' => Http::response('', 200)]), expect isReachable() toBeTrue.
- "returns false when the probe is unreachable" — Http::fake([config('services.infovotantes.probe_url').'*' => Http::failedConnection()]), expect isReachable() toBeFalse.
- "starts a lookup against the /lookup/infovotantes endpoint and returns the session_id" — Http::fake(['*/lookup/infovotantes' => Http::response(['session_id' => 'info-session-abc'], 200)]), expect startLookup('1102812122') toBe 'info-session-abc', and Http::assertSent asserting the request URL ends with '/lookup/infovotantes'.
- "throws when the lookup endpoint is unreachable" — Http::fake(['*/lookup/infovotantes' => fn () => throw new \Illuminate\Http\Client\ConnectionException('Connection refused')]), expect(fn () => $service->startLookup(...))->toThrow(\Illuminate\Http\Client\ConnectionException::class).
- "passes through already-structured fields from getResult without any HTML parsing" — Http::fake(['*/result/*' => Http::response(['status' => 'done', 'data' => ['puesto_nombre' => 'IE LA CAMPIÑA', 'puesto_codigo' => '1', 'zona_codigo' => '1', 'mesa_numero' => '05', 'departamento' => 'SUCRE', 'municipio' => 'SINCELEJO', 'direccion' => 'CALLE FALSA 123'], 'error' => null])]), assert result status 'done' and data fields pass through unmodified (proves no HTML-parsing code path exists on this class).
- "returns an error status when the session is not found" — Http::fake(['*/result/*' => Http::response(['error' => 'not found'], 404)]), expect getResult() status toBe 'error'.

Create tests/Feature/Services/PollingPlaceResolverPriorityTest.php (uses RefreshDatabase since resolveAutomated() touches Voter/PollingPlace/Municipality):

Test A — "resolveAutomated tries infovotantes first and never calls the wsp lookup endpoint when infovotantes succeeds":
  - config(['services.infovotantes.live_enabled' => true, 'services.registraduria.live_enabled' => true]).
  - Http::fake([config('services.infovotantes.probe_url').'*' => Http::response('', 200), '*/lookup/infovotantes' => Http::response(['session_id' => 'info-session'], 200), '*/result/info-session' => Http::response(['status' => 'done', 'data' => ['puesto_nombre' => 'IE LA CAMPIÑA', 'puesto_codigo' => '1', 'zona_codigo' => '1', 'mesa_numero' => '05', 'departamento' => 'SUCRE', 'municipio' => 'SINCELEJO', 'direccion' => 'CALLE FALSA 123'], 'error' => null])]).
  - $voter = Voter::factory()->create(['polling_place_source' => null]); $resolver = app(PollingPlaceResolver::class); $result = $resolver->resolveAutomated('1102812122', $voter).
  - expect($result)->not->toBeNull() and $result->source toBe PollingPlaceSource::LIVE.
  - Http::assertSent(fn ($request) => str_contains($request->url(), '/lookup/infovotantes')).
  - Http::assertNotSent(fn ($request) => str_ends_with($request->url(), '/lookup') && ! str_contains($request->url(), '/lookup/infovotantes')) — proves wsp's startLookup endpoint was never hit.

Test B — "resolveAutomated falls back to wsp when infovotantes is unreachable, without ever calling its lookup endpoint":
  - config(['services.infovotantes.live_enabled' => true, 'services.registraduria.live_enabled' => true]).
  - Http::fake([config('services.infovotantes.probe_url').'*' => Http::failedConnection(), config('services.registraduria.probe_url').'*' => Http::response('', 200), '*/lookup' => Http::response(['session_id' => 'wsp-session'], 200), '*/result/wsp-session' => Http::response(['status' => 'done', 'data' => ['raw_message_html' => ''], 'error' => null])]).
  - $voter = Voter::factory()->create(['polling_place_source' => null]); $resolver = app(PollingPlaceResolver::class); $resolver->resolveAutomated('1102812122', $voter).
  - Http::assertNotSent(fn ($request) => str_contains($request->url(), '/lookup/infovotantes')) — infovotantes' lookup endpoint is never called when unreachable.
  - Http::assertSent(fn ($request) => str_ends_with($request->url(), '/lookup')) — wsp's lookup endpoint IS called as the fallback.

Test C — "AppServiceProvider registers InfovotantesService ahead of RegistraduriaService in liveAdapters":
  - $resolver = app(PollingPlaceResolver::class); use ReflectionProperty(PollingPlaceResolver::class, 'liveAdapters'), setAccessible(true), getValue($resolver) to obtain the array/iterable, convert to array if needed via iterator_to_array if it's not already a plain array.
  - expect($adapters[0])->toBeInstanceOf(InfovotantesService::class) and $adapters[1] toBeInstanceOf RegistraduriaService::class — a direct regression test on the binding order this plan changes.

After writing both files, run the full targeted test suite plus a Pint check per CLAUDE.md.
  </action>
  <verify>
    <automated>php artisan test --filter=InfovotantesServiceTest && php artisan test --filter=PollingPlaceResolverPriorityTest && php artisan test --filter=RegistraduriaServiceReachabilityTest && php artisan test --filter=RegistraduriaServiceParserTest && php artisan test --filter=PollingPlaceResolverTest && vendor/bin/pint --dirty --test</automated>
  </verify>
  <done>
    tests/Feature/Services/InfovotantesServiceTest.php and tests/Feature/Services/PollingPlaceResolverPriorityTest.php exist and pass. All pre-existing wsp-related test files (RegistraduriaServiceReachabilityTest, RegistraduriaServiceParserTest, PollingPlaceResolverTest) still pass unmodified, proving zero regression. Pint reports no changes needed.
  </done>
</task>

</tasks>

<verification>
Run the full targeted suite one more time to confirm nothing else broke:
php artisan test --filter=Registraduria
php artisan test --filter=Infovotantes
php artisan test --filter=PollingPlaceResolver
git diff registraduria-service/app.py — confirm zero modifications to existing wsp-related lines, only additions.
</verification>

<success_criteria>
- InfovotantesService exists, implements LiveSourceAdapter, and is registered ahead of RegistraduriaService in PollingPlaceResolver's liveAdapters binding.
- registraduria-service/app.py serves both flows (wsp at /lookup, infovotantes at /lookup/infovotantes) from the same Flask process, sharing /result/session_id, with zero modification to existing wsp code.
- Automated tests prove: infovotantes tried first when reachable (wsp never called), wsp used as fallback when infovotantes is unreachable, and all pre-existing wsp/resolver tests still pass unmodified.
- No dependency changes, no resolver code changes — purely additive per the user's explicit "coexist, not replace" decision.
</success_criteria>

<output>
After completion, create `.planning/quick/260726-eu3-restore-infovotantes-as-a-coexisting-liv/260726-eu3-SUMMARY.md`
</output>

---
phase: quick
plan: 260730-cs3
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/VoterValidationService.php
  - app/Jobs/DispatchCensusRevalidation.php
  - app/Jobs/ValidateVoterAgainstCensus.php
  - app/Jobs/ReconcileFallbackPollingPlaces.php
  - app/Models/RevalidationRun.php
  - database/migrations/2026_07_30_000000_create_revalidation_runs_table.php
  - app/Console/Commands/RemediateMisrejectedCensusVoters.php
  - app/Filament/Resources/Voters/Tables/VotersTable.php
  - app/Filament/Widgets/RevalidationProgressWidget.php
  - app/Filament/Resources/Voters/Pages/ListVoters.php
  - tests/Feature/CensusUnifiedResolutionTest.php
  - tests/Feature/RevalidationCoverageTest.php
  - tests/Feature/RemediateMisrejectedCensusVotersTest.php
  - tests/Feature/VoterValidationServiceTest.php
  - tests/Feature/Services/VoterValidationServiceTest.php
  - tests/Feature/VoterCensusValidationTest.php
  - tests/Feature/Jobs/DispatchCensusRevalidationTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "Census validation resolves via PollingPlaceResolver::resolveAutomated() cascade (permanent lookup -> live -> national snapshot), NOT the orphaned census_records table"
    - "A single revalidation pass resolves BOTH census status AND polling_place_source for the same voter"
    - "Voters with polling_place_source IS NULL are picked up by the revalidation flow even when their census status is already verified_census"
    - "ReconcileFallbackPollingPlaces performs first-time resolution for polling_place_source IS NULL voters, not only upgrades of already-resolved ones"
    - "The flow no longer produces a hard REJECTED_CENSUS until the full cascade is exhausted; unresolved voters land in CENSUS_NOT_FOUND (re-enters reconciliation) instead"
    - "A cedula present in national_identity_records counts as census-found even when no polling place is resolvable"
    - "Remediation command reverts sigma-betha campaign_id=1 rejected_census voters to pending_review with a ValidationHistory audit row, touching no other campaign"
    - "Pressing 'Revalidar apoyos de un lider' shows a non-blocking progress indicator (started_at, in-progress, finished_at + processed/changed summary) without blocking the Apoyos screen"
  artifacts:
    - path: "app/Services/VoterValidationService.php"
      provides: "Unified census+polling resolution delegating to PollingPlaceResolver, national-identity fallback, no dead-end REJECTED_CENSUS"
    - path: "app/Jobs/DispatchCensusRevalidation.php"
      provides: "Widened selection (status set + polling_place_source IS NULL) processed inline with RevalidationRun progress tracking"
    - path: "app/Jobs/ReconcileFallbackPollingPlaces.php"
      provides: "whereNotNull guard dropped so NULL-source voters get first-time resolution"
    - path: "app/Models/RevalidationRun.php"
      provides: "Progress record: campaign_id, leader_id, started_at, finished_at, total, processed, changed"
    - path: "app/Console/Commands/RemediateMisrejectedCensusVoters.php"
      provides: "One-off, campaign-scoped remediation of misrejected voters with audit rows"
    - path: "app/Filament/Widgets/RevalidationProgressWidget.php"
      provides: "wire:poll non-blocking progress banner reading latest RevalidationRun for the campaign"
  key_links:
    - from: "VoterValidationService::validateAndUpdate"
      to: "PollingPlaceResolver::resolveAutomated"
      via: "constructor-injected resolver, resolvedVia: 'census_validation'"
    - from: "DispatchCensusRevalidation"
      to: "VoterValidationService::validateAndUpdate"
      via: "inline loop over widened voter set, updating RevalidationRun counters"
    - from: "RevalidationProgressWidget"
      to: "RevalidationRun (latest for campaign)"
      via: "wire:poll on ListVoters header"
---

<objective>
Fix the root cause documented in `.planning/debug/apoyos-mass-rejected-census-betha.md`: census validation currently compares Apoyos against the orphaned per-campaign `census_records` table (45 incidental rows on sigma-betha), mass-marking real voters `REJECTED_CENSUS`, and `polling_place_source` stays NULL for 187/188 because no scheduled job ever does first-time polling-place resolution.

Unify census validation onto the SAME automated cascade the polling-place engine already uses (`PollingPlaceResolver::resolveAutomated()`), so ONE revalidation pass resolves BOTH the census status AND `polling_place_source` together. Widen the revalidation and reconciliation selections so NULL-source voters are covered, not just by-status ones. Remediate the 148 already-misrejected sigma-betha voters. Add a non-blocking on-screen progress indicator for the "Revalidar apoyos de un lider" action.

Purpose: one button/job resolves census + polling place in a single pass using the free permanent cache first, so no second separate fix is needed later and no repeated live-lookup cost is paid.
Output: refactored service, widened jobs, progress record + widget, remediation command, Pest tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/apoyos-mass-rejected-census-betha.md
@.planning/quick/260730-cs3-fix-root-cause-in-planning-debug-apoyos-/260730-cs3-CONTEXT.md
@app/Services/PollingPlaceResolver.php
@app/Services/VoterValidationService.php
@app/Jobs/DispatchCensusRevalidation.php
@app/Jobs/ValidateVoterAgainstCensus.php
@app/Jobs/ReconcileFallbackPollingPlaces.php
@app/Models/Voter.php
@app/Models/NationalIdentityRecord.php
@app/Enums/VoterStatus.php
@app/Filament/Resources/Voters/Tables/VotersTable.php
@routes/console.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify VoterValidationService onto PollingPlaceResolver cascade (census status + polling_place_source in one pass)</name>
  <files>
    app/Services/VoterValidationService.php,
    tests/Feature/CensusUnifiedResolutionTest.php,
    tests/Feature/VoterValidationServiceTest.php,
    tests/Feature/Services/VoterValidationServiceTest.php,
    tests/Feature/VoterCensusValidationTest.php
  </files>
  <read_first>
    app/Services/PollingPlaceResolver.php (resolveAutomated, resolveFromPermanentLookup, resolveFromNationalSnapshot, persist),
    app/Services/VoterValidationService.php (current census_records logic to replace),
    app/Models/NationalIdentityRecord.php (keyed on `cedula`),
    app/Enums/VoterStatus.php
  </read_first>
  <behavior>
    - Test: voter whose cedula is in `registraduria_lookups` -> resolveAutomated hits tier 0, voter ends VERIFIED_CENSUS (or VERIFIED_REGISTRADURIA when source is LIVE) AND polling_place_source is set (not NULL)
    - Test: voter resolvable only via national_census_records snapshot -> VERIFIED_CENSUS and polling_place_source = SNAPSHOT
    - Test: voter NOT resolvable to a polling place but cedula EXISTS in national_identity_records -> counts as census-found (VERIFIED_CENSUS), polling_place_source may remain NULL
    - Test: voter with cedula absent from every tier AND absent from national_identity_records -> status becomes CENSUS_NOT_FOUND (NOT REJECTED_CENSUS)
    - Test: validateAndUpdate no longer queries census_records (assert an existing census_records row for a different cedula does not flip an otherwise-unresolvable voter to found)
    - Test: a ValidationHistory row is written on status change with validation_type = 'census'
  </behavior>
  <action>
Inject `PollingPlaceResolver` into `VoterValidationService` via constructor property promotion (add `use App\Services\PollingPlaceResolver;`). Keep the class resolvable from the container (it already is via `app(VoterValidationService::class)` and `ValidateVoterAgainstCensus`'s handle injection).

Rewrite `validateAgainstCensus(Voter $voter)` so census "found" is determined by the unified cascade, NOT `census_records`:
1. `$result = $this->resolver->resolveAutomated($voter->document_number, $voter, resolvedVia: 'census_validation');`
   - This already persists `polling_place_source` + a `PollingPlaceResolution` audit row when any tier (permanent lookup / live / national snapshot) hits, honoring the no-downgrade guard. This is the polling-place half of the single pass.
2. `$found = $result !== null || NationalIdentityRecord::query()->where('cedula', $voter->document_number)->exists();`
   - The national-identity check is the census-existence fallback: a real cedula counts as found even when no polling place is resolvable (a person can exist in the roll without a snapshot-resolvable puesto).

Rewrite `updateVoterStatus(Voter $voter, bool $found)`:
- **No-downgrade guard (exact set).** If the voter's current status is already one of `VERIFIED_REGISTRADURIA`, `VERIFIED_CALL`, `CONFIRMED`, `VOTED`, `DID_NOT_VOTE`, leave the status unchanged (early-return before mutating status and before writing a status-change ValidationHistory row), regardless of the found/not-found result — these represent stronger downstream operational states that census validation must not clobber. VoterStatus has no built-in precedence method, so enumerate this set explicitly in the action (e.g. a private `const NON_DOWNGRADABLE = [...]` or an inline `in_array` check against these five cases). Confirm each name exists in `app/Enums/VoterStatus.php` while reading it first; if `DID_NOT_VOTE` is spelled differently in the enum, use the actual case name and keep the intent (any post-verification/Day-D terminal state is protected).
- found (and current status NOT in the protected set): `VERIFIED_CENSUS` + `census_validated_at = now()`.
- not found (and current status NOT in the protected set): set `CENSUS_NOT_FOUND` (re-enters the reconciliation cycle) instead of the dead-end `REJECTED_CENSUS`. Update the note text accordingly (Spanish). REJECTED_CENSUS is no longer produced by this path.
- Keep writing the `ValidationHistory` row (previous_status, new_status, validated_by = Auth::id(), validation_type = 'census', Spanish note).

Update `documentExistsInCensus(int $campaignId, string $documentNumber)` (used by register-voter.blade.php / create-leader.blade.php warning): stop querying `census_records`; return true if the cedula is in `registraduria_lookups` OR `national_census_records` OR `national_identity_records`. Signature stays the same so callers are untouched. Note: register-voter.blade.php already checks `RegistraduriaLookup` separately for VERIFIED_REGISTRADURIA — that is fine, leave the blade untouched.

`getCensusInfo()` / `findInCensus()` returning `CensusRecord`: these are now vestigial. Leave `census_records`/`CensusRecord` model and the incidental `HasRegistraduriaPolling` write ALONE (CONTEXT: Claude's discretion, no forced removal). Mark `CensusImporter` and the census_records-based lookups as deprecated in a short scope note comment only — do not delete.

**Update the three pre-existing test files that assert the OLD, now-contradicted behavior** (these MUST be reconciled or the suite breaks on green):
- `tests/Feature/VoterValidationServiceTest.php` (repo root):
  - `validateAgainstCensus()` returning a `CensusRecord` match (~lines 33-38, 112, 219): the method no longer returns a `CensusRecord`. Rework these to assert the new contract — census "found" is driven by `resolveAutomated` + `national_identity_records`, and the observable outcome is `polling_place_source` set and status `VERIFIED_CENSUS` (not a returned CensusRecord). Seed `registraduria_lookups` / `national_census_records` / `national_identity_records` fixtures instead of `census_records`.
  - `updateVoterStatus($voter, false)` asserting `REJECTED_CENSUS` (~line 88): change the expected status to `CENSUS_NOT_FOUND` (this path no longer produces REJECTED_CENSUS).
  - `documentExistsInCensus()` returning true when only a `CensusRecord` exists (~line 168): change the fixture to seed `registraduria_lookups`/`national_census_records`/`national_identity_records` (the new sources) and assert true; add/keep a case proving a lone `census_records` row does NOT make it true.
  - `Voter::where('status', REJECTED_CENSUS)->count()` after `validatePendingVoters()` (~line 157): assert on `CENSUS_NOT_FOUND` instead for the unresolvable cohort.
- `tests/Feature/Services/VoterValidationServiceTest.php` (~line 45): a not-found voter now ends at `CENSUS_NOT_FOUND`, not `REJECTED_CENSUS` — update the assertion.
- `tests/Feature/VoterCensusValidationTest.php` (~lines 69/75): `validateAndUpdate()` on an unresolvable voter now yields `CENSUS_NOT_FOUND`, not `REJECTED_CENSUS` — update both assertions.

**Also fix 3 "found"-fixture tests that seed a `CensusRecord` expecting it to register as found — these break silently (found flips to false) because `census_records` is no longer consulted at all, independent of the wording fixes above:**
- `tests/Feature/VoterValidationServiceTest.php`, test `'validates all pending voters for a campaign'` (~lines 115-158): seeds two `CensusRecord` rows expecting `$result['found']` to be 2 (~line 153) and `Voter::where('status', VERIFIED_CENSUS)->count()` to be 2 (~line 156). Re-seed those two voters' fixtures with `registraduria_lookups` (or `national_identity_records`) rows keyed on their `document_number` instead of `CensusRecord`, so they still resolve as found and land on `VERIFIED_CENSUS`.
- `tests/Feature/Services/VoterValidationServiceTest.php` (~lines 12-46): the `voterA` fixture is a `CensusRecord` expected to yield `VERIFIED_CENSUS` (~line 44) and `$result['found']` == 1 (~line 40). Re-seed `voterA`'s fixture as a `registraduria_lookups`/`national_identity_records` row for the same `document_number` so it still resolves found; keep `voterB`'s not-found case updated per the wording fix above.
- `tests/Feature/VoterCensusValidationTest.php`, test `'the validation history records the previous status before validation ran'` (~lines 78-100): seeds a `CensusRecord` for document `33333333` expecting `history->new_status` == `VERIFIED_CENSUS` (~line 99). Re-seed that document as a `national_identity_records` (or `registraduria_lookups`) row instead so it still resolves as found and the history assertion holds.

For any case that becomes a pure duplicate of a new `CensusUnifiedResolutionTest.php` scenario, delete it there and leave a one-line `// superseded by CensusUnifiedResolutionTest` note rather than maintaining two copies. Do NOT remove whole test files without approval (per repo rules) — edit assertions and fixtures in place.

Run `vendor/bin/pint --dirty`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/CensusUnifiedResolutionTest.php tests/Feature/VoterValidationServiceTest.php tests/Feature/Services/VoterValidationServiceTest.php tests/Feature/VoterCensusValidationTest.php --stop-on-failure</automated>
  </verify>
  <done>All tests pass, including the three reconciled pre-existing suites (no lingering REJECTED_CENSUS / CensusRecord-match / census_records-driven assertions). Census found/not-found is driven by resolveAutomated + national_identity_records, polling_place_source is set in the same call, REJECTED_CENSUS is no longer produced, census_records is not consulted, and post-verification statuses are not downgraded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen revalidation + reconciliation coverage to NULL-source voters, with RevalidationRun progress tracking</name>
  <files>
    app/Jobs/DispatchCensusRevalidation.php,
    app/Jobs/ValidateVoterAgainstCensus.php,
    app/Jobs/ReconcileFallbackPollingPlaces.php,
    app/Models/RevalidationRun.php,
    database/migrations/2026_07_30_000000_create_revalidation_runs_table.php,
    tests/Feature/RevalidationCoverageTest.php,
    tests/Feature/Jobs/DispatchCensusRevalidationTest.php
  </files>
  <read_first>
    app/Jobs/DispatchCensusRevalidation.php (current whereIn status filter),
    app/Jobs/ReconcileFallbackPollingPlaces.php (the whereNotNull('polling_place_source') guard at line 33 that blocks first-time resolution),
    app/Enums/VoterStatus.php, app/Enums/PollingPlaceSource.php
  </read_first>
  <behavior>
    - Test: DispatchCensusRevalidation selects a voter with status VERIFIED_CENSUS but polling_place_source IS NULL (proves NULL-source coverage independent of census status)
    - Test: DispatchCensusRevalidation selects PENDING_REVIEW, CENSUS_NOT_FOUND, and REJECTED_CENSUS voters (proves already-rejected re-enter)
    - Test: DispatchCensusRevalidation does NOT re-touch a voter already VERIFIED_CENSUS with polling_place_source = LIVE (nothing left to resolve)
    - Test: after DispatchCensusRevalidation runs, a RevalidationRun row exists with finished_at set, total = selected count, processed = total, changed >= 0
    - Test: ReconcileFallbackPollingPlaces selects a voter with polling_place_source IS NULL (first-time resolution) as well as a non-LIVE-tier voter (upgrade) — dropping the whereNotNull guard
    - Test: ReconcileFallbackPollingPlaces still skips reconciliation_exhausted_at voters and LIVE-source voters
  </behavior>
  <action>
**Migration + `RevalidationRun` model:** table `revalidation_runs` — `id`, `campaign_id` (nullable, indexed), `leader_id` (nullable), `started_at`, `finished_at` (nullable), `total` (unsigned int, default 0), `processed` (unsigned int, default 0), `changed` (unsigned int, default 0), timestamps. Model `App\Models\RevalidationRun` with `$fillable` for those columns and `casts()` for the datetimes.

**DispatchCensusRevalidation** — widen selection and process inline (it is a queued `ShouldQueue`, so the loop already runs off the HTTP request = non-blocking to the UI). Add an optional `?int $campaignId` alongside the existing `?int $leaderId` so the run record can be campaign-scoped for the widget:
```
$voters = Voter::query()
    ->where(function ($q) {
        $q->whereIn('status', [
            VoterStatus::PENDING_REVIEW->value,
            VoterStatus::CENSUS_NOT_FOUND->value,
            VoterStatus::REJECTED_CENSUS->value,
        ])->orWhereNull('polling_place_source');
    })
    ->when($this->leaderId, fn ($query) => $query->where('registered_by', $this->leaderId))
    ->when($this->campaignId, fn ($query) => $query->where('campaign_id', $this->campaignId))
    ->get();
```
Create a `RevalidationRun` (started_at = now(), total = $voters->count(), campaign_id, leader_id). Loop the voters calling `app(VoterValidationService::class)->validateAndUpdate($voter)` inline; increment `processed`; increment `changed` when the returned voter's status differs from its previous status. Persist counters (batch the update — e.g. update the run once at the end, or every N) and set `finished_at = now()` when done. Keep the existing `Log::info('census.revalidation.dispatched', ...)`.
- Because `validateAndUpdate` now also runs `resolveAutomated`, a single pass over this widened set resolves BOTH census status AND polling_place_source. This is the unification acceptance criterion.
- `ValidateVoterAgainstCensus` is no longer dispatched per-voter by this job. Either delete it (it becomes unused) OR keep it only if another caller needs it — grep confirms its only caller is DispatchCensusRevalidation, so remove it and its reference. (Per repo rules: delete cleanly, no compat shim.)

**Rewrite `tests/Feature/Jobs/DispatchCensusRevalidationTest.php` to match the new inline-processing behavior** (it currently imports and asserts against the deleted class, which would fatal with class-not-found):
- Remove the `use App\Jobs\ValidateVoterAgainstCensus;` import (line ~4) and **every** reference to `ValidateVoterAgainstCensus::class` in this file — confirmed at lines ~23-25 (`Bus::assertDispatched`/`assertNotDispatched` in the first test) AND lines ~39-40 (`Bus::assertDispatched`/`assertNotDispatched` in the second test), 5 call sites total. The class no longer exists — any surviving reference fatals the suite with class-not-found. The job no longer dispatches a per-voter job — it processes voters inline via `app(VoterValidationService::class)->validateAndUpdate()`.
- Replace those dispatch assertions with the new observable outcomes: after the job runs, the selected voters have their status/`polling_place_source` updated, and a `RevalidationRun` row exists with `finished_at` set and `total`/`processed`/`changed` populated (this overlaps with `RevalidationCoverageTest`; keep whichever assertions belong here — the leader-scoping selection — and defer the full RevalidationRun-counter coverage to `RevalidationCoverageTest` to avoid duplication).
- Keep and adapt the **leader-scoping** test: assert that passing `leaderId` only touches voters with `registered_by = $leaderId` (now observed via which voters changed status, not via a dispatched-job payload). If the test used `Bus::fake()` solely to assert dispatch, drop `Bus::fake()` for that case so the inline processing actually runs against the DB.
- Keep the `census:reconcile-validation` schedule/command tests as-is if they assert the command exists and is scheduled; only touch assertions that assumed the old per-voter-dispatch behavior. If a schedule test asserted the command dispatches `ValidateVoterAgainstCensus`, update it to assert the new inline effect (voters resolved / RevalidationRun written) instead.

**ReconcileFallbackPollingPlaces (ACCEPTANCE CRITERION — first-time NULL-source resolution):** drop the `whereNotNull('polling_place_source')` guard at line 33 so the hourly `census:reconcile-live` job also performs the FIRST resolution for NULL-source voters, not just upgrades. Replace the selection predicate with:
```
->where(function ($q) {
    $q->whereNull('polling_place_source')
      ->orWhere('polling_place_source', '!=', PollingPlaceSource::LIVE->value);
})
->whereNull('reconciliation_exhausted_at')
```
Keep MAX_VOTERS_PER_RUN, the exhaustion counter, ordering (order by `polling_place_resolved_at` — NULLs sort first on MySQL, which is desirable here), and all logging. `resolveAutomated` already persists first-time results, so no other change is needed inside the loop.
- Acceptance: a voter with `polling_place_source IS NULL` and `reconciliation_exhausted_at IS NULL` is now selected and gets a first-time resolution attempt; LIVE-source and exhausted voters are still excluded.

Run `vendor/bin/pint --dirty`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/RevalidationCoverageTest.php tests/Feature/Jobs/DispatchCensusRevalidationTest.php --stop-on-failure</automated>
  </verify>
  <done>Both jobs select NULL-source voters; DispatchCensusRevalidation writes a completed RevalidationRun with accurate total/processed/changed; ReconcileFallbackPollingPlaces does first-time resolution with the whereNotNull guard removed; ValidateVoterAgainstCensus removed; DispatchCensusRevalidationTest no longer references the deleted class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One-off remediation of the 148 misrejected sigma-betha voters</name>
  <files>
    app/Console/Commands/RemediateMisrejectedCensusVoters.php,
    tests/Feature/RemediateMisrejectedCensusVotersTest.php
  </files>
  <read_first>
    app/Models/ValidationHistory.php (column names: previous_status, new_status, validated_by, validation_type, notes),
    app/Enums/VoterStatus.php
  </read_first>
  <behavior>
    - Test: rejected_census voters in the target campaign are reverted to pending_review
    - Test: each reverted voter gets a ValidationHistory row (previous_status = rejected_census, new_status = pending_review, validation_type = 'census', a note explaining this is a data-correction, validated_by nullable)
    - Test: voters in OTHER campaigns are NOT touched (campaign scoping)
    - Test: voters with statuses other than rejected_census are NOT touched
    - Test: command is idempotent (a second run touches 0 voters)
  </behavior>
  <action>
Command `App\Console\Commands\RemediateMisrejectedCensusVoters`, signature `census:remediate-misrejected {--campaign=1 : Campaign ID to remediate} {--dry-run}`, description in Spanish. `declare(strict_types=1)`.

`handle()`:
1. `$campaignId = (int) $this->option('campaign');`
2. Select `Voter::where('campaign_id', $campaignId)->where('status', VoterStatus::REJECTED_CENSUS->value)->get()`.
3. For each (skip writes when `--dry-run`): within a DB transaction, revert `status => PENDING_REVIEW` and write a `ValidationHistory` row (previous_status = REJECTED_CENSUS, new_status = PENDING_REVIEW, validated_by = null, validation_type = 'census', notes = Spanish explanation that the previous rejection was a data-correction — rejection was against the orphaned census_records table, not a real absence).
4. Print a summary: campaign id, count reverted. Return `self::SUCCESS`.

Do NOT auto-run against production and do NOT touch any campaign other than the one passed (default 1 = sigma-betha "Alcaldia 2027"). This is a manual, campaign-scoped operator command; it does not get a scheduler entry. After the census unification (Task 1) ships, these pending_review voters re-enter the normal cascade via the revalidation/reconciliation jobs and resolve correctly.

Run `vendor/bin/pint --dirty`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/RemediateMisrejectedCensusVotersTest.php --stop-on-failure</automated>
  </verify>
  <done>Command reverts only the target campaign's rejected_census voters to pending_review with audit rows, is idempotent, and leaves other campaigns/statuses untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Non-blocking progress indicator on the Apoyos screen</name>
  <files>
    app/Filament/Resources/Voters/Tables/VotersTable.php,
    app/Filament/Widgets/RevalidationProgressWidget.php,
    app/Filament/Resources/Voters/Pages/ListVoters.php
  </files>
  <read_first>
    app/Filament/Resources/Voters/Tables/VotersTable.php (revalidateLeaderVoters header action at ~line 245),
    app/Filament/Resources/Voters/Pages/ListVoters.php,
    app/Filament/Widgets/FallbackSourceOverview.php (existing widget pattern for reference),
    app/Services/CampaignContext.php (currentCampaignId)
  </read_first>
  <action>
**VotersTable header action** — in `revalidateLeaderVoters`'s `->action()`, pass the current campaign so the run is scoped for the widget:
```
DispatchCensusRevalidation::dispatch(
    leaderId: (int) $data['leader_id'],
    campaignId: \App\Services\CampaignContext::currentCampaignId(),
);
```
Keep the existing success Notification. (Confirm the constructor arg order matches Task 2's `DispatchCensusRevalidation(?int $leaderId = null, ?int $campaignId = null)`.)

**RevalidationProgressWidget** (`app/Filament/Widgets/RevalidationProgressWidget.php`) — a lightweight widget rendering a non-blocking banner from the latest `RevalidationRun` for the current campaign, with `protected static ?string $pollingInterval = '5s';` (Filament v4 wire:poll). It reads (does not block): if the latest run has `finished_at === null` show "Revalidacion en progreso" + started_at + processed/total; if finished, show finished_at + summary ("N apoyos revisados, M cambiaron de estado"); if no run exists, render nothing. Query: `RevalidationRun::where('campaign_id', CampaignContext::currentCampaignId())->latest('started_at')->first()`. Use a simple blade view for the widget (follow an existing widget's view pattern) — Spanish copy. Least new surface area: a stat/overview-style or plain custom widget, executor's discretion per CONTEXT.

**ListVoters page** — register the widget in `getHeaderWidgets()` so it appears above the Apoyos table:
```
protected function getHeaderWidgets(): array
{
    return [ RevalidationProgressWidget::class ];
}
```
Ensure the widget is full-width and does not block table interaction (it is a sibling header widget; wire:poll refreshes only the widget).

Run `vendor/bin/pint --dirty`. Since this is Filament UI, verify in the browser per CLAUDE.md if a dev server is available; otherwise state that UI was not browser-verified.
  </action>
  <verify>
    <automated>php artisan config:clear && php artisan test tests/Feature/RevalidationCoverageTest.php --stop-on-failure</automated>
    <manual>Load /admin Apoyos list, press "Revalidar apoyos de un lider", confirm the banner shows in-progress then a finished summary without blocking the table (browser check if dev server available).</manual>
  </verify>
  <done>Header widget polls the latest RevalidationRun and shows start/in-progress/finished summary in Spanish, non-blocking; the header action scopes the run to the current campaign.</done>
</task>

</tasks>

<verification>
- `php artisan test tests/Feature/CensusUnifiedResolutionTest.php tests/Feature/RevalidationCoverageTest.php tests/Feature/RemediateMisrejectedCensusVotersTest.php tests/Feature/VoterValidationServiceTest.php tests/Feature/Services/VoterValidationServiceTest.php tests/Feature/VoterCensusValidationTest.php tests/Feature/Jobs/DispatchCensusRevalidationTest.php` — all pass
- `grep -rn "census_records" app/Services/VoterValidationService.php` — no census_records lookups drive validation (only vestigial/deprecated notes remain)
- `grep -n "whereNotNull('polling_place_source')" app/Jobs/ReconcileFallbackPollingPlaces.php` — no match (guard removed)
- `php artisan census:remediate-misrejected --campaign=1 --dry-run` — reports the count with no writes
- `vendor/bin/pint --dirty` — clean
</verification>

<success_criteria>
- Census validation is driven by PollingPlaceResolver::resolveAutomated() + national_identity_records, not census_records
- One revalidation pass sets BOTH census status and polling_place_source for the same voter
- NULL-source voters are covered by DispatchCensusRevalidation AND by ReconcileFallbackPollingPlaces (whereNotNull guard removed) — explicit acceptance criteria met
- REJECTED_CENSUS is no longer produced until the full cascade is exhausted; unresolved -> CENSUS_NOT_FOUND
- The 148 sigma-betha misrejected voters are revertible to pending_review with audit rows, campaign-scoped and idempotent
- A non-blocking progress indicator shows start/in-progress/finished summary for the revalidation run
- Every change is covered by Pest tests
</success_criteria>

<output>
After completion, create `.planning/quick/260730-cs3-fix-root-cause-in-planning-debug-apoyos-/260730-cs3-SUMMARY.md`
</output>

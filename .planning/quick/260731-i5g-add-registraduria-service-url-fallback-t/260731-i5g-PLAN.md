---
phase: quick
plan: 260731-i5g
type: execute
wave: 1
depends_on: []
files_modified:
  - config/services.php
  - .env.example
  - tests/Feature/Services/ConsultaCensoServiceTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "In any environment where CONSULTA_CENSO_SERVICE_URL is never explicitly set (e.g. production today), ConsultaCensoService still resolves its baseUrl to REGISTRADURIA_SERVICE_URL's value (the correct Docker-network hostname), not a hardcoded localhost default"
    - "The fallback order is exactly 3-level: CONSULTA_CENSO_SERVICE_URL (own var, highest priority) > REGISTRADURIA_SERVICE_URL (shared middle fallback) > 'http://localhost:5757' (final hardcoded default), identical in shape to the pre-existing infovotantes.url pattern"
    - "No behavior changes for any environment that already sets CONSULTA_CENSO_SERVICE_URL explicitly (own var still wins, per the chain's first tier)"
  artifacts:
    - path: "config/services.php"
      provides: "consulta_censo.url default changed from a flat env('CONSULTA_CENSO_SERVICE_URL', 'http://localhost:5757') to the nested env('CONSULTA_CENSO_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757')) fallback chain, mirroring the infovotantes block exactly. registraduria.url, infovotantes.url, consulta_censo.live_enabled, and consulta_censo.probe_url all left untouched."
    - path: "tests/Feature/Services/ConsultaCensoServiceTest.php"
      provides: "New test proving the real 3-level env() fallback chain order (own var > REGISTRADURIA_SERVICE_URL > localhost default) by freshly re-requiring config/services.php with manipulated process env vars -- not a static/regex check of the file source."
  key_links:
    - from: "config/services.php consulta_censo.url"
      to: "env('REGISTRADURIA_SERVICE_URL')"
      via: "env('CONSULTA_CENSO_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757'))"
    - from: "tests/Feature/Services/ConsultaCensoServiceTest.php new fallback-chain test"
      to: "config/services.php"
      via: "require base_path('config/services.php') re-executed fresh (not require_once) after putenv()/$_ENV/$_SERVER manipulation, so env() calls actually re-resolve rather than reading Laravel's already-booted cached config"
---

<objective>
Change `config/services.php`'s `consulta_censo.url` default from a flat `http://localhost:5757` fallback to the same nested `env('CONSULTA_CENSO_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757'))` chain already used by the `infovotantes` block, so `ConsultaCensoService` keeps resolving to the correct Docker-network hostname in production even though `CONSULTA_CENSO_SERVICE_URL` is never explicitly set there.

Purpose: production's real `.env` sets `REGISTRADURIA_SERVICE_URL=http://sigma-registraduria:5757` (the Docker-network hostname where the Python microservice actually lives) but has no `CONSULTA_CENSO_SERVICE_URL` of its own, and cannot have one added right now (explicit operational constraint). Without this fallback, `ConsultaCensoService` currently falls straight through to the hardcoded `http://localhost:5757` default in production, which is wrong (Laravel and the Python service run in separate containers there) -- it just hasn't broken yet because `consulta_censo.live_enabled` traffic hasn't hit that path in anger. This fix is the permanent resilience pattern, matching the established `infovotantes` precedent exactly, not a stopgap.

Output:
- `config/services.php`: `consulta_censo.url` uses the 3-level nested fallback.
- `.env.example`: confirmed already consistent with the (uncommented) convention used for `INFOVOTANTES_SERVICE_URL` -- no changes needed there (documented in this plan's task, not silently skipped).
- `tests/Feature/Services/ConsultaCensoServiceTest.php`: new test directly exercising the real fallback-chain evaluation order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Current state of config/services.php (registraduria/infovotantes/consulta_censo blocks -- only consulta_censo.url's default changes in this plan):

    'registraduria' => [
        'url' => env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757'),
        'live_enabled' => env('REGISTRADURIA_LIVE_ENABLED', true),
        'probe_url' => env('REGISTRADURIA_PROBE_URL', 'https://wsp.registraduria.gov.co/censo/consultar/'),
    ],

    'infovotantes' => [
        'url' => env('INFOVOTANTES_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757')),
        'live_enabled' => env('INFOVOTANTES_LIVE_ENABLED', true),
        'probe_url' => env('INFOVOTANTES_PROBE_URL', 'https://eleccionescolombia.registraduria.gov.co/identificacion'),
    ],

    'consulta_censo' => [
        'url' => env('CONSULTA_CENSO_SERVICE_URL', 'http://localhost:5757'),
        'live_enabled' => env('CONSULTA_CENSO_LIVE_ENABLED', true),
        'probe_url' => env('CONSULTA_CENSO_PROBE_URL', 'https://consultacenso.registraduria.gov.co/'),
    ],

Only the `consulta_censo.url` line's default changes in this plan.

.env.example's relevant lines today (no comments on any of these three var groups -- confirmed by direct inspection, this IS the established "documentation convention" to mirror, i.e. none needed):

    REGISTRADURIA_SERVICE_URL=http://localhost:5757
    REGISTRADURIA_LIVE_ENABLED=true
    REGISTRADURIA_PROBE_URL=https://apiweb-eleccionescolombia.infovotantes.com
    INFOVOTANTES_SERVICE_URL=http://localhost:5757
    INFOVOTANTES_LIVE_ENABLED=true
    INFOVOTANTES_PROBE_URL=https://eleccionescolombia.registraduria.gov.co/identificacion
    CONSULTA_CENSO_SERVICE_URL=http://localhost:5757
    CONSULTA_CENSO_LIVE_ENABLED=true
    CONSULTA_CENSO_PROBE_URL=https://consultacenso.registraduria.gov.co/

`app/Services/ConsultaCensoService.php`'s constructor (unmodified by this plan, this is WHY the config default matters):

    public function __construct()
    {
        $this->baseUrl = config('services.consulta_censo.url', 'http://localhost:5757');
    }

Existing tests/Feature/Services/ConsultaCensoServiceTest.php (unmodified test cases -- every existing test already explicitly sets config(['services.consulta_censo.*' => ...]) itself, so none of them depend on the default value being changed; confirmed safe to leave as-is, only appending a new test):

    use App\Services\ConsultaCensoService;
    use Illuminate\Http\Client\ConnectionException;
    use Illuminate\Support\Facades\Http;

    it('returns false with no HTTP call when the live kill switch is off', function () { ... });
    it('returns true when the probe responds successfully', function () { ... });
    it('returns false when the probe is unreachable', function () { ... });
    it('starts a lookup against the /lookup/censo endpoint and returns the session_id', function () { ... });
    it('throws when the lookup endpoint is unreachable', function () { ... });
    it('passes through already-structured fields from getResult without any HTML parsing', function () { ... });
    it('returns an error status when the session is not found', function () { ... });
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nest consulta_censo.url's fallback through REGISTRADURIA_SERVICE_URL + prove the chain with a real env-re-evaluation test</name>
  <files>
    config/services.php
    tests/Feature/Services/ConsultaCensoServiceTest.php
  </files>
  <behavior>
    - Test: when CONSULTA_CENSO_SERVICE_URL is unset but REGISTRADURIA_SERVICE_URL is set to a custom value, a freshly re-evaluated config/services.php resolves consulta_censo.url to that REGISTRADURIA_SERVICE_URL value (the actual bug this plan fixes).
    - Test: when both are unset, it falls through to the hardcoded 'http://localhost:5757' default (proves the chain terminates correctly, doesn't just always inherit the wrong tier).
    - Test: when CONSULTA_CENSO_SERVICE_URL IS set, it still wins over REGISTRADURIA_SERVICE_URL (proves own-var priority is preserved, zero regression for environments that already configure it explicitly).
  </behavior>
  <action>
In `config/services.php`, change ONLY the `consulta_censo.url` line (do not touch `registraduria.url`, `infovotantes.url`, `consulta_censo.live_enabled`, or `consulta_censo.probe_url`):

    'url' => env('CONSULTA_CENSO_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757')),

This is byte-for-byte the same nested-fallback shape as the existing `infovotantes.url` line directly above it in the same file.

`.env.example` needs NO changes: `INFOVOTANTES_SERVICE_URL` (the established precedent for this exact fallback pattern) has no distinguishing comment in `.env.example` today -- it's a plain `KEY=value` line identical in style to `CONSULTA_CENSO_SERVICE_URL`'s existing line. Since there is no documentation convention beyond the plain line itself, `CONSULTA_CENSO_SERVICE_URL=http://localhost:5757` already mirrors it correctly as-is. Confirm this by re-reading `.env.example` before finishing (do not add a comment that `INFOVOTANTES_SERVICE_URL` itself doesn't have).

Append a new test to `tests/Feature/Services/ConsultaCensoServiceTest.php` (after the existing 7 tests, same file, same Pest function style, `App\Services\ConsultaCensoService` already imported at the top). This test must NOT rely on Laravel's already-booted/cached `config()` repository (env() is only evaluated once at boot and Laravel's `config()` values are cached afterward) -- instead it re-executes `config/services.php`'s actual array-returning source fresh via plain `require` (not `require_once`, which would only run once and then return `true` on subsequent calls) after manipulating the real process environment via `putenv()` + `$_ENV` + `$_SERVER` together (matching how Laravel's own env() reader observes environment changes), then restores the original values in a `finally` block so no other test in the suite is affected:

    it('resolves consulta_censo.url through the real 3-level env fallback chain: own var > REGISTRADURIA_SERVICE_URL > localhost default', function () {
        $original = [
            'CONSULTA_CENSO_SERVICE_URL' => $_ENV['CONSULTA_CENSO_SERVICE_URL'] ?? null,
            'REGISTRADURIA_SERVICE_URL' => $_ENV['REGISTRADURIA_SERVICE_URL'] ?? null,
        ];

        $setEnv = function (string $key, ?string $value): void {
            if ($value === null) {
                putenv($key);
                unset($_ENV[$key], $_SERVER[$key]);
            } else {
                putenv("{$key}={$value}");
                $_ENV[$key] = $value;
                $_SERVER[$key] = $value;
            }
        };

        try {
            // Own var unset, middle-tier set -> resolves to REGISTRADURIA_SERVICE_URL (the actual bug this plan fixes).
            $setEnv('CONSULTA_CENSO_SERVICE_URL', null);
            $setEnv('REGISTRADURIA_SERVICE_URL', 'http://sigma-registraduria:5757');
            $config = require base_path('config/services.php');
            expect($config['consulta_censo']['url'])->toBe('http://sigma-registraduria:5757');

            // Both unset -> falls through to the hardcoded localhost default.
            $setEnv('REGISTRADURIA_SERVICE_URL', null);
            $config = require base_path('config/services.php');
            expect($config['consulta_censo']['url'])->toBe('http://localhost:5757');

            // Own var set -> wins over the middle-tier fallback (zero regression for explicit configs).
            $setEnv('CONSULTA_CENSO_SERVICE_URL', 'http://custom-consulta:9999');
            $setEnv('REGISTRADURIA_SERVICE_URL', 'http://sigma-registraduria:5757');
            $config = require base_path('config/services.php');
            expect($config['consulta_censo']['url'])->toBe('http://custom-consulta:9999');
        } finally {
            $setEnv('CONSULTA_CENSO_SERVICE_URL', $original['CONSULTA_CENSO_SERVICE_URL']);
            $setEnv('REGISTRADURIA_SERVICE_URL', $original['REGISTRADURIA_SERVICE_URL']);
        }
    });

Run `vendor/bin/pint --dirty` on the modified test file before finishing, per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -F "env('CONSULTA_CENSO_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL'," config/services.php && php artisan test --filter=ConsultaCensoServiceTest</automated>
  </verify>
  <done>
    config/services.php's consulta_censo.url reads env('CONSULTA_CENSO_SERVICE_URL', env('REGISTRADURIA_SERVICE_URL', 'http://localhost:5757')) -- confirmed via grep. All 8 tests in tests/Feature/Services/ConsultaCensoServiceTest.php pass (7 pre-existing + 1 new), including the new test proving the real 3-level fallback order by re-requiring the config file with manipulated process env vars. registraduria.url, infovotantes.url, consulta_censo.live_enabled, and consulta_censo.probe_url are unchanged (confirmed via git diff showing only the single intended line changed in config/services.php). .env.example has zero diff.
  </done>
</task>

</tasks>

<verification>
git diff config/services.php -- confirm only the consulta_censo.url line changed, nothing else in the file.
git diff .env.example -- confirm zero changes.
php artisan test --filter=ConsultaCensoServiceTest -- confirm all 8 tests pass.
vendor/bin/pint --dirty --test -- confirm no PSR-12 issues on the modified test file.
</verification>

<success_criteria>
- config/services.php's consulta_censo.url resolves through CONSULTA_CENSO_SERVICE_URL > REGISTRADURIA_SERVICE_URL > 'http://localhost:5757', matching the infovotantes.url precedent exactly.
- A new, passing Pest test directly proves the 3-level fallback order by re-evaluating the real config file source with manipulated environment variables (not a static/regex assertion against file text).
- Zero changes to registraduria.url, infovotantes.url, consulta_censo.live_enabled, consulta_censo.probe_url, or .env.example.
- All pre-existing ConsultaCensoServiceTest cases still pass unmodified.
</success_criteria>

<output>
After completion, create `.planning/quick/260731-i5g-add-registraduria-service-url-fallback-t/260731-i5g-SUMMARY.md`
</output>

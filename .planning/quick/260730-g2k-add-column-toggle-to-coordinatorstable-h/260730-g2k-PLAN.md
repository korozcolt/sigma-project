---
phase: quick
plan: 260730-g2k
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "CoordinatorsTable columns are toggleable; Correo and Creado start hidden by default"
  artifacts:
    - path: "app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php"
      provides: "All columns toggleable; email and created_at hidden by default"
  key_links: []
---

<objective>
Same column-visibility pattern just applied to LeadersTable/VotersTable (quick task 260730-g0h),
now applied to CoordinatorsTable: make columns toggleable, hide Correo and Creado by default —
per explicit user request "las columnas ocultas que apliquen tambien para coordinadores".
</objective>

<context>
@app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add column toggling to CoordinatorsTable, hide Correo/Creado by default</name>
  <files>
    app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php
  </files>
  <read_first>
    app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php
    app/Filament/Resources/Leaders/Tables/LeadersTable.php (just-applied reference pattern from quick task 260730-g0h — same ->toggleable() / ->toggleable(isToggledHiddenByDefault: true) usage)
  </read_first>
  <action>
In `CoordinatorsTable::configure()`, add `->toggleable()` to all 5 columns:
- `name` (Nombre): `->toggleable()` — visible by default
- `email` (Correo): `->toggleable(isToggledHiddenByDefault: true)` — hidden by default
- `municipality.name` (Municipio): `->toggleable()` — visible by default
- `leaders_count` (Líderes): `->toggleable()` — visible by default (keep its existing `->visible(fn (): bool => Schema::hasColumn(...))` conditional untouched, just add toggleable alongside it)
- `created_at` (Creado): `->toggleable(isToggledHiddenByDefault: true)` — hidden by default

No other change to the file (searchable/sortable/copyable/placeholder/the Schema::hasColumn visibility check all stay as-is).

Run `vendor/bin/pint --dirty`.
  </action>
  <verify>
    <automated>grep -c "toggleable" app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php</automated>
  </verify>
  <done>5 ->toggleable() calls present; email and created_at use isToggledHiddenByDefault: true; leaders_count's existing visible() conditional untouched.</done>
</task>

</tasks>

<verification>
- `grep -c "toggleable" app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php` returns 5
- `vendor/bin/pint --dirty` clean
</verification>

<success_criteria>
- Coordinadores list: Correo and Creado start hidden, all columns toggleable via "Alternar columnas"
</success_criteria>

<output>
After completion, create `.planning/quick/260730-g2k-add-column-toggle-to-coordinatorstable-h/260730-g2k-SUMMARY.md`
</output>

---
phase: 260830-il4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Filament/Resources/Voters/Tables/VotersTable.php
  - app/Filament/Resources/Leaders/Tables/LeadersTable.php
  - app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php
  - tests/Feature/Filament/VoterResourceHierarchyColumnsTest.php
  - tests/Feature/Filament/LeaderResourceArticuladorColumnTest.php
  - tests/Feature/Filament/CoordinatorResourceArticuladorColumnTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "Un usuario que ve el visor de Apoyos puede identificar el Coordinador y el Articulador del líder/coordinador que registró cada apoyo"
    - "Un usuario que ve el visor de Líderes puede identificar el Articulador del coordinador de cada líder"
    - "Un usuario que ve el visor de Coordinadores puede identificar su Articulador directo"
  artifacts:
    - path: "app/Filament/Resources/Voters/Tables/VotersTable.php"
      provides: "Columnas Coordinador y Articulador con eager load de registeredBy.coordinator.areaCoordinator"
    - path: "app/Filament/Resources/Leaders/Tables/LeadersTable.php"
      provides: "Columna Articulador (coordinator.areaCoordinator.name) con eager load"
    - path: "app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php"
      provides: "Columna Articulador (areaCoordinator.name) con eager load"
  key_links:
    - from: "app/Filament/Resources/Voters/Tables/VotersTable.php"
      to: "app/Models/User.php"
      via: "registeredBy relation state() closure resolving coordinator/areaCoordinator by role"
      pattern: "registeredBy\\?->coordinator\\?->areaCoordinator"
---

<objective>
Agregar columnas de jerarquía superior (Coordinador / Articulador) a los visores Filament de Apoyos, Líderes y Coordinadores, siguiendo el patrón ya existente en `ApoyosLideresCoordinadoresTable` (líneas ~50-60) que resuelve el caso donde el registrador puede ser líder o coordinador directamente.

Purpose: Dar visibilidad de la cadena de mando (líder -> coordinador -> articulador) directamente en las tablas principales, sin tener que abrir cada registro individualmente.
Output: 3 archivos de tabla Filament modificados + 3 tests Pest nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Relaciones ya existentes en app/Models/User.php — no requieren cambios -->

```php
// User.php
public function coordinator(): BelongsTo // belongsTo(User::class, 'coordinator_user_id')
public function areaCoordinator(): BelongsTo // belongsTo(User::class, 'area_coordinator_user_id')
```

```php
// Voter.php
public function registeredBy(): BelongsTo // belongsTo(User::class, 'registered_by')
```

<!-- Patrón de referencia ya en producción: app/Filament/Widgets/ApoyosLideresCoordinadoresTable.php -->
```php
->with(['registeredBy.coordinator', 'municipality', 'gremio', 'subcategoria'])
// ...
TextColumn::make('coordinador')
    ->label('Coordinador')
    ->state(function (Voter $record): string {
        $registrador = $record->registeredBy;

        if ($registrador?->hasRole(UserRole::COORDINATOR->value)) {
            return $registrador->name;
        }

        return $registrador?->coordinator?->name ?? 'N/A';
    }),
```

<!-- Término UI confirmado: el modelo usa "areaCoordinator" pero la etiqueta en toda la app es "Articulador" -->
<!-- Ver app/Enums/UserRole.php AREA_COORDINATOR->getLabel() = 'Articulador', y AreaCoordinatorResource -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar columnas Coordinador y Articulador a VotersTable</name>
  <files>app/Filament/Resources/Voters/Tables/VotersTable.php, tests/Feature/Filament/VoterResourceHierarchyColumnsTest.php</files>
  <action>
En `app/Filament/Resources/Voters/Tables/VotersTable.php`:

1. Agregar `->modifyQueryUsing(fn (Builder $query) => $query->with('registeredBy.coordinator.areaCoordinator'))` al inicio de la cadena del `Table` (antes de `->columns([...])`) para evitar N+1. `Builder` ya está importado.

2. Importar `App\Enums\UserRole` (ya está importado) — se necesita para el chequeo `hasRole()`.

3. Inmediatamente después de la columna `registeredBy.name` ("Registrado por", línea ~128-131), agregar dos columnas nuevas siguiendo EXACTAMENTE el patrón de `ApoyosLideresCoordinadoresTable::table()` (líneas 50-60), extendido un nivel más para el Articulador:

```php
TextColumn::make('coordinador')
    ->label('Coordinador')
    ->toggleable()
    ->state(function (Voter $record): string {
        $registrador = $record->registeredBy;

        if ($registrador?->hasRole(UserRole::COORDINATOR->value)) {
            return $registrador->name;
        }

        return $registrador?->coordinator?->name ?? 'N/A';
    }),

TextColumn::make('articulador')
    ->label('Articulador')
    ->toggleable()
    ->state(function (Voter $record): string {
        $registrador = $record->registeredBy;

        if ($registrador?->hasRole(UserRole::COORDINATOR->value)) {
            return $registrador->areaCoordinator?->name ?? 'N/A';
        }

        return $registrador?->coordinator?->areaCoordinator?->name ?? 'N/A';
    }),
```

No usar `->searchable()` ni `->sortable()` en estas dos columnas (son columnas derivadas vía `->state()`, igual que `coordinador` en `ApoyosLideresCoordinadoresTable`, que tampoco las usa).

Crear `tests/Feature/Filament/VoterResourceHierarchyColumnsTest.php` (mismo estilo que `VoterResourceCampaignColumnTogglingTest.php`: roles seed, `actingAs` super_admin, `Session::put('campaign_context.mode', 'all')`). Casos:
- Un apoyo registrado por un líder muestra el nombre del coordinador del líder en la columna `coordinador`, y el nombre del articulador del coordinador en `articulador`.
- Un apoyo registrado directamente por un coordinador (sin líder intermedio) muestra el propio nombre del coordinador en `coordinador`, y el nombre de su articulador en `articulador`.
- Un apoyo cuyo líder/coordinador no tiene articulador asignado muestra `'N/A'` en `articulador`.

Usar `Livewire::test(ListVoters::class)->assertTableColumnStateSet('coordinador', $expectedName, record: $voter)` (o el método equivalente de aserción de estado de columna Filament v4 disponible) para validar los valores renderizados, no solo la existencia de la columna.
  </action>
  <verify>
    <automated>php artisan test --filter=VoterResourceHierarchyColumnsTest</automated>
  </verify>
  <done>Las columnas Coordinador y Articulador existen en VotersTable, resuelven correctamente el caso líder-vía-coordinador y coordinador-directo, eager-loading evita N+1, y los 3 casos de test pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar columna Articulador a LeadersTable y CoordinatorsTable</name>
  <files>app/Filament/Resources/Leaders/Tables/LeadersTable.php, app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php, tests/Feature/Filament/LeaderResourceArticuladorColumnTest.php, tests/Feature/Filament/CoordinatorResourceArticuladorColumnTest.php</files>
  <action>
En `app/Filament/Resources/Leaders/Tables/LeadersTable.php`:

1. Modificar el `->modifyQueryUsing(...)` existente (línea 22) para encadenar el eager load, sin romper la llamada a `MetadataAssignmentService`:

```php
->modifyQueryUsing(fn (Builder $query) => app(MetadataAssignmentService::class)
    ->withCurrentValueSelects($query)
    ->with('coordinator.areaCoordinator'))
```

2. Inmediatamente después de la columna `coordinator.name` ("Coordinador", líneas 38-43), agregar:

```php
TextColumn::make('coordinator.areaCoordinator.name')
    ->label('Articulador')
    ->searchable()
    ->sortable()
    ->placeholder('—')
    ->toggleable(),
```

(Misma convención que la columna vecina `coordinator.name` — dot notation directa, sin `->state()` closure, porque `registeredBy` aquí siempre es un líder con relación `coordinator` fija, a diferencia del caso polimórfico de Voters.)

En `app/Filament/Resources/Coordinators/Tables/CoordinatorsTable.php`:

1. Modificar el `->modifyQueryUsing(...)` existente (línea 23) igual que arriba:

```php
->modifyQueryUsing(fn (Builder $query) => app(MetadataAssignmentService::class)
    ->withCurrentValueSelects($query)
    ->with('areaCoordinator'))
```

2. Inmediatamente después de la columna `email` (líneas 31-37) y antes de `municipality.name`, agregar:

```php
TextColumn::make('areaCoordinator.name')
    ->label('Articulador')
    ->searchable()
    ->sortable()
    ->placeholder('—')
    ->toggleable(),
```

Crear `tests/Feature/Filament/LeaderResourceArticuladorColumnTest.php` (mismo estilo que `LeaderResourceColumnTogglingTest.php`): un líder con coordinador asignado, cuyo coordinador tiene un articulador (`area_coordinator_user_id`) asignado, muestra el nombre del articulador en la columna `coordinator.areaCoordinator.name`; un líder cuyo coordinador NO tiene articulador muestra `'—'`.

Crear `tests/Feature/Filament/CoordinatorResourceArticuladorColumnTest.php` (mismo estilo): un coordinador con `area_coordinator_user_id` asignado muestra el nombre del articulador en la columna `areaCoordinator.name`; un coordinador sin articulador muestra `'—'`.
  </action>
  <verify>
    <automated>php artisan test --filter="LeaderResourceArticuladorColumnTest|CoordinatorResourceArticuladorColumnTest"</automated>
  </verify>
  <done>LeadersTable y CoordinatorsTable muestran la columna Articulador con el valor correcto (o '—' si no hay asignación), ambos tests pasan.</done>
</task>

</tasks>

<verification>
Ejecutar el conjunto completo de los 3 nuevos archivos de test más una pasada de regresión sobre los resources tocados:

```bash
php artisan test --filter="VoterResourceHierarchyColumnsTest|LeaderResourceArticuladorColumnTest|CoordinatorResourceArticuladorColumnTest|VoterResourceCampaignColumnTogglingTest|LeaderResourceColumnTogglingTest"
```

Ejecutar `vendor/bin/pint --dirty` antes de finalizar.
</verification>

<success_criteria>
- VotersTable muestra columnas "Coordinador" y "Articulador" que resuelven correctamente tanto el caso líder-registra-apoyo como coordinador-registra-apoyo-directamente.
- LeadersTable muestra columna "Articulador" (coordinador del líder -> su articulador).
- CoordinatorsTable muestra columna "Articulador" (articulador directo).
- Ningún N+1 nuevo introducido (eager load explícito en las 3 tablas).
- Los 3 tests nuevos pasan, y no hay regresiones en los tests de toggling existentes.
</success_criteria>

<output>
After completion, create `.planning/quick/260830-il4-agregar-columnas-de-coordinador-y-articu/260830-il4-SUMMARY.md`
</output>
</output>

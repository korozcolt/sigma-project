---
phase: quick-260814-jb8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Filament/Widgets/DuplicatesReportTable.php
  - app/Filament/Widgets/RejectionsCountersOverview.php
  - app/Providers/Filament/AdminPanelProvider.php
  - tests/Feature/Filament/RejectionsCountersOverviewTest.php
  - tests/Feature/Filament/AdminPanelProviderTest.php
autonomous: true
requirements: [JB8-DUP-DEFINITION, JB8-DASHBOARD-WIDGETS]
must_haves:
  truths:
    - "The RejectionsCountersOverview widget's 'Duplicados' stat counts cédulas that are actively in dispute (unresolved document_number groups spanning campaigns), the same definition DuplicatesReportTable already uses — not the legacy VoterStatus::DUPLICATE status count"
    - "Visiting /admin (Escritorio) shows the Contadores de Rechazos widget (Rechazados, Duplicados, Fuera de Jurisdicción) and the Resumen de Jurisdicción widget, in addition to their existing presence on /reports"
    - "The /reports panel's registration of both widgets is untouched — this is an addition to /admin, not a move"
    - "The 'Duplicados' stat has no dead or cross-panel-permission-breaking link — its ->url() is removed with the reasoning documented in code, since a document_number-group criterion has no single VoterResource status filter to point to"
  artifacts:
    - path: "app/Filament/Widgets/DuplicatesReportTable.php"
      provides: "public static disputedDocumentNumbers(?Campaign $activeCampaign): Collection — the reusable disputed-duplicate query, extracted from table(), unchanged SQL"
      contains: "public static function disputedDocumentNumbers"
    - path: "app/Filament/Widgets/RejectionsCountersOverview.php"
      provides: "Duplicados stat computed via DuplicatesReportTable::disputedDocumentNumbers(), no reimplemented query"
      contains: "DuplicatesReportTable::disputedDocumentNumbers"
    - path: "app/Providers/Filament/AdminPanelProvider.php"
      provides: "RejectionsCountersOverview and JurisdictionSummaryOverview registered in ->widgets()"
      contains: "RejectionsCountersOverview::class"
  key_links:
    - from: "app/Filament/Widgets/RejectionsCountersOverview.php"
      to: "app/Filament/Widgets/DuplicatesReportTable.php"
      via: "DuplicatesReportTable::disputedDocumentNumbers($activeCampaign) static call, reused not reimplemented"
      pattern: "DuplicatesReportTable::disputedDocumentNumbers"
    - from: "app/Providers/Filament/AdminPanelProvider.php"
      to: "app/Filament/Widgets/RejectionsCountersOverview.php + JurisdictionSummaryOverview.php"
      via: "->widgets([...]) array registration"
      pattern: "RejectionsCountersOverview::class,\\s*\\n.*RejectionsReportTable::class"
---

<objective>
Add the existing RejectionsCountersOverview and JurisdictionSummaryOverview stat widgets to the Admin panel (/admin, the "Escritorio") alongside their current home on /reports, and fix RejectionsCountersOverview's "Duplicados" stat to use the same "duplicados activos en disputa" definition DuplicatesReportTable already uses, instead of the legacy resolved VoterStatus::DUPLICATE status count.

Purpose: The user asked for count widgets (rechazados, duplicados, jurisdicción) while looking at /admin. Both widgets already exist but only render on /reports. Separately, this session's DuplicatesReportTable work (commits c04861e/0b1a298/8138c13) surfaced that RejectionsCountersOverview's "Duplicados" count uses a different, narrower definition (resolved status=duplicate) than the disputed-groups definition the report table uses — the user confirmed the disputed-groups definition is the correct one for this stat too.

Output: DuplicatesReportTable exposes its disputed-document-number query as a reusable static method; RejectionsCountersOverview reuses it for "Duplicados" and drops the now-inapplicable ->url(); both widgets are registered in AdminPanelProvider (ReportsPanelProvider registration untouched); Pest coverage for both changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The widget whose query becomes the shared source of truth for "duplicados en disputa".
# Class docblock explains the D-06 cross-campaign exception — do not alter the query semantics.
@app/Filament/Widgets/DuplicatesReportTable.php

# The widget being fixed — currently counts VoterStatus::DUPLICATE (resolved), must switch
# to the disputed-groups definition and drop its now-inapplicable ->url().
@app/Filament/Widgets/RejectionsCountersOverview.php

# The other widget being added to /admin (no code changes needed to this file itself).
@app/Filament/Widgets/JurisdictionSummaryOverview.php

# Where both widgets already live — copy the relative ordering
# (RejectionsCountersOverview -> RejectionsReportTable -> DuplicatesReportTable ->
# JurisdictionSummaryOverview -> JurisdictionReportTable) into AdminPanelProvider.
@app/Providers/Filament/ReportsPanelProvider.php

# The panel being modified — add the two widgets here, do not touch anything else.
@app/Providers/Filament/AdminPanelProvider.php

# Existing test to update for the new Duplicados definition.
@tests/Feature/Filament/RejectionsCountersOverviewTest.php

# Existing widget-registration test pattern to extend (Filament::getPanel('admin')).
@tests/Feature/Filament/AdminPanelProviderTest.php

<interfaces>
<!-- Verified facts the executor should rely on directly — no re-exploration needed. -->

DuplicatesReportTable::table() currently computes (inline, to be extracted verbatim):

  $documentNumbers = $activeCampaign
      ? Voter::withoutGlobalScopes()
          ->withTrashed()
          ->select('document_number')
          ->groupBy('document_number')
          ->havingRaw('COUNT(*) > 1')
          ->havingRaw('SUM(CASE WHEN campaign_id = ? THEN 1 ELSE 0 END) > 0', [$activeCampaign->id])
          ->pluck('document_number')
      : collect();

This is a distinct-document_number list: groups with more than one Voter row
system-wide (withoutGlobalScopes + withTrashed) where at least one row belongs to
the given campaign. That is the "duplicados activos en disputa" definition.

RejectionsCountersOverview::getStats() currently computes (to be replaced):

  $duplicados = Voter::query()
      ->where('campaign_id', $activeCampaign->id)
      ->where('status', VoterStatus::DUPLICATE->value)
      ->count();

CampaignContext::currentCampaign(?User $user = null): ?Campaign — already called at
the top of both widgets' relevant methods, stored in $activeCampaign. Reuse it, do
not call it a second time.

DuplicatesReportTable and RejectionsCountersOverview are both in namespace
App\Filament\Widgets — no `use` import needed for the cross-reference, they are
in the same namespace already.

Filament\Panel (via HasComponents trait) exposes getWidgets(): array — the same
pattern tests/Feature/Filament/AdminPanelProviderTest.php already uses for
Filament::getPanel('admin')->getNavigationGroups() applies to ->getWidgets().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared disputed-duplicates query; fix RejectionsCountersOverview's Duplicados definition</name>
  <files>app/Filament/Widgets/DuplicatesReportTable.php, app/Filament/Widgets/RejectionsCountersOverview.php, tests/Feature/Filament/RejectionsCountersOverviewTest.php</files>
  <behavior>
    - A Voter row with a document_number NOT shared by any other row (even if manually
      marked VoterStatus::DUPLICATE, e.g. legacy data) does NOT count toward "Duplicados" —
      proves the old resolved-status definition is gone.
    - A document_number shared by exactly one row in the active campaign and one row in a
      DIFFERENT campaign counts as exactly 1 in "Duplicados" for the active campaign (one
      disputed group, not two rows, not zero) — proves the group-based, cross-campaign
      definition from DuplicatesReportTable is reused correctly.
    - A document_number shared by two rows that are BOTH in a different (non-active) campaign
      does NOT count toward the active campaign's "Duplicados" — proves campaign-touch
      isolation still holds (unchanged from DuplicatesReportTable's existing guarantee).
    - The "Duplicados" Stat has no ->url() (getUrl() returns null) — the group/cross-campaign
      criterion has no single VoterResource status filter or safely reachable page to link to.
  </behavior>
  <action>
    Step 1 — extract the reusable query in app/Filament/Widgets/DuplicatesReportTable.php:
    Add `use App\Models\Campaign;` and `use Illuminate\Support\Collection;` to the existing
    alphabetical `use` block. Add a new public static method (place it above `table()`):

      /**
       * Document numbers with more than one Voter row system-wide (withoutGlobalScopes,
       * withTrashed) where at least one of those rows belongs to the given campaign — i.e.
       * cédulas actively in dispute that this campaign is party to. Shared with
       * RejectionsCountersOverview's "Duplicados" stat so both surfaces use the exact same
       * definition of "duplicate" (see the class docblock above for the D-06 cross-campaign
       * rationale — this method does not change that query's semantics).
       *
       * @return Collection<int, string>
       */
      public static function disputedDocumentNumbers(?Campaign $activeCampaign): Collection
      {
          if (! $activeCampaign) {
              return collect();
          }

          return Voter::withoutGlobalScopes()
              ->withTrashed()
              ->select('document_number')
              ->groupBy('document_number')
              ->havingRaw('COUNT(*) > 1')
              ->havingRaw('SUM(CASE WHEN campaign_id = ? THEN 1 ELSE 0 END) > 0', [$activeCampaign->id])
              ->pluck('document_number');
      }

    Then replace the inline `$documentNumbers = $activeCampaign ? ... : collect();` block
    inside `table()` with:

      $documentNumbers = self::disputedDocumentNumbers($activeCampaign);

    Do not change ->query(), ->columns(), ->headerActions(), ->recordUrl(), or the
    class-level docblock — only the extraction described above.

    Step 2 — fix app/Filament/Widgets/RejectionsCountersOverview.php:
    Replace the `$duplicados = Voter::query()->where(...)->where('status', ...)->count();`
    block with:

      $duplicados = DuplicatesReportTable::disputedDocumentNumbers($activeCampaign)->count();

    (No new `use` import needed — same namespace App\Filament\Widgets.)

    Update the "Duplicados" Stat entry: change the description to
    'Cédulas duplicadas activas en disputa (incluye otras campañas)' and remove the
    entire ->url(VoterResource::getUrl('index', [...])) call, replacing it with a comment
    explaining why:

      Stat::make('Duplicados', number_format($duplicados))
          ->description('Cédulas duplicadas activas en disputa (incluye otras campañas)')
          ->descriptionIcon('heroicon-m-document-duplicate')
          ->color('warning'),
          // No ->url(): "duplicados en disputa" is a cross-campaign document_number-group
          // criterion (see DuplicatesReportTable::disputedDocumentNumbers()), not a single
          // Voter::status value VoterResource's table filter can express. The one place that
          // DOES render this data (DuplicatesReportTable, on /reports) requires the
          // REPORTS_VIEWER role, which not every viewer of this widget on /admin will have —
          // linking there could 403. Dropping the link is safer than a broken or wrong one.

    Confirm the VoterStatus and Voter imports in this file are both still used elsewhere
    (Rechazados and Fuera de Jurisdicción stats) — do not remove them.

    Step 3 — update tests/Feature/Filament/RejectionsCountersOverviewTest.php:
    a) In 'rejections counters overview reports correct non-overlapping counts for the
       active campaign': replace the single
       `Voter::factory()->create(['campaign_id' => $campaign->id, 'status' => VoterStatus::DUPLICATE])`
       with a genuine duplicate pair sharing one document_number under the active campaign
       (mirrors the DuplicatesReportTableTest fixture pattern):

         Voter::factory()->create([
             'campaign_id' => $campaign->id,
             'document_number' => '5551234567',
             'duplicate_sequence' => 0,
         ]);
         Voter::factory()->create([
             'campaign_id' => $campaign->id,
             'document_number' => '5551234567',
             'duplicate_sequence' => 1,
             'status' => VoterStatus::DUPLICATE,
         ]);

       This keeps `$stats[1]->getValue()` expected at `number_format(1)` (one group) — the
       existing assertions do not need to change in value, only the fixture creating them.

    b) In 'rejections counters overview never leaks another campaign\'s voters into the
       active campaign\'s counts': replace the single
       `Voter::factory()->create(['campaign_id' => $otherCampaign->id, 'status' => VoterStatus::DUPLICATE])`
       with a genuine duplicate PAIR entirely inside $otherCampaign (both rows sharing a
       document_number, both under $otherCampaign, created while the session is still
       pointed at $otherCampaign) — proves a real disputed group that never touches the
       active campaign still correctly contributes 0. Keep the expected value at
       `number_format(0)`.

    c) Add a new test: 'rejections counters overview duplicados stat counts disputed
       document_number groups, not the legacy DUPLICATE status alone' — create (i) a lone
       Voter under the active campaign with `status: VoterStatus::DUPLICATE` but a UNIQUE
       document_number (no sibling anywhere), and (ii) a genuine cross-campaign duplicate
       pair (one row under the active campaign, one row — created via the session-switch
       pattern from DuplicatesReportTableTest — under a second campaign, same
       document_number). Assert `$stats[1]->getValue()` equals `number_format(1)` (only the
       genuine group counts; the lone legacy-status voter does not).

    d) Add a new test: 'rejections counters overview duplicados stat has no url' — assert
       `$stats[1]->getUrl()` is null (using the same active-campaign setup as the first
       test).
  </action>
  <verify>
    <automated>php artisan test --filter=RejectionsCountersOverviewTest</automated>
  </verify>
  <done>DuplicatesReportTable exposes disputedDocumentNumbers() as the single source of truth for the disputed-duplicate query; RejectionsCountersOverview's Duplicados stat reuses it and has no ->url(); all Pest cases in RejectionsCountersOverviewTest.php pass, including the two new ones covering the legacy-status-vs-group distinction and the removed url.</done>
</task>

<task type="auto">
  <name>Task 2: Register both widgets on the Admin panel (Escritorio) + coverage</name>
  <files>app/Providers/Filament/AdminPanelProvider.php, tests/Feature/Filament/AdminPanelProviderTest.php</files>
  <action>
    In app/Providers/Filament/AdminPanelProvider.php, add two imports to the existing
    alphabetical `use` block:

      use App\Filament\Widgets\JurisdictionSummaryOverview;
      use App\Filament\Widgets\RejectionsCountersOverview;

    (JurisdictionSummaryOverview sorts alphabetically right after JurisdictionReportTable's
    import line would go; RejectionsCountersOverview sorts right before RejectionsReportTable —
    both already exist as imports in this file, insert the new ones adjacent and keep the
    block alphabetical.)

    In the ->widgets([...]) array, insert the two new entries to match the relative
    ordering already used in ReportsPanelProvider (RejectionsCountersOverview ->
    RejectionsReportTable -> DuplicatesReportTable -> JurisdictionSummaryOverview ->
    JurisdictionReportTable). The array currently reads (in relevant part):

      TopPollingPlacesTable::class,
      RejectionsReportTable::class,
      DuplicatesReportTable::class,
      JurisdictionReportTable::class,
      ApoyosLideresCoordinadoresTable::class,

    Change it to:

      TopPollingPlacesTable::class,
      RejectionsCountersOverview::class,
      RejectionsReportTable::class,
      DuplicatesReportTable::class,
      JurisdictionSummaryOverview::class,
      JurisdictionReportTable::class,
      ApoyosLideresCoordinadoresTable::class,

    Do not reorder, remove, or otherwise touch any other widget in this array, and do not
    touch app/Providers/Filament/ReportsPanelProvider.php at all — its existing registration
    of both widgets stays exactly as-is (this is an addition to /admin, not a move).

    In tests/Feature/Filament/AdminPanelProviderTest.php, add a new test asserting both
    widgets are registered on the admin panel, following the file's existing
    `Filament::getPanel('admin')` pattern:

      test('admin panel registers the rejections counters and jurisdiction summary widgets', function () {
          $widgets = Filament::getPanel('admin')->getWidgets();

          expect($widgets)->toContain(RejectionsCountersOverview::class)
              ->and($widgets)->toContain(JurisdictionSummaryOverview::class);
      });

    Add the two corresponding `use` imports at the top of the test file
    (`use App\Filament\Widgets\JurisdictionSummaryOverview;` and
    `use App\Filament\Widgets\RejectionsCountersOverview;`), alphabetically ordered
    alongside the existing `use Filament\Facades\Filament;` import.

    Finally, run `vendor/bin/pint --dirty` to format every file touched across both tasks
    in this plan (PSR-12, alphabetical use imports, curly braces per CLAUDE.md).
  </action>
  <verify>
    <automated>php artisan test --filter=AdminPanelProviderTest && php artisan test --group=dashboard-widgets</automated>
  </verify>
  <done>AdminPanelProvider registers RejectionsCountersOverview and JurisdictionSummaryOverview alongside all existing /admin widgets; ReportsPanelProvider is untouched; the new AdminPanelProviderTest case passes; the full dashboard-widgets test group (including RejectionsCountersOverviewTest, JurisdictionSummaryOverviewTest, DuplicatesReportTableTest, DashboardWidgetsTest, WidgetDrillThroughTest) is green; Pint clean.</done>
</task>

</tasks>

<verification>
- `php artisan test --filter=RejectionsCountersOverviewTest` — Duplicados stat reflects the disputed-groups definition, matches DuplicatesReportTable's own criterion, has no dead url.
- `php artisan test --filter=AdminPanelProviderTest` — both widgets confirmed registered on the admin panel.
- `php artisan test --group=dashboard-widgets` — full regression across every widget test file touched by this session (no cross-widget breakage).
- `vendor/bin/pint --dirty` clean.
- Manual browser check (per user's browser-verify-before-prod rule, before any deploy): visit /admin as a super admin with an active campaign that has at least one genuinely disputed cédula, confirm "Contadores de Rechazos" and "Resumen de Jurisdicción" render with correct values, and that the Duplicados stat is not a clickable link.
</verification>

<success_criteria>
- RejectionsCountersOverview's "Duplicados" stat counts disputed document_number groups (DuplicatesReportTable's definition), not the legacy resolved VoterStatus::DUPLICATE status.
- DuplicatesReportTable's query logic is reused via a shared static method, not reimplemented.
- /admin (Escritorio) renders both RejectionsCountersOverview and JurisdictionSummaryOverview; /reports registration is unchanged.
- Duplicados stat has no ->url(), with the reasoning documented in code.
- Full dashboard-widgets Pest group green; Pint clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260814-jb8-agregar-widgets-de-conteo-rechazados-dup/260814-jb8-SUMMARY.md`.
</output>

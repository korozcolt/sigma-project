---
phase: 09-live-source-feasibility-spike
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md
autonomous: true
requirements: [LIVE-02]
must_haves:
  truths:
    - "At least one real cédula is submitted end-to-end against the live wsp.registraduria.gov.co endpoint via a real solved 2captcha token, driven through app.py's rewritten flow"
    - "Every attempt's outcome is classified into success/denied_by_score/not_found/session_expired/source_unreachable — a solved token is never treated as success on its own"
    - "The spike stays within the ~20-30 solve budget (D-04) and stops at budget exhaustion or a hard blocker (e.g. insufficient 2captcha balance), whichever comes first (D-07)"
    - "A documented go/no-go recommendation for adopting wsp as a live-source adapter exists regardless of outcome (D-05, D-06)"
  artifacts:
    - path: .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md
      provides: "Extracted sitekey/#token samples, a full per-attempt outcome-taxonomy table, and the final go/no-go recommendation with rationale"
  key_links:
    - from: "09-SPIKE-RESULTS.md's attempt table Outcome column"
      to: "registraduria-service/app.py's outcome classifier (Plan 09-01)"
      via: "each logged row's outcome value is one of the five states app.py emits"
      pattern: "success|denied_by_score|not_found|session_expired|source_unreachable"
---

<objective>
Spend the ~20-30 real 2captcha budget (D-04) against `registraduria-service/app.py`'s wsp-targeting flow (Plan 09-01), attempting the three known cédulas, applying the score-escalation strategy from `09-RESEARCH.md` where warranted, and documenting every attempt's classified outcome. Produce the dedicated `SPIKE-RESULTS.md` with a go/no-go recommendation, per D-06/D-07.

Purpose: This is the empirical core of LIVE-02 — the one question that cannot be answered without spending real money against the real endpoint. The milestone's deterministic core (snapshot + provenance + reconciliation) does not depend on this outcome, but Phase 11 needs the documented recommendation to scope how much automated reconciliation is realistically achievable via the live path.
Output: `.planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md` with the full attempt log, taxonomy breakdown, and go/no-go verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-live-source-feasibility-spike/09-CONTEXT.md
@.planning/phases/09-live-source-feasibility-spike/09-RESEARCH.md
@.planning/phases/09-live-source-feasibility-spike/09-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact HTTP contract this plan's executor calls. Produced by Plan 09-01, Task 1. -->

`POST http://localhost:5757/lookup`
Request body: {"cedula": "1102812122", "enterprise": false}
Response, 200: {"session_id": "<uuid>"}

`GET http://localhost:5757/result/<session_id>`
Response fields: status (pending/solving_captcha/waiting_result/done/error), outcome (null until status is done/error, then one of success/denied_by_score/not_found/session_expired/source_unreachable), data, error, sitekey, message, raw_response.

Poll `GET /result/<session_id>` every ~10s until `status` is `done` or `error` (cap at ~180s per attempt — 150s is the 2captcha poll ceiling plus margin for the page interaction). `outcome` is only meaningful once `status` reaches `done` or `error`.

The three locked test cédulas (D-03, no need to ask): `1102812122`, `1102815878`, `64552231`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Execute the live spike against the three cédulas within the ~20-30 attempt budget</name>
  <read_first>
    registraduria-service/app.py
    .planning/phases/09-live-source-feasibility-spike/09-CONTEXT.md
    .planning/phases/09-live-source-feasibility-spike/09-RESEARCH.md
  </read_first>
  <files>.planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md</files>
  <action>
1. Confirm the Flask service from Plan 09-01 is still running on port 5757 (check with lsof -i :5757). If not, restart it: cd registraduria-service, then nohup ./venv/bin/python3 app.py >> service.log 2>&1 &, then disown.

2. Create .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md with a header and this attempt-log table (append a row after every attempt completes, in real time — do not batch them at the end):

   Title: "Phase 9: Live-Source Feasibility Spike -- Results"
   Section heading: "## Attempt Log"
   Table columns: Number | Cedula | Enterprise | Outcome | Message/Detail | Timestamp

3. Round 1, baseline (enterprise=false): for each of the 3 cedulas (1102812122, 1102815878, 64552231), run 2 attempts each (6 total, cycling through the cedulas). For each attempt: POST to /lookup with the cedula and enterprise=false, capture the returned session_id, then poll GET /result/<session_id> every ~10s until status is done or error (cap 180s). Append a row to the table immediately after each attempt completes: attempt number, cedula, enterprise flag used, the outcome value verbatim, the message/error string (truncate to ~200 chars if long), and a timestamp.

4. Escalation check (per 09-RESEARCH.md's corrected finding -- no numeric score parameter exists; enterprise=1 is the only escalation lever): after the 6 baseline attempts, compute the denied_by_score rate. If 4 or more of the 6 (over 50%) are denied_by_score, switch to enterprise=true for all subsequent attempts. Otherwise continue with enterprise=false to firm up the signal.

5. Round 2 and beyond: continue attempts across the 3 cedulas (cycling), using whichever enterprise value step 4 selected, up to a hard ceiling of 30 total attempts (D-04). Stop earlier, at a minimum of 20 attempts, once a clear signal has emerged (either consistent success, or consistent non-recoverable denial across all 3 cedulas after the escalation attempt).

6. Hard-blocker handling: if any /result poll ever surfaces outcome=source_unreachable whose error string indicates a 2captcha account or balance problem (for example containing ZERO_BALANCE, WRONG_USER_KEY, or the 2captcha submit's raw status=0 payload), stop immediately -- do not keep spending. Append a row with Outcome=source_unreachable and a Message/Detail beginning with the literal text "HARD BLOCKER:" describing what was found, then proceed directly to Task 2 with whatever data has been gathered so far.

7. Never exceed 30 real /lookup calls in total across this task (D-04's ceiling) -- count every call made, including any that errored before reaching 2captcha.
  </action>
  <acceptance_criteria>
    - .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md exists and contains an "Attempt Log" heading
    - The attempt table contains at least 6 data rows (the baseline round across the 3 cedulas x 2), UNLESS a hard blocker (D-07) stopped the spike early per step 6 of the action -- in that case the 6-row minimum is waived and at least 1 row (ending in a "HARD BLOCKER:" Message/Detail) is sufficient
    - The attempt table contains no more than 30 data rows total
    - Every row's Outcome column contains exactly one of: success, denied_by_score, not_found, session_expired, source_unreachable -- no other free-text value appears in that column
    - If a hard blocker was hit, at least one row's Message/Detail begins with "HARD BLOCKER:" and the total row count is below 30 (early stop, not budget exhaustion)
    - All 3 cedulas (1102812122, 1102815878, 64552231) appear at least twice each in the Cedula column, unless a hard blocker stopped the spike before all three could be cycled through twice (in which case the file must say so explicitly)
  </acceptance_criteria>
  <verify>
    <automated>F=".planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md"; test -f "$F" && grep -q "Attempt Log" "$F" && ROWS=$(grep -c "^| [0-9]" "$F"); (grep -q "HARD BLOCKER:" "$F" && test "$ROWS" -ge 1 || test "$ROWS" -ge 6) && test "$ROWS" -le 30 && echo SPIKE_LOG_OK</automated>
  </verify>
  <done>All planned attempts (6 baseline plus escalation/continuation up to the 30-attempt ceiling, or an earlier stop on a hard blocker) were executed against the live wsp endpoint through app.py's rewritten flow, and every attempt is logged in 09-SPIKE-RESULTS.md's Attempt Log with an explicit taxonomy outcome -- never treating a solved token alone as success.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize SPIKE-RESULTS.md with the extracted values, outcome summary, and go/no-go recommendation</name>
  <read_first>
    .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md
    .planning/phases/09-live-source-feasibility-spike/09-RESEARCH.md
  </read_first>
  <files>.planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md</files>
  <action>
Append the following sections to .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md, after the Attempt Log table produced by Task 1:

1. Section "## Extracted Values" -- list the sitekey observed during the spike (from app.py's sitekey field in at least one /result response) and at least 2 distinct #token anti-replay nonce samples observed across different attempts (confirms the per-load rotation finding from 09-RESEARCH.md). If the raw nonce values were not captured verbatim in Task 1's log, re-extract two via the same zero-cost DOM check used in Plan 09-01 Task 2 (no budget cost) and note they were captured post-hoc for documentation purposes.

2. Section "## Outcome Summary" -- a count of attempts per taxonomy bucket (success, denied_by_score, not_found, session_expired, source_unreachable), the overall success rate, the denied_by_score rate, and whether the enterprise=1 escalation was triggered and, if so, whether it changed the outcome distribution.

3. Section "## Go/No-Go Recommendation" -- an explicit verdict, written as one of these exact literal strings on its own line: "Verdict: GO", "Verdict: NO-GO", or "Verdict: NO-GO FOR NOW" (per D-07, a mixed or inconclusive result is documented as NO-GO FOR NOW, not a reason to keep spending budget). Follow the verdict with a rationale paragraph referencing the actual observed data from the Attempt Log and Outcome Summary above (for example: "X/Y attempts succeeded", "hit a HARD BLOCKER: insufficient 2captcha balance after N attempts", "consistently denied_by_score even after the enterprise=1 escalation across all 3 cedulas").

4. Section "## Scope Note" -- state explicitly that per D-05, no production wiring (RegistraduriaService.php, LiveSourceAdapter, REGISTRADURIA_LIVE_ENABLED, or any config change) was performed in this phase regardless of the verdict above, and that Phase 11 should consult this document's Go/No-Go Recommendation to scope how much automated reconciliation is realistically achievable via the live path.

Do not, under any circumstance, add code changes or instructions in this file that modify app/Services/RegistraduriaService.php, app/Services/LiveSourceAdapter.php, or flip REGISTRADURIA_LIVE_ENABLED -- that remains out of scope for Phase 9 per D-05. Mentioning those names only in the Scope Note's "this was NOT touched" statement is fine and expected.
  </action>
  <acceptance_criteria>
    - .planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md contains all four headings: "## Extracted Values", "## Outcome Summary", "## Go/No-Go Recommendation", "## Scope Note"
    - The "## Go/No-Go Recommendation" section contains one of the literal strings "Verdict: GO", "Verdict: NO-GO", or "Verdict: NO-GO FOR NOW"
    - The "## Extracted Values" section contains at least 2 distinct token-shaped strings (each 10+ characters) representing separate anti-replay nonce observations
    - The "## Scope Note" section contains the string "Phase 11"
    - The file's Scope Note explicitly states RegistraduriaService.php/LiveSourceAdapter/REGISTRADURIA_LIVE_ENABLED were NOT modified — no instructions anywhere in the file describe how to modify them
  </acceptance_criteria>
  <verify>
    <automated>F=".planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md"; grep -q "Extracted Values" "$F" && grep -q "Outcome Summary" "$F" && grep -q "Go/No-Go Recommendation" "$F" && grep -q "Scope Note" "$F" && grep -q "Verdict:" "$F" && grep -q "Phase 11" "$F" && echo RESULTS_DOC_OK</automated>
  </verify>
  <done>09-SPIKE-RESULTS.md is complete per D-06 -- extracted sitekey/token values, full outcome-taxonomy breakdown, and an explicit go/no-go verdict with rationale -- ready for Phase 11 to consult, with no production wiring performed per D-05.</done>
</task>

</tasks>

<verification>
Confirm the final document satisfies both tasks' automated checks:
```
F=".planning/phases/09-live-source-feasibility-spike/09-SPIKE-RESULTS.md"
grep -q "Attempt Log" "$F" && grep -q "Go/No-Go Recommendation" "$F" && grep -q "Verdict:" "$F"
```
</verification>

<success_criteria>
- The live spike attempted all 3 known cédulas within the ~20-30 solve budget (or stopped early on a documented hard blocker).
- Every attempt is classified into the explicit five-state taxonomy — no attempt's success is inferred merely from a solved captcha token.
- `09-SPIKE-RESULTS.md` documents extracted values, the outcome breakdown, and an explicit go/no-go verdict with rationale.
- No production wiring (RegistraduriaService.php, LiveSourceAdapter, REGISTRADURIA_LIVE_ENABLED) was touched, per D-05.
</success_criteria>

<output>
After completion, create `.planning/phases/09-live-source-feasibility-spike/09-02-SUMMARY.md`
</output>

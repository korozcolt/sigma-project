---
phase: 05.1-cross-phase-hardening-closure
plan: 08
subsystem: election-day-diad
tags: [laravel, filament, eloquent, sqlite, unique-constraint, migrations, pest]

# Dependency graph
requires:
  - phase: 5
    provides: DiaD.php page, VoteRecord model, DiaDStatsOverview widget, ElectionEvent model
provides:
  - DB-level unique constraint on vote_records(voter_id, election_event_id) with a safe pre-migration dedup guard
  - markVoted() QueryException(23000) race-condition handling that reverts voter status and notifies
  - markDidNotVote() conflict guard blocking the status flip when a VoteRecord already exists for the active event
  - Municipality::voters() HasMany relation
  - DiaDTerritorialProgressTable widget showing per-municipality total/voted/did-not-vote/% participation
affects: [phase-05.1-remaining-plans, dia-d-election-day-flows]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "QueryException SQLSTATE-code branch (23000) as the DB-level fallback guard behind an existing app-level uniqueness check"
    - "Migration-time dedup-then-constrain pattern: GROUP BY ... HAVING COUNT(*) > 1, delete all but MIN(id), then add unique index"
    - "Real-Eloquent-model + withCount() aggregate widget convention (matches TopPollingPlacesTable) for territory breakdown tables"

key-files:
  created:
    - database/migrations/2026_07_23_160000_add_unique_constraint_to_vote_records_table.php
    - app/Filament/Widgets/DiaDTerritorialProgressTable.php
    - tests/Feature/DiaDDuplicateVoteConstraintTest.php
    - tests/Feature/DiaDTerritorialProgressTest.php
  modified:
    - app/Filament/Pages/DiaD.php
    - app/Models/Municipality.php

key-decisions:
  - "Kept the existing app-level `$existingRecord` check in markVoted() as the fast-fail primary guard; the new try/catch around VoteRecord::create() is purely the DB-level race-condition fallback"
  - "markDidNotVote() only blocks when an active ElectionEvent exists and a VoteRecord for it exists — no active event means no possible conflict, so the guard is a no-op in that case"
  - "DiaDTerritorialProgressTable mirrors TopPollingPlacesTable's real-Eloquent-model + withCount() convention rather than a raw grouped query, so rows have stable `id`s"

requirements-completed: [DAYD-03, DAYD-04]

# Metrics
duration: 25min
completed: 2026-07-23
---

# Phase 05.1 Plan 08: Day D Duplicate-Vote DB Guard + Territorial Progress Summary

**DB-level unique constraint (with pre-migration dedup) closes the vote_records race condition, markDidNotVote() now blocks on conflicting evidence, and a new per-municipality Day D widget replaces campaign-only stats.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-07-23T14:30:00-05:00
- **Completed:** 2026-07-23T14:56:30-05:00
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- `vote_records` now has a real DB unique index on `(voter_id, election_event_id)`, with the new migration safely deduping any pre-existing duplicates (keeping the earliest row) before adding the constraint
- `markVoted()` catches the SQLSTATE 23000 race case, reverts the voter's status, and shows a warning notification instead of an uncaught 500
- `markDidNotVote()` now checks for an existing `VoteRecord` on the active event first and blocks the status flip with a danger notification if one exists
- New `DiaDTerritorialProgressTable` widget shows per-municipality total/voted/did-not-vote/% participation, registered alongside the existing campaign-level `DiaDStatsOverview`
- Full test suite green: 839 passed, 0 failed

## Task Commits

1. **Task 1: DB unique constraint + voted/did-not-vote conflict handling (DAYD-03)** - `c556749` (feat, tdd)
2. **Task 2: Per-municipality Day D participation breakdown (DAYD-04)** - `4f688a7` (feat, tdd)

**Plan metadata:** (this commit)

## Files Created/Modified
- `database/migrations/2026_07_23_160000_add_unique_constraint_to_vote_records_table.php` - Dedup guard + unique index on vote_records(voter_id, election_event_id)
- `app/Filament/Pages/DiaD.php` - markVoted() QueryException(23000) catch branch; markDidNotVote() pre-flight conflict check; registered DiaDTerritorialProgressTable widget
- `app/Models/Municipality.php` - Added voters(): HasMany relation
- `app/Filament/Widgets/DiaDTerritorialProgressTable.php` - New per-municipality Day D participation table widget
- `tests/Feature/DiaDDuplicateVoteConstraintTest.php` - 4 tests covering DB rejection, migration dedup, markDidNotVote conflict block, and the 23000 catch branch/notification structure
- `tests/Feature/DiaDTerritorialProgressTest.php` - 2 tests covering per-municipality counts and zero-voter municipality exclusion

## Decisions Made
- Kept the app-level duplicate check in `markVoted()` unchanged as the primary fast-fail guard; the DB constraint + try/catch is strictly the race-condition safety net for concurrent requests
- `markDidNotVote()`'s new guard is scoped to the active `ElectionEvent` only, matching how `markVoted()` already scopes its own duplicate check
- Widget query follows the codebase's existing `TopPollingPlacesTable` convention (real Eloquent model + `withCount()`) instead of a raw grouped-select, for stable row identity and easier future drill-through

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree was missing phase 05.1 planning artifacts entirely**
- **Found during:** Initial plan load
- **Issue:** This agent's isolated git worktree branched from a commit (`235461f`) that predated the 7 commits on `main` that created the entire `.planning/phases/05.1-cross-phase-hardening-closure/` directory (CONTEXT, RESEARCH, VALIDATION, and all 9 PLAN.md files). The plan file referenced in the task objective did not exist anywhere in this worktree or in any commit reachable from its branch.
- **Fix:** Fast-forward merged `main` into the worktree branch (`git merge main-ref-tmp`), which brought in the phase 05.1 planning docs plus a handful of unrelated already-completed main-branch commits (a finished quick task, an unrelated `HasRegistraduriaPolling`/`VoterForm` change). No conflicts; fast-forward only.
- **Files modified:** N/A (git history merge, no working-tree edits)
- **Verification:** `.planning/phases/05.1-cross-phase-hardening-closure/05.1-08-PLAN.md` present and readable after merge; `git log --oneline -8` confirms the merged commits
- **Committed in:** N/A (fast-forward merge, no new commit created)

**2. [Rule 3 - Blocking] Worktree had no vendor/, .env, or public/build — tests could not run at all**
- **Found during:** First `php artisan test` attempt (Task 1 verification)
- **Issue:** This parallel-executor worktree was never provisioned with `composer install` output, a `.env`, or built frontend assets, all of which are gitignored and must exist per-worktree. Running any artisan command failed on a missing `vendor/autoload.php`; once that was resolved, the full suite showed 27 unrelated failures, all `Vite manifest not found` errors from `public/build/manifest.json` missing.
- **Fix:** Copied `vendor/`, `.env`, and `public/build/` from the main repo checkout (all gitignored, non-code, environment-only artifacts) into this worktree rather than running a full `composer install`/`npm run build` for speed. No composer.json/package.json changes were made.
- **Files modified:** N/A (gitignored environment directories only: `vendor/`, `.env`, `public/build/`)
- **Verification:** `composer test` afterward reports 839 passed, 0 failed
- **Committed in:** N/A (all copied paths are gitignored, nothing to commit)

---

**Total deviations:** 2 auto-fixed (both Rule 3 - blocking environment/artifact issues, no code-behavior changes)
**Impact on plan:** Both fixes were purely about making the pre-existing plan and test environment available/runnable in this isolated worktree. No scope creep, no changes to plan-specified behavior.

## Issues Encountered
None beyond the two blocking/environment issues documented above under Deviations.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- DAYD-03 and DAYD-04 are closed; `vote_records` is now protected at the DB layer against concurrent duplicate votes, and `markDidNotVote()` can no longer silently conflict with existing vote evidence.
- Day D dashboards now surface per-municipality participation, giving coordinators/admins a drill-down beyond campaign-wide totals.
- Remaining Phase 05.1 plans (01-07, 09) are independent of this plan's changes (`depends_on: []`) and can proceed without additional coordination on these files.

---
*Phase: 05.1-cross-phase-hardening-closure*
*Completed: 2026-07-23*

## Self-Check: PASSED

All 6 created/modified files confirmed present on disk; both task commits (`c556749`, `4f688a7`) confirmed in git history.

---
phase: 04.1-reportes-avanzados-de-apoyos-ranking-lider-coordinador-puesto-votacion-informe-rechazos-informe-duplicados-export-csv-plano-informe-jurisdiccion-dentro-fuera
verified: 2026-07-23T15:26:21Z
status: passed
score: 6/6 must-haves verified (D-01 through D-06)
---

# Phase 04.1: Reportes Avanzados de Apoyos Verification Report

**Phase Goal:** Six reports/dashboards over Apoyo data — coverage per coordinator + coordinator ranking (team rollup), leader ranking with duplicate exclusion, polling-place ranking, rejections report, duplicates report (intentional cross-campaign exception), combined flat CSV export (Apoyos+Líderes+Coordinadores), and jurisdiction dentro/fuera report (hidden for Nacional-scope campaigns).
**Verified:** 2026-07-23T15:26:21Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to locked decisions D-01..D-06 from 04.1-CONTEXT.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01: Rankings/counts exclude `VoterStatus::DUPLICATE` in TopLeadersTable, TopCoordinatorsTable, TopPollingPlacesTable | ✓ VERIFIED | All 3 widgets' `whereHas`/`withCount` closures contain `->where('status', '!=', VoterStatus::DUPLICATE->value)` (confirmed by direct read of each file) |
| 2 | D-02: All 6 new reports are dashboard widgets with a working export action, single source of truth | ✓ VERIFIED | All 7 widget files (6 new + fixed TopLeadersTable) contain `->headerActions([Action::make('export')...])`; all registered in `AdminPanelProvider.php` |
| 3 | D-03: Combined flat CSV — one row per Apoyo, flattened líder/coordinador columns | ✓ VERIFIED | `ApoyosLideresCoordinadoresExport::query()` returns `Voter::query()` (not grouped); `map()` returns 15 flattened columns including líder/coordinador name/phone/email, with correct direct-coordinator-registration fallback |
| 4 | D-04: Jurisdiction report hidden for Nacional-scope campaigns; defined behavior for Regional-scope | ✓ VERIFIED | `JurisdictionReportTable::canView()` returns `false` for `CampaignScope::Nacional`; returns `true` for Regional-scope with a set municipality/department reference (branch order handles both) |
| 5 | D-05: Coordinator ranking sums valid apoyos across ALL assigned leaders (team rollup), not direct registrations | ✓ VERIFIED | `TopCoordinatorsTable` uses `withCount(['leaders as apoyos_equipo_count' => ...join voters on registered_by = leaders.id...])`, i.e., counts the coordinator's team's voters, not `registeredVoters` (the coordinator's own). Test suite explicitly asserts 10+5=15 across 2 leaders. |
| 6 | D-06: DuplicatesReportTable/DuplicatesExport are the ONLY files using `withoutGlobalScopes` (sole intentional cross-campaign exception) | ✓ VERIFIED | `grep -rl "withoutGlobalScopes" app/Filament/Widgets/*.php app/Exports/*.php` returns exactly `app/Filament/Widgets/DuplicatesReportTable.php` and `app/Exports/DuplicatesExport.php` — no other match |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/Filament/Widgets/TopLeadersTable.php` | D-01 fix + export | ✓ VERIFIED | 2 DUPLICATE-exclusion filters (whereHas + withCount), headerActions present |
| `app/Filament/Widgets/TopCoordinatorsTable.php` | D-05 team-rollup + export | ✓ VERIFIED | No `having`/`whereHas('leaders'` filter (zero-count coordinators stay visible), correct join via `qualifyColumn('id')` |
| `app/Filament/Widgets/TopPollingPlacesTable.php` | Polling-place ranking + export | ✓ VERIFIED | `PollingPlace::query()` based, DUPLICATE excluded, `getTableDescription()` surfaces "sin puesto" caveat |
| `app/Filament/Widgets/RejectionsReportTable.php` | Consolidated rejections + export | ✓ VERIFIED | `orWhereHas('verificationCalls', ...)` (EXISTS subquery, no `->join(`), `motivo` computed via `array_unique`+`implode` to avoid double-listing |
| `app/Filament/Widgets/DuplicatesReportTable.php` | Cross-campaign duplicates + export | ✓ VERIFIED | `withoutGlobalScopes()`+`withTrashed()`, PHPDoc exception callout present, per-row `campaign.name` badge |
| `app/Filament/Widgets/JurisdictionReportTable.php` | D-04 hide + dentro/fuera + export | ✓ VERIFIED | Static `canView()` gate present; branch order (municipality_id first, then department_id) covers Municipal/Departmental/referenced-Regional |
| `app/Filament/Widgets/ApoyosLideresCoordinadoresTable.php` | D-03 dashboard source + export | ✓ VERIFIED | Campaign-scoped (no `withoutGlobalScopes`), Gremio/Subcategoría filters present |
| `app/Exports/TopLeadersExport.php`, `TopCoordinatorsExport.php`, `TopPollingPlacesExport.php`, `RejectionsExport.php`, `DuplicatesExport.php`, `JurisdictionExport.php`, `ApoyosLideresCoordinadoresExport.php` | Matching exports | ✓ VERIFIED | All 7 implement `FromQuery, ShouldAutoSize, WithHeadings, WithMapping, WithStyles` with real `query()`/`map()` bodies |
| `app/Providers/Filament/AdminPanelProvider.php` | 6 new widget registrations | ✓ VERIFIED | All 6 `use` imports + all 6 `::class` entries present in `->widgets([...])` array, admin panel only (Coordinator/Leader panels untouched, confirmed by plan's own git-diff check in 04.1-05) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `TopLeadersTable.php` | `VoterStatus::DUPLICATE` | `where('status','!=',...)` in whereHas+withCount | ✓ WIRED | 2 matches confirmed |
| `TopCoordinatorsTable.php` | `User::leaders()` relation | `withCount(['leaders as apoyos_equipo_count' => join voters...])` | ✓ WIRED | Uses `$q->qualifyColumn('id')` fix (documented deviation) instead of hardcoded `users.id`, confirmed correct via passing test asserting 15 = 10+5 |
| `JurisdictionReportTable.php` | `CampaignScope::Nacional` | `canView(): bool` static gate | ✓ WIRED | Confirmed in code and by passing test |
| `ApoyosLideresCoordinadoresExport.php` | `User::coordinator()` | `$voter->registeredBy->coordinator` + direct-coordinator fallback | ✓ WIRED | `coordinadorFor()` helper checks `hasRole(UserRole::COORDINATOR)` first, else falls back to `$lider?->coordinator` |
| `DuplicatesReportTable.php` | `Voter::withoutGlobalScopes()` | bypasses CampaignContextScope | ✓ WIRED | Confirmed sole exception via grep across all phase widget/export files |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| TopLeadersTable | `registered_voters_count` | `withCount` aggregate on real `User::registeredVoters` relation, DB-backed | Yes | ✓ FLOWING |
| TopCoordinatorsTable | `apoyos_equipo_count`/`leaders_count` | `withCount` aggregate joining real `voters`/`users` tables | Yes | ✓ FLOWING |
| TopPollingPlacesTable | `apoyos_count` | `withCount` on real `PollingPlace::voters()` relation | Yes | ✓ FLOWING |
| RejectionsReportTable | `motivo` (computed column) | Real `Voter::status` + `VerificationCall::call_result` DB columns | Yes | ✓ FLOWING |
| DuplicatesReportTable | document_number groups | Real DB `havingRaw`/`groupBy` against `voters` table (bypassing scope) | Yes | ✓ FLOWING |
| JurisdictionReportTable | `jurisdiccion` (computed column) | Real `Campaign::municipality_id`/`department_id` vs `Voter::municipality_id`/`municipality.department_id` | Yes | ✓ FLOWING |
| ApoyosLideresCoordinadoresTable/Export | líder/coordinador columns | Real `registeredBy`/`registeredBy.coordinator` eager-loaded relations | Yes | ✓ FLOWING |

No hollow/static/disconnected data paths found — all widgets query live Eloquent relations against real DB tables, no hardcoded empty arrays or static returns.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| D-01 regression fix is genuinely fixed | `php artisan test --filter="excludes DUPLICATE status voters"` | 1 passed | ✓ PASS |
| D-05 team-rollup coordinator ranking | `php artisan test tests/Feature/Filament/TopCoordinatorsTableTest.php` | 4 passed (15 = 10+5 assertion) | ✓ PASS |
| D-06 cross-campaign sibling visibility | `php artisan test tests/Feature/Filament/DuplicatesReportTableTest.php` | 3 passed | ✓ PASS |
| D-04 Nacional-hide + dentro/fuera | `php artisan test tests/Feature/Filament/JurisdictionReportTableTest.php` | 3 passed | ✓ PASS |
| D-03 combined CSV export map() | `php artisan test tests/Unit/Exports/ApoyosLideresCoordinadoresExportTest.php` | 3 passed | ✓ PASS |
| Full-phase widget/test surface | `php artisan test` (8 phase test files) | 36 passed (110 assertions) | ✓ PASS |
| Full application test suite | `php artisan test` (run 3x) | 829 passed / then rotating single failures in unrelated files (`UserResourceTest`, `IsElectionDayMiddlewareTest`, `VoterResourceTest`) on repeat runs, each passing in isolation | ✓ PASS (see note below) |
| Pint formatting on phase-touched files | `vendor/bin/pint --test` on all 15 phase files | 0 violations | ✓ PASS |
| D-06 exclusivity | `grep -rl "withoutGlobalScopes" app/Filament/Widgets/*.php app/Exports/*.php` | Exactly `DuplicatesReportTable.php`, `DuplicatesExport.php` | ✓ PASS |

**Note on full-suite flakiness:** Ran the full `php artisan test` suite 3 times. Results: run 1 = 829/829 passed; run 2 = 1 failure (`Tests\Feature\Filament\UserResourceTest > can update user campaigns`); run 3 = 1 failure (`Tests\Feature\Filament\VoterResourceTest > can search voters by first name`). Both failing tests pass reliably in isolation (`php artisan test --filter=...`). Neither `UserResourceTest`, `VoterResourceTest`, nor `IsElectionDayMiddlewareTest` (also seen failing once) touch any file in this phase's surface (the 6 new widgets/exports + `TopLeadersTable`). This matches the orchestrator-confirmed pre-existing test-order/state-leak flake documented in `deferred-items.md` — the specific rotating test varies across runs, but the pattern (unrelated file, passes in isolation, fails only under full-suite ordering) is consistent and was independently reproduced here across 3 runs. Not attributed to Phase 04.1.

### Requirements Coverage

(No formal REQ-IDs — cross-referenced against 04.1-CONTEXT.md's `<decisions>` section per orchestrator instruction)

| Decision | Source Plan | Description | Status | Evidence |
|----------|------------|-------------|--------|----------|
| D-01 | 04.1-02 | Exclude DUPLICATE from rankings/counts everywhere, fix existing TopLeadersTable bug | ✓ SATISFIED | Verified in TopLeadersTable, TopCoordinatorsTable, TopPollingPlacesTable |
| D-02 | 04.1-02/03/04/05 | Widget+export pattern, no orphaned downloads | ✓ SATISFIED | All 7 widgets have headerActions; all registered in AdminPanelProvider |
| D-03 | 04.1-04 | Combined CSV (Apoyos+Líderes+Coordinadores), not all system tables | ✓ SATISFIED | Export/widget scoped to Voter+registeredBy+coordinator only, no calls/surveys/messages tables referenced |
| D-04 | 04.1-04 | Jurisdiction report hidden for Nacional-scope, defined for Regional | ✓ SATISFIED | `canView()` + branch-order classification logic verified |
| D-05 | 04.1-02 | Coordinator ranking = team rollup, not direct registrations | ✓ SATISFIED | `leaders as apoyos_equipo_count` withCount verified, no use of `registeredVoters` for coordinators |
| D-06 | 04.1-03/05 | Duplicates report is the sole cross-campaign exception | ✓ SATISFIED | grep-confirmed exclusivity across all phase widget/export files |

No orphaned decisions found — all 6 (D-01 through D-06) are addressed across the 5 plans.

### Anti-Patterns Found

None. Scanned all 7 widget files + 7 export files for TODO/FIXME/placeholder/empty-implementation patterns — zero matches.

### Human Verification Required

None required for functional correctness — all behaviors are covered by automated Pest/Livewire tests that exercise the actual query logic (not mocked). The following are optional visual/UX checks a human may still want to do, but they do not block phase completion:

1. **Visual dashboard layout check**
   **Test:** Load `/admin` dashboard as a super_admin user with an active campaign selected.
   **Expected:** All 9 widgets (3 pre-existing + TopLeadersTable fixed + 6 new) render in the documented row order without visual overlap or layout breakage, and the "Exportar" buttons visibly download `.xlsx` files when clicked.
   **Why human:** Visual layout/spacing and actual file-download UX are not captured by Livewire component tests, which assert on table state, not rendered pixel layout or browser download behavior.

## Gaps Summary

No gaps found. All 6 locked decisions (D-01 through D-06) from 04.1-CONTEXT.md are genuinely implemented and verified against the actual codebase (not just SUMMARY claims). All artifacts exist, are substantive (real query logic, not stubs), are wired (registered on the admin dashboard), and produce real data (live DB-backed Eloquent queries). The full phase-specific test surface (36 tests, 110 assertions) passes, Pint is clean on all 15 phase-touched files, and the full application suite is green modulo a pre-existing, orchestrator-confirmed test-order flake unrelated to this phase's file surface (independently reproduced here across 3 full-suite runs, rotating between `UserResourceTest`, `VoterResourceTest`, and `IsElectionDayMiddlewareTest`, none of which are touched by any 04.1 plan).

---

*Verified: 2026-07-23T15:26:21Z*
*Verifier: Claude (gsd-verifier)*

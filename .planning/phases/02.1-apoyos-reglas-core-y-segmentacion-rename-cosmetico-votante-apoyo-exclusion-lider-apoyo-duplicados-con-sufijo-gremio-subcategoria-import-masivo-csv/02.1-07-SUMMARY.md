---
phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv
plan: 07
subsystem: rename-cosmetico
tags: [rename, votante-apoyo, leader-panel, coordinator-panel, campaign-admin, call-center, landing-page, adr-006]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: Locked D-01 rename decision and ADR-006 interface rules (only user-facing strings renamed)
provides:
  - Leader panel (nav, dashboard, my-voters, register-voter) UI text using Apoyo terminology
  - Coordinator panel (leaders list, dashboard, leader-voters) UI text using Apoyo terminology
  - Campaign-admin dashboard UI text using Apoyo terminology
  - Call-center Livewire views (queue, register) UI text using Apoyo terminology
  - Public marketing landing page (welcome.blade.php) UI text using Apoyo terminology
  - Updated LeaderAppTest.php assertions matching renamed empty-state copy
affects: [04.1-reportes-avanzados-de-apoyos]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ADR-006 rename scope: only user-facing strings (headings, flux:heading/flux:text content, labels, button text, empty-state copy, page titles passed to layout(...)) are renamed; Livewire component names, route names, Volt file paths, PHP variable names, and CSV filename variables are left untouched"
    - "PHP comments adjacent to renamed labels (e.g. '// Total Votantes', '// Crear el votante') were also renamed for internal consistency with sibling plan 02.1-05's precedent, even where not explicitly listed in the plan's <action> block, since the plan's acceptance criteria required a 0-match grep count per file"

key-files:
  created: []
  modified:
    - resources/views/components/layouts/leader.blade.php
    - resources/views/livewire/leader/dashboard.blade.php
    - resources/views/livewire/leader/my-voters.blade.php
    - resources/views/livewire/leader/register-voter.blade.php
    - resources/views/livewire/coordinator/leaders.blade.php
    - resources/views/livewire/coordinator/dashboard.blade.php
    - resources/views/livewire/coordinator/leader-voters.blade.php
    - resources/views/livewire/campaign-admin/dashboard.blade.php
    - resources/views/livewire/calls/queue.blade.php
    - resources/views/livewire/calls/register.blade.php
    - resources/views/welcome.blade.php
    - tests/Feature/Leader/LeaderAppTest.php

key-decisions:
  - "Renamed a small number of PHP comments not explicitly listed in the plan's <action> blocks (e.g. register-voter.blade.php '// Crear el votante' and '// Redirigir a Mis Votantes...', coordinator/dashboard.blade.php '// Total Votantes', campaign-admin/dashboard.blade.php '// Top 5 lideres con mas votantes') because each task's acceptance criteria explicitly required a 0-match grep count per file, and Task 2's own action list already renamed sibling comments (e.g. 'Total Votantes' comment), establishing plan-internal precedent for comment renames in this specific plan (unlike plan 02.1-06, which explicitly left comments untouched by its own action text)."

requirements-completed: [D-01]

# Metrics
duration: 3min
completed: 2026-07-23
---

# Phase 02.1 Plan 07: Rename Votante to Apoyo in Leader/Coordinator/Campaign-admin/Call-center/Landing Views Summary

**Renamed every remaining user-facing "Votante"/"votante" string across 11 Blade/Livewire view files (Leader panel, Coordinator panel, Campaign-admin dashboard, call-center views, and the public marketing landing page) to "Apoyo"/"apoyo", completing the D-01 rename for the daily-use operator panels and the public-facing landing page.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-07-22T23:43:00Z (approx)
- **Completed:** 2026-07-22T23:46:17Z
- **Tasks:** 3 completed
- **Files modified:** 12

## Accomplishments
- Leader panel nav link, dashboard stat labels/empty-state/button text, my-voters list heading/export copy/empty-state/button, and register-voter page title/success message/checkbox label/submit button all renamed; CSV filename variables (`mis-votantes-filtrados-`, `mis-votantes-todos-`) intentionally left untouched (download filenames, not UI text)
- Coordinator panel: leaders list stat label/self-promote copy/inline count, dashboard stat label/leader-productivity badge, and leader-voters page title/heading/stat label/empty-state all renamed
- Campaign-admin dashboard: stat labels, comments, top-leaders badge, and "Apoyos por Municipio" chart heading renamed
- Call-center queue table header ("Apoyo"), call registration heading and survey-prompt copy renamed
- Public landing page (welcome.blade.php) hero copy, feature heading/description, and stat label renamed
- `LeaderAppTest.php` assertions updated to match renamed empty-state copy (`No hay apoyos registrados` / `Comienza registrando tu primer apoyo`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename in Leader panel views** - `75a40d3` (feat)
2. **Task 2: Rename in Coordinator panel and Campaign-admin dashboard views** - `17c6bab` (feat)
3. **Task 3: Rename in call-center views and the marketing landing page** - `cc1cf9a` (feat)

**Plan metadata:** committed with this SUMMARY (see final commit below)

## Files Created/Modified
- `resources/views/components/layouts/leader.blade.php` - Nav link "Mis Votantes" -> "Mis Apoyos"
- `resources/views/livewire/leader/dashboard.blade.php` - Welcome copy, stat label, quick-action button, and empty-state renamed
- `resources/views/livewire/leader/my-voters.blade.php` - Layout title, export copy, empty-state, and button renamed; CSV filename variables (lines 150/179) left untouched
- `resources/views/livewire/leader/register-voter.blade.php` - Layout title, success message, checkbox label, submit button, and 2 PHP comments renamed
- `resources/views/livewire/coordinator/leaders.blade.php` - Stat label, self-promote copy, and inline leader-count text renamed
- `resources/views/livewire/coordinator/dashboard.blade.php` - Stat label/comment and leader-productivity badge renamed
- `resources/views/livewire/coordinator/leader-voters.blade.php` - Layout title, page heading, stat label, and empty-state renamed
- `resources/views/livewire/campaign-admin/dashboard.blade.php` - Stat labels, 3 comments, top-leaders badge, and municipality chart heading renamed
- `resources/views/livewire/calls/queue.blade.php` - Queue table header column "Votante" -> "Apoyo"
- `resources/views/livewire/calls/register.blade.php` - Card heading and survey-prompt copy renamed
- `resources/views/welcome.blade.php` - Hero paragraph, feature 2 heading/description, and stat label renamed
- `tests/Feature/Leader/LeaderAppTest.php` - 2 pairs of `assertSee()` calls updated to match renamed empty-state copy

## Decisions Made
- Renamed several PHP comments not explicitly called out in the plan's per-line `<action>` instructions (`register-voter.blade.php` L187/L228, `coordinator/dashboard.blade.php` L26, `campaign-admin/dashboard.blade.php` L43) because each task's `<acceptance_criteria>` explicitly required the file-scoped grep count to reach 0, and this plan's own Task 2 action list already established the precedent of renaming adjacent comments (e.g. "Total Votantes" comment on campaign-admin/dashboard.blade.php L110). This differs from sibling plan 02.1-06, whose task text explicitly instructed leaving certain comments/log lines untouched — no such explicit exception existed here, so the comments were renamed to satisfy the stated acceptance criteria.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Plan gap] Renamed unlisted PHP comments to satisfy acceptance criteria**
- **Found during:** Tasks 1 and 2
- **Issue:** `register-voter.blade.php` (2 comments), `coordinator/dashboard.blade.php` (1 comment), and `campaign-admin/dashboard.blade.php` (1 comment) contained "votante(s)" in PHP comments not listed in the plan's per-line action items, but each task's acceptance criteria required a 0-match grep count per file.
- **Fix:** Renamed the comments to "apoyo(s)" for consistency with the plan's stated acceptance criteria and its own precedent (Task 2 explicitly renames similar comments elsewhere in the same files).
- **Files modified:** `resources/views/livewire/leader/register-voter.blade.php`, `resources/views/livewire/coordinator/dashboard.blade.php`, `resources/views/livewire/campaign-admin/dashboard.blade.php`
- **Commit:** `75a40d3` (register-voter.blade.php), `17c6bab` (coordinator/dashboard.blade.php, campaign-admin/dashboard.blade.php)

### Concurrency notes (not code deviations)
- This plan executed concurrently with 5 sibling plans (02.1-02, 02.1-03, 02.1-04, 02.1-05, 02.1-06) in the same working directory with no worktree isolation. Two harmless side effects of this were observed and are documented for transparency:
  1. `git status --short` snapshots taken mid-execution showed dirty files belonging to sibling plans (e.g. `app/Filament/Widgets/*.php`, `PublicVoterRegistrationController.php`) — these were sibling agents' own in-progress/uncommitted work, not touched by this plan's `Edit`/`Write`/`git add` calls, and were independently committed by those sibling agents shortly after.
  2. The Task 3 commit (`cc1cf9a`) unintentionally also includes `02.1-02-SUMMARY.md` because a sibling agent's `git add` for that file interleaved with this plan's `git add` + `git commit` sequence (git commits the full staged index, not just the files passed to the preceding `git add`). The file's content is legitimate and owned by sibling plan 02.1-02; no content was altered or lost.
  3. A brief, non-reproducible `DashboardWidgetsTest` failure was observed mid-execution (3 tests failed referencing "Distribución Territorial de Votantes") caused by a sibling agent's concurrent rename of Filament widgets (plan 02.1-05) racing with that test file's own rename — re-running immediately after showed 16/16 passing. Confirmed via `git stash` that this plan's own file changes were not the cause.
- Pre-existing Pint style issues (`single_import_per_statement`, `unary_operator_spaces`, etc.) found via `vendor/bin/pint --test` across nearly all touched Volt files were confirmed (via `git stash`) to predate this plan's edits. Logged to `deferred-items.md` per the SCOPE BOUNDARY rule; not fixed, since `vendor/bin/pint --dirty` (the plan's actual verification command) reports 0 issues on all files this plan touched.

## Issues Encountered
None blocking. See Concurrency notes above for transient, non-blocking observations during parallel execution.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Leader panel, Coordinator panel, Campaign-admin dashboard, call-center Livewire views, and the public marketing landing page all use Apoyo/apoyo terminology
- `LeaderAppTest` passes (30 passed, 77 assertions)
- `DashboardWidgetsTest` and `CallCenterLoadQueueTest` (regression checks) pass
- Only intentionally-excluded CSV filename variables in `my-voters.blade.php` still contain "votante" (by design, per the plan's own interface rule)
- No blockers for subsequent phases

---
*Phase: 02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv*
*Completed: 2026-07-23*

## Self-Check: PASSED

All 12 modified files confirmed present on disk. deferred-items.md confirmed present on disk. All 3 task commits (`75a40d3`, `17c6bab`, `cc1cf9a`) confirmed present in git history.

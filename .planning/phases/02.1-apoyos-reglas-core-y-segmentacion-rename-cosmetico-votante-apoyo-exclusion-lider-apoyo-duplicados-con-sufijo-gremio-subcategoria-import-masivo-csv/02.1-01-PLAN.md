---
phase: 02.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - database/migrations/*_create_notifications_table.php
  - database/migrations/*_create_imports_table.php
  - database/migrations/*_create_exports_table.php
  - database/migrations/*_create_failed_import_rows_table.php
  - tests/Feature/ApoyoLeaderExclusionTest.php
  - tests/Feature/ApoyoDuplicateSequenceTest.php
  - tests/Feature/Filament/ApoyoImporterTest.php
  - tests/Unit/Models/GremioSubcategoriaTest.php
autonomous: true
requirements: [D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "The Filament native ImportAction can be used later in this phase without a 'table not found' runtime error"
    - "A test file exists for every new behavior this phase introduces (leader exclusion, duplicate sequence, CSV import, Gremio/Subcategoria) before that behavior is implemented"
  artifacts:
    - path: "database/migrations"
      provides: "notifications, imports, exports, failed_import_rows tables required by Filament's ImportAction"
    - path: "tests/Feature/ApoyoLeaderExclusionTest.php"
      provides: "RED scaffold for D-08 (leader/coordinator document exclusion)"
    - path: "tests/Feature/ApoyoDuplicateSequenceTest.php"
      provides: "RED scaffold for D-02/D-03/D-10 (duplicate sequence + reassignment audit)"
    - path: "tests/Feature/Filament/ApoyoImporterTest.php"
      provides: "RED scaffold for D-06/D-07 (CSV import partial success + role restriction)"
    - path: "tests/Unit/Models/GremioSubcategoriaTest.php"
      provides: "RED scaffold for D-04/D-05/D-09 (hierarchical optional global catalog)"
  key_links:
    - from: "tests/Feature/Filament/ApoyoImporterTest.php"
      to: "imports / failed_import_rows tables"
      via: "Filament\\Actions\\ImportAction queued job persistence"
      pattern: "imports|failed_import_rows"
---

<objective>
Land the test scaffolding and Filament import infrastructure that every later plan in Phase 02.1 depends on, per `02.1-VALIDATION.md` Wave 0 requirements and the phase's critical sequencing note.

Purpose: This phase reverses an existing, tested business rule (global uniqueness of `voters.document_number`) and introduces five new pieces of behavior (rename, exclusion rule, duplicate-with-suffix scheme, Gremio/Subcategoria catalog, CSV import). Per the Nyquist rule, no later plan may claim `<automated>` verification against a test file that doesn't exist yet — this plan creates those files first (RED state, expected to fail/error until later plans implement the behavior). It also publishes the Filament `ImportAction` infrastructure tables so `ApoyoImporter` (built in `02.1-10`) doesn't hit a "table not found" error at runtime.
Output: 4 new test files (failing/erroring until later plans implement the behavior they describe) + `notifications`, `imports`, `exports`, `failed_import_rows` tables migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv/02.1-CONTEXT.md
@.planning/phases/02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv/02.1-RESEARCH.md
@.planning/phases/02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv/02.1-VALIDATION.md
</context>

<interfaces>
<!-- These test files describe behavior that does NOT exist yet. They are expected to error (class not found) or fail
     until the plans listed below implement the real code. That is correct RED state for Wave 0 — do not try to make
     these tests pass in this plan. -->

Behavior each scaffold documents, and which later plan implements it:
- tests/Feature/ApoyoLeaderExclusionTest.php  → implemented by plan 02.1-04 (App\Rules\DocumentNotBelongsToLeaderOrCoordinator) and wired in 02.1-09/02.1-10
- tests/Feature/ApoyoDuplicateSequenceTest.php → implemented by plan 02.1-03 (duplicate_sequence column + VoterDuplicateAssignmentService) and 02.1-08 (reassignment action)
- tests/Feature/Filament/ApoyoImporterTest.php → implemented by plan 02.1-10 (App\Filament\Imports\ApoyoImporter)
- tests/Unit/Models/GremioSubcategoriaTest.php → implemented by plan 02.1-02 (App\Models\Gremio, App\Models\Subcategoria)

Existing sibling test conventions to mirror (read before writing scaffolds):
- tests/Feature/UserVoterRelationTest.php — `uses()->group(...)`, `beforeEach` building Department/Municipality/Neighborhood/Campaign fixtures
- tests/Feature/Filament/VoterResourceTest.php — Livewire::test() pattern, role setup via `Role::firstOrCreate`, `actingAs($this->admin)`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Publish Filament ImportAction infrastructure migrations</name>
  <files>database/migrations (new files created by the commands below)</files>
  <read_first>
    - database/migrations/0001_01_01_000002_create_jobs_table.php (confirms `job_batches` ALREADY EXISTS — do NOT run `make:queue-batches-table`, it would collide)
    - vendor/filament/actions/docs/11-import.md (lines ~1-20: exact required migrations)
  </read_first>
  <action>
    Run, in this exact order, from the project root:
    1. `php artisan make:notifications-table --no-interaction` — creates `database/migrations/xxxx_create_notifications_table.php`. Do NOT run `make:queue-batches-table` — `job_batches` already exists in `0001_01_01_000002_create_jobs_table.php` and re-creating it will fail at migrate time.
    2. `php artisan vendor:publish --tag=filament-actions-migrations --no-interaction` — copies `create_imports_table.php`, `create_exports_table.php`, `create_failed_import_rows_table.php` from `vendor/filament/actions/database/migrations` into `database/migrations/`.
    3. `php artisan migrate --no-interaction` — applies all four new tables against the local dev database.
    4. Since the project's `notifiable` model is `App\Models\User` with an integer auto-increment `id` (not UUID), verify the published notifications migration uses `$table->morphs('notifiable')` (not `uuidMorphs`) — this is the Laravel default, no edit needed unless the publish produced `uuidMorphs`.
  </action>
  <verify>
    <automated>php artisan migrate:status | grep -E "create_notifications_table|create_imports_table|create_exports_table|create_failed_import_rows_table"</automated>
  </verify>
  <acceptance_criteria>
    - `database/migrations/*_create_notifications_table.php` exists
    - `database/migrations/*_create_imports_table.php` exists
    - `database/migrations/*_create_exports_table.php` exists
    - `database/migrations/*_create_failed_import_rows_table.php` exists
    - `php artisan migrate:status` shows all four as `Ran`
    - No migration file named `*_create_job_batches_table.php` was added (job_batches already existed)
  </acceptance_criteria>
  <done>The four Filament-required tables exist and are migrated; `ImportAction` will not throw "no such table" when exercised in plan 02.1-10.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold RED tests for duplicate sequence and leader-exclusion behavior</name>
  <files>tests/Feature/ApoyoDuplicateSequenceTest.php, tests/Feature/ApoyoLeaderExclusionTest.php</files>
  <read_first>
    - tests/Feature/VoterTest.php (existing conventions: `Voter::factory()`, `assertDatabaseHas`, imports at top)
    - tests/Feature/UserVoterRelationTest.php (existing conventions: `beforeEach` builds Department/Municipality/Neighborhood/Campaign; `UserObserver` manual invocation pattern)
    - .planning/phases/02.1-.../02.1-RESEARCH.md sections "Pattern 2: Duplicate-Cédula Schema" and "Pattern 3: Leader-Cannot-Be-Apoyo Validation"
  </read_first>
  <action>
    Create `tests/Feature/ApoyoDuplicateSequenceTest.php` with Pest `it(...)` blocks (no implementation exists yet — these MUST fail/error, that is expected and correct):
    - `it('assigns duplicate_sequence 0 to the first voter registered with a document_number')`
    - `it('assigns duplicate_sequence 1 and status DUPLICATE to a second voter registered with the same document_number')`
    - `it('assigns duplicate_sequence 2 to a third voter registered with the same document_number, keeping sequence 0 and 1 untouched')`
    - `it('reassigning the owner of a duplicate requires a mandatory note and writes a validation_histories row with validation_type duplicate_reassignment')`
    - `it('reassigning a duplicate never changes duplicate_sequence on any row (D-10 immutability)')`
    Each test body should construct the minimum fixtures (Campaign, Municipality factories) then call `Voter::factory()->create(['document_number' => '...'])` twice/three times and assert on `duplicate_sequence`/`status` — reference `App\Enums\VoterStatus::DUPLICATE` (will not exist until plan 02.1-03; that is fine, this file is expected to fail until then).

    Create `tests/Feature/ApoyoLeaderExclusionTest.php` with Pest `it(...)` blocks:
    - `it('rejects registering an Apoyo whose document_number belongs to an existing leader user')`
    - `it('rejects registering an Apoyo whose document_number belongs to an existing coordinator user')`
    - `it('allows registering an Apoyo whose document_number belongs to an admin_campaign user')` (D-08 scopes exclusion to leader/coordinator only, per RESEARCH.md Open Question 1 resolution)
    - `it('does not block UserObserver from auto-creating a leader\'s own system voter record')` — mirror `tests/Feature/UserVoterRelationTest.php`'s `'creating user with required data and campaign auto-creates voter record'` test, asserting the auto-created system voter for a leader still gets created without validation errors
    Reference `App\Rules\DocumentNotBelongsToLeaderOrCoordinator` (will not exist until plan 02.1-04).
  </action>
  <verify>
    <automated>php artisan test --filter=ApoyoDuplicateSequenceTest --filter=ApoyoLeaderExclusionTest; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/ApoyoDuplicateSequenceTest.php` exists and contains at least 5 `it(...)` blocks
    - `tests/Feature/ApoyoLeaderExclusionTest.php` exists and contains at least 4 `it(...)` blocks
    - Running `php artisan test --filter=ApoyoDuplicateSequenceTest` currently fails or errors (class `VoterStatus::DUPLICATE` / duplicate_sequence column do not exist yet) — this is the expected RED state, not a bug
    - Running `php artisan test --filter=ApoyoLeaderExclusionTest` currently fails or errors (class `DocumentNotBelongsToLeaderOrCoordinator` does not exist yet) — expected RED state
  </acceptance_criteria>
  <done>Both scaffold files exist with concrete, specific assertions describing D-02/D-03/D-08/D-10 behavior; they fail for the right reason (missing implementation, not typos).</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold RED tests for CSV import and Gremio/Subcategoria catalog</name>
  <files>tests/Feature/Filament/ApoyoImporterTest.php, tests/Unit/Models/GremioSubcategoriaTest.php</files>
  <read_first>
    - tests/Feature/Filament/VoterResourceTest.php (Livewire::test conventions, role/actingAs setup)
    - app/Imports/NeighborhoodsImport.php (existing import pattern, for contrast — ApoyoImporter uses Filament's native Importer instead)
    - .planning/phases/02.1-.../02.1-RESEARCH.md section "Pattern 4: ApoyosImport"
  </read_first>
  <action>
    Create `tests/Feature/Filament/ApoyoImporterTest.php` with Pest `test(...)` blocks:
    - `test('admin_campaign can see the import action on the voters list page')`
    - `test('leader cannot see the import action on the voters list page')` (D-07)
    - `test('reviewer cannot see the import action on the voters list page')` (D-07)
    - `test('importing a CSV with a mix of valid and invalid rows creates only the valid Apoyos and produces a downloadable failed-rows report')` (D-06) — reference `App\Filament\Imports\ApoyoImporter` (will not exist until plan 02.1-10)
    - `test('importing a CSV row whose cedula belongs to an existing leader is rejected with a clear reason')` (D-06 + D-08 integration)
    Reference `App\Filament\Imports\ApoyoImporter::class` and `Filament\Actions\Imports\Models\Import`.

    Create `tests/Unit/Models/GremioSubcategoriaTest.php` with Pest `it(...)` blocks:
    - `it('a Subcategoria belongs to exactly one Gremio')`
    - `it('a Gremio has many Subcategorias')`
    - `it('Gremio and Subcategoria have no campaign_id column (global catalog, D-09)')`
    - `it('a Voter can be created without gremio_id or subcategoria_id (both optional, D-05)')`
    - `it('GremioFactory and SubcategoriaFactory produce valid records')`
    Reference `App\Models\Gremio`, `App\Models\Subcategoria` (will not exist until plan 02.1-02).
  </action>
  <verify>
    <automated>php artisan test --filter=ApoyoImporterTest --filter=GremioSubcategoriaTest; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Filament/ApoyoImporterTest.php` exists and contains at least 5 `test(...)` blocks
    - `tests/Unit/Models/GremioSubcategoriaTest.php` exists and contains at least 5 `it(...)` blocks
    - Running both currently fails or errors (classes `Gremio`, `Subcategoria`, `ApoyoImporter` do not exist yet) — expected RED state
  </acceptance_criteria>
  <done>Both scaffold files exist with concrete assertions describing D-04/D-05/D-06/D-07/D-09 behavior; they fail for the right reason (missing implementation).</done>
</task>

</tasks>

<verification>
Run `php artisan migrate:status` to confirm the four infrastructure tables are `Ran`. Run `php artisan test --filter=Apoyo --filter=GremioSubcategoria` and confirm all new tests fail/error due to missing classes (`Gremio`, `Subcategoria`, `VoterStatus::DUPLICATE`, `DocumentNotBelongsToLeaderOrCoordinator`, `ApoyoImporter`) — not due to typos in the test files themselves. Do not attempt to make these tests pass in this plan.
</verification>

<success_criteria>
- notifications/imports/exports/failed_import_rows tables migrated
- 4 new test files exist, each with 4+ concrete assertions describing exact expected behavior per D-ID
- Every later plan in this phase can reference an existing `<automated>` test command instead of `MISSING`
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-apoyos-reglas-core-y-segmentacion-rename-cosmetico-votante-apoyo-exclusion-lider-apoyo-duplicados-con-sufijo-gremio-subcategoria-import-masivo-csv/02.1-01-SUMMARY.md`
</output>

---
phase: 23-differentiator-visualizations
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - app/Filament/Widgets/ValidationHistorySankeyChart.php
  - app/Filament/Widgets/RejectionReasonsStackedAreaChart.php
  - app/Providers/Filament/AdminPanelProvider.php
  - tests/Browser/ValidationHistorySankeyChartTest.php
  - tests/Browser/RejectionReasonsStackedAreaChartTest.php
autonomous: true
requirements: [VIZ-07, VIZ-10]

must_haves:
  truths:
    - "Admin sees a Sankey diagram of ValidationHistory state transitions on the Admin dashboard, curated to a top-N-by-volume set plus an Otros collapse bucket rather than an unfiltered dump"
    - "Admin sees a stacked-area chart of rejection reasons over time on the Admin dashboard, broken down into 4 distinct VoterStatus rejection series (not one aggregate counter)"
  artifacts:
    - path: "app/Filament/Widgets/ValidationHistorySankeyChart.php"
      provides: "Top-N + Otros curated ValidationHistory transition aggregation (kind: sankey)"
    - path: "app/Filament/Widgets/RejectionReasonsStackedAreaChart.php"
      provides: "Weekly-bucketed 4-series rejection-reason aggregation (kind: stacked-area)"
    - path: "tests/Browser/ValidationHistorySankeyChartTest.php"
      provides: "Real-browser proof of non-degenerate Sankey rendering"
    - path: "tests/Browser/RejectionReasonsStackedAreaChartTest.php"
      provides: "Real-browser proof of 4-series stacked-area rendering"
  key_links:
    - from: "app/Filament/Widgets/ValidationHistorySankeyChart.php"
      to: "resources/js/charts/components/SankeyChart.jsx"
      via: "getChartKind() returns 'sankey' (registered in ChartRouter.jsx by 23-01)"
      pattern: "getChartKind\\(\\).*'sankey'"
    - from: "app/Filament/Widgets/RejectionReasonsStackedAreaChart.php"
      to: "resources/js/charts/components/StackedAreaChart.jsx"
      via: "getChartKind() returns 'stacked-area' (registered in ChartRouter.jsx by 23-01)"
      pattern: "getChartKind\\(\\).*'stacked-area'"
    - from: "app/Providers/Filament/AdminPanelProvider.php"
      to: "app/Filament/Widgets/{ValidationHistorySankeyChart,RejectionReasonsStackedAreaChart}.php"
      via: "->widgets([...]) panel-global registration"
      pattern: "ValidationHistorySankeyChart::class|RejectionReasonsStackedAreaChart::class"
---

<objective>
Ship VIZ-07 (a curated Sankey of ValidationHistory state transitions) and VIZ-10 (a weekly stacked-area of rejection reasons over time), both admin-only, campaign-wide, both registered on the Admin dashboard.

Purpose: Both widgets query the same underlying ValidationHistory model with the same campaign-scoping join gap (no direct campaign_id column, must join through voter_id to voters.campaign_id), and both must touch AdminPanelProvider.php's shared ->widgets([...]) array - bundling them into one plan avoids a cross-plan file conflict on that array (this plan runs after 23-02, which also touches that file).
Output: ValidationHistorySankeyChart (kind: sankey, depends on 23-01's SankeyChart.jsx) and RejectionReasonsStackedAreaChart (kind: stacked-area, depends on 23-01's StackedAreaChart.jsx), both registered on the Admin dashboard, each with its own Browser test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-differentiator-visualizations/23-CONTEXT.md
@.planning/phases/23-differentiator-visualizations/23-RESEARCH.md
@.planning/phases/23-differentiator-visualizations/23-UI-SPEC.md
</context>

<interfaces>
Reused patterns and contracts, read directly from the codebase and from 23-01's output.

From app/Filament/Widgets/CallContactabilityFunnelChart.php (existing ValidationHistory-sibling join pattern - VerificationCall also has no direct campaign_id, same join-through-voter_id gap this plan's ValidationHistory queries need):

$baseQuery = fn (): Builder => VerificationCall::query()
    ->join('voters', 'voters.id', '=', 'verification_calls.voter_id')
    ->where('voters.campaign_id', $activeCampaign->id);

From app/Models/ValidationHistory.php (both plan's widgets query this model):

class ValidationHistory extends Model
{
    protected $fillable = ['voter_id', 'previous_status', 'new_status', 'validated_by', 'validation_type', 'notes'];
    protected function casts(): array
    {
        return ['previous_status' => VoterStatus::class, 'new_status' => VoterStatus::class];
    }
    // No campaign_id column - must join through voter_id -> voters.campaign_id.
}

From resources/js/charts/components/SankeyChart.jsx (built by 23-01 - consumes exactly this shape):
data = { nodes: [{ name: string }], links: [{ source: number, target: number, value: number }] }
source/target are INDEXES into the nodes array, not status name strings (Recharts Sankey API constraint).

From resources/js/charts/components/StackedAreaChart.jsx (built by 23-01 - consumes the same labels/datasets shape as StackedBarChart.jsx, via toSeriesRows()):
data = { labels: string[], datasets: [{ label: string, data: number[] }, ...] }

From app/Providers/Filament/AdminPanelProvider.php (current state AFTER 23-02's edit - this plan's Task 3 appends to it):
->widgets([
    ...
    CoordinatorTeamStackedBarChart::class,
    VoterHappyPathFunnelChart::class,
    VoterLifecycleBranchCountersOverview::class,
    BirthdayWidget::class,
])
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ValidationHistorySankeyChart.php</name>
  <files>app/Filament/Widgets/ValidationHistorySankeyChart.php</files>
  <read_first>
    app/Models/ValidationHistory.php
    app/Filament/Widgets/CallContactabilityFunnelChart.php
    app/Enums/VoterStatus.php
  </read_first>
  <action>
Create app/Filament/Widgets/ValidationHistorySankeyChart.php. Recharts' Sankey requires source/target as node-ARRAY-INDEXES, not status strings (verified against node_modules/recharts/types/chart/Sankey.d.ts's LinkDataItem interface) - build a stable name-to-index map before building links. Implements D-05 (top-N by volume, target ~8-10, remainder collapsed into per-source-node "Otros" edges - the richer per-source-node collapse per 23-RESEARCH.md's Open Question 1 recommendation, preserving directional information instead of one flattened bucket), D-06 (null previous_status renders as a synthetic "Nuevo" source node), D-07 (no per-voter cycle dedup - each recorded pair counts every occurrence), D-08 (all campaign history, no date filter). Includes a defensive guard against degenerate previous_status === new_status rows (23-RESEARCH.md Pitfall 5).

namespace App\Filament\Widgets;

use App\Models\ValidationHistory;
use App\Services\CampaignContext;
use Filament\Widgets\ChartWidget;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Support\Facades\DB;

class ValidationHistorySankeyChart extends ChartWidget
{
    protected static ?int $sort = 24;

    protected ?string $heading = 'Transiciones de Estado de Apoyos';

    protected ?string $pollingInterval = '120s';

    protected string $view = 'filament.widgets.react-chart';

    private const TOP_N = 8;

    protected function getData(): array
    {
        $activeCampaign = CampaignContext::currentCampaign();

        $emptyPayload = fn (string $reason): array => ['nodes' => [], 'links' => [], 'emptyReason' => $reason];

        if (! $activeCampaign) {
            return $emptyPayload('no_campaign');
        }

        // ValidationHistory has no campaign_id directly - join through voter_id, same pattern as
        // CallContactabilityFunnelChart's VerificationCall join. Guard excludes degenerate
        // previous_status === new_status rows (Pitfall 5 - should not occur in valid data, but a
        // defensive filter avoids a zero-length self-referencing Sankey link).
        $transitions = ValidationHistory::query()
            ->join('voters', 'voters.id', '=', 'validation_history.voter_id')
            ->where('voters.campaign_id', $activeCampaign->id)
            ->where(function (Builder $q) {
                $q->whereNull('validation_history.previous_status')
                    ->orWhereColumn('validation_history.previous_status', '!=', 'validation_history.new_status');
            })
            ->select('validation_history.previous_status', 'validation_history.new_status', DB::raw('COUNT(*) as total'))
            ->groupBy('validation_history.previous_status', 'validation_history.new_status')
            ->orderByDesc('total')
            ->get();

        if ($transitions->isEmpty()) {
            return $emptyPayload('no_transitions');
        }

        // D-05: top-N by volume, remainder collapsed into "Otros" edges grouped by source node.
        $kept = $transitions->take(self::TOP_N);
        $excluded = $transitions->slice(self::TOP_N);

        // D-06: null previous_status renders as synthetic "Nuevo" source node.
        $nodeNames = collect(['Nuevo'])
            ->merge($kept->flatMap(fn ($t) => [
                $t->previous_status?->getLabel() ?? 'Nuevo',
                $t->new_status->getLabel(),
            ]))
            ->push('Otros')
            ->unique()
            ->values();

        $nodeIndex = $nodeNames->flip();

        $links = $kept->map(fn ($t) => [
            'source' => $nodeIndex[$t->previous_status?->getLabel() ?? 'Nuevo'],
            'target' => $nodeIndex[$t->new_status->getLabel()],
            'value' => (int) $t->total,
        ])->values();

        if ($excluded->isNotEmpty()) {
            $otrosBySource = $excluded->groupBy(fn ($t) => $t->previous_status?->getLabel() ?? 'Nuevo');
            foreach ($otrosBySource as $sourceLabel => $group) {
                $links->push([
                    'source' => $nodeIndex[$sourceLabel],
                    'target' => $nodeIndex['Otros'],
                    'value' => (int) $group->sum('total'),
                ]);
            }
        }

        return [
            'nodes' => $nodeNames->map(fn ($name) => ['name' => $name])->toArray(),
            'links' => $links->values()->toArray(),
        ];
    }

    protected function getType(): string
    {
        return $this->getChartKind();
    }

    protected function getChartKind(): string
    {
        return 'sankey';
    }
}
  </action>
  <verify>
    <automated>cd "/Volumes/NAS(MAC)/Data/Herd/sigma-project" && php -l app/Filament/Widgets/ValidationHistorySankeyChart.php</automated>
  </verify>
  <done>File exists, php -l passes, getChartKind() returns 'sankey', getData() returns {nodes, links} with numeric source/target indexes (never status name strings), guards the previous_status !== new_status self-loop case.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RejectionReasonsStackedAreaChart.php</name>
  <files>app/Filament/Widgets/RejectionReasonsStackedAreaChart.php</files>
  <read_first>
    app/Models/ValidationHistory.php
    app/Enums/VoterStatus.php
    app/Filament/Widgets/RejectionsCountersOverview.php
  </read_first>
  <action>
Create app/Filament/Widgets/RejectionReasonsStackedAreaChart.php. Implements D-18 (VoterStatus rejection states only - REJECTED_CENSUS, REJECTED_OUT_OF_SCOPE, CENSUS_NOT_FOUND, CORRECTION_REQUIRED - driven by ValidationHistory.new_status, deliberately NOT RejectionsCountersOverview's mixed CallResult-based definition; DUPLICATE excluded per Phase 22 D-04 precedent), D-19 (weekly buckets, full campaign history, no bounded window), D-20 (bucketed by the ValidationHistory row's created_at, not the voter's registration date). CRITICAL: week bucketing MUST happen in PHP/Carbon, never raw SQL YEARWEEK()/strftime() - MySQL (production) and sqlite (tests) disagree on ISO week-numbering semantics, which would silently diverge between the test suite and real production data (23-RESEARCH.md Pitfall 2).

namespace App\Filament\Widgets;

use App\Enums\VoterStatus;
use App\Models\ValidationHistory;
use App\Services\CampaignContext;
use Filament\Widgets\ChartWidget;
use Illuminate\Support\Carbon;

class RejectionReasonsStackedAreaChart extends ChartWidget
{
    protected static ?int $sort = 25;

    protected ?string $heading = 'Motivos de Rechazo en el Tiempo';

    protected ?string $pollingInterval = '120s';

    protected string $view = 'filament.widgets.react-chart';

    // D-18: VoterStatus rejection states only, in this fixed emission order (also determines
    // color-rank order per 23-UI-SPEC.md - REJECTED_CENSUS is always index 0/accent).
    private const REJECTION_STATUSES = [
        VoterStatus::REJECTED_CENSUS,
        VoterStatus::REJECTED_OUT_OF_SCOPE,
        VoterStatus::CENSUS_NOT_FOUND,
        VoterStatus::CORRECTION_REQUIRED,
    ];

    protected function getData(): array
    {
        $activeCampaign = CampaignContext::currentCampaign();

        $emptyPayload = fn (string $reason): array => ['labels' => [], 'datasets' => [], 'emptyReason' => $reason];

        if (! $activeCampaign) {
            return $emptyPayload('no_campaign');
        }

        $rows = ValidationHistory::query()
            ->join('voters', 'voters.id', '=', 'validation_history.voter_id')
            ->where('voters.campaign_id', $activeCampaign->id)
            ->whereIn('validation_history.new_status', array_map(fn (VoterStatus $s) => $s->value, self::REJECTION_STATUSES))
            ->select('validation_history.new_status', 'validation_history.created_at')
            ->get();

        if ($rows->isEmpty()) {
            return $emptyPayload('no_rejections');
        }

        // D-19/D-20: bucket by created_at into weeks, in PHP/Carbon - never raw SQL, which
        // disagrees between MySQL (prod) and sqlite (tests) on week-numbering (Pitfall 2).
        $byWeek = $rows->groupBy(fn ($r) => Carbon::parse($r->created_at)->startOfWeek()->format('Y-m-d'));
        $weekLabels = $byWeek->keys()->sort()->values();

        $datasets = collect(self::REJECTION_STATUSES)->map(fn (VoterStatus $status) => [
            'label' => $status->getLabel(),
            'data' => $weekLabels->map(
                fn ($week) => $byWeek[$week]->where('new_status', $status)->count()
            )->toArray(),
        ])->toArray();

        return ['labels' => $weekLabels->toArray(), 'datasets' => $datasets];
    }

    protected function getType(): string
    {
        return $this->getChartKind();
    }

    protected function getChartKind(): string
    {
        return 'stacked-area';
    }
}
  </action>
  <verify>
    <automated>cd "/Volumes/NAS(MAC)/Data/Herd/sigma-project" && php -l app/Filament/Widgets/RejectionReasonsStackedAreaChart.php</automated>
  </verify>
  <done>File exists, php -l passes, getChartKind() returns 'stacked-area', getData() returns exactly 4 datasets in the fixed REJECTION_STATUSES order, weeks bucketed via Carbon::parse(...)->startOfWeek(), never a raw SQL week function.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register both on Admin dashboard + Browser tests</name>
  <files>app/Providers/Filament/AdminPanelProvider.php, tests/Browser/ValidationHistorySankeyChartTest.php, tests/Browser/RejectionReasonsStackedAreaChartTest.php</files>
  <read_first>
    app/Providers/Filament/AdminPanelProvider.php
    tests/Browser/CallContactabilityFunnelChartTest.php
    tests/Pest.php
  </read_first>
  <action>
Edit app/Providers/Filament/AdminPanelProvider.php (this file was already modified by 23-02 - read its current state first, do not overwrite 23-02's additions):

1. Add two new use imports, alphabetically placed among the existing App\Filament\Widgets\* imports: `use App\Filament\Widgets\RejectionReasonsStackedAreaChart;` and `use App\Filament\Widgets\ValidationHistorySankeyChart;`.

2. Inside ->widgets([...]), add both new entries directly after 23-02's `VoterLifecycleBranchCountersOverview::class,` line and before `BirthdayWidget::class,`:

                ValidationHistorySankeyChart::class, // Sankey de transiciones de estado de ValidationHistory (VIZ-07)
                RejectionReasonsStackedAreaChart::class, // Área apilada de motivos de rechazo en el tiempo (VIZ-10)

3. Run `vendor/bin/pint --dirty` afterward to auto-fix import ordering.

Create tests/Browser/ValidationHistorySankeyChartTest.php. Recharts' Sankey has no default visible node/link text label (verified against node_modules/recharts/es6/chart/Sankey.js - the default node renderer is a bare Rectangle) - proving real render means asserting real .recharts-sankey-node / .recharts-sankey-link elements exist (a broken index-wiring bug, Pitfall 1, produces an empty/blank SVG with zero rendered elements and no console error):

use App\Enums\UserRole;
use App\Models\Campaign;
use App\Models\User;
use App\Models\ValidationHistory;
use App\Models\Voter;
use Database\Seeders\RoleSeeder;

beforeEach(function () {
    $this->seed(RoleSeeder::class);
});

it('renders ValidationHistorySankeyChart as a real Recharts sankey with real transition data', function () {
    $campaign = Campaign::factory()->create(['status' => 'active']);
    $admin = User::factory()->withoutTwoFactor()->create([
        'email' => 'validation-history-sankey@example.com',
        'password' => 'password',
    ]);
    $admin->assignRole(UserRole::SUPER_ADMIN->value);
    $admin->campaigns()->attach($campaign->id);

    $voterA = Voter::factory()->create(['campaign_id' => $campaign->id, 'registered_by' => $admin->id]);
    $voterB = Voter::factory()->create(['campaign_id' => $campaign->id, 'registered_by' => $admin->id]);
    $voterC = Voter::factory()->create(['campaign_id' => $campaign->id, 'registered_by' => $admin->id]);

    // Seed a few distinct (previous_status, new_status) pairs so the Sankey renders a
    // non-degenerate diagram (Pitfall 5's recommended fixture-seeding guidance).
    ValidationHistory::factory()->for($voterA, 'voter')->censusValidation()->create();
    ValidationHistory::factory()->for($voterB, 'voter')->create(['previous_status' => null, 'new_status' => \App\Enums\VoterStatus::PENDING_REVIEW]);
    ValidationHistory::factory()->for($voterC, 'voter')->rejection()->create();

    loginRealBrowserUser($admin);

    $page = visit(route('filament.admin.pages.dashboard'));

    foreach (range(1, 10) as $i) {
        $page->script('window.scrollTo(0, document.body.scrollHeight)');
        $page->wait(1);
    }

    $page->assertVisible('[data-chart-kind="sankey"]');
    $page->assertVisible('[data-chart-kind="sankey"] .recharts-sankey-node >> nth=0');
    $page->assertVisible('[data-chart-kind="sankey"] .recharts-sankey-link >> nth=0');
    $page->assertNoJavaScriptErrors();
});

Create tests/Browser/RejectionReasonsStackedAreaChartTest.php. StackedAreaChart.jsx has no static series-label legend (matching StackedBarChart.jsx's precedent) - proving real render means asserting multiple real .recharts-area path elements exist (one per rejection-status series):

use App\Enums\UserRole;
use App\Enums\VoterStatus;
use App\Models\Campaign;
use App\Models\User;
use App\Models\ValidationHistory;
use App\Models\Voter;
use Database\Seeders\RoleSeeder;

beforeEach(function () {
    $this->seed(RoleSeeder::class);
});

it('renders RejectionReasonsStackedAreaChart as a real Recharts stacked area with real rejection data', function () {
    $campaign = Campaign::factory()->create(['status' => 'active']);
    $admin = User::factory()->withoutTwoFactor()->create([
        'email' => 'rejection-reasons-stacked-area@example.com',
        'password' => 'password',
    ]);
    $admin->assignRole(UserRole::SUPER_ADMIN->value);
    $admin->campaigns()->attach($campaign->id);

    $voter = Voter::factory()->create(['campaign_id' => $campaign->id, 'registered_by' => $admin->id]);
    ValidationHistory::factory()->for($voter, 'voter')->create([
        'previous_status' => VoterStatus::PENDING_REVIEW,
        'new_status' => VoterStatus::REJECTED_CENSUS,
    ]);

    loginRealBrowserUser($admin);

    $page = visit(route('filament.admin.pages.dashboard'));

    foreach (range(1, 10) as $i) {
        $page->script('window.scrollTo(0, document.body.scrollHeight)');
        $page->wait(1);
    }

    $page->assertVisible('[data-chart-kind="stacked-area"]');
    $page->assertVisible('[data-chart-kind="stacked-area"] .recharts-area >> nth=0');
    $page->assertNoJavaScriptErrors();
});
  </action>
  <verify>
    <automated>cd "/Volumes/NAS(MAC)/Data/Herd/sigma-project" && vendor/bin/pint --test app/Providers/Filament/AdminPanelProvider.php; php artisan test tests/Browser/ValidationHistorySankeyChartTest.php tests/Browser/RejectionReasonsStackedAreaChartTest.php</automated>
  </verify>
  <done>AdminPanelProvider.php registers both widgets alongside 23-02's entries (not replacing them) and passes Pint; both Browser tests pass with zero JS errors.</done>
</task>

</tasks>

<verification>
Run `php artisan test tests/Browser/ValidationHistorySankeyChartTest.php tests/Browser/RejectionReasonsStackedAreaChartTest.php` - both must pass. Grep-confirm AdminPanelProvider.php's ->widgets([...]) array contains both new classes AND still contains 23-02's VoterHappyPathFunnelChart/VoterLifecycleBranchCountersOverview entries (proves this plan appended rather than overwrote).
</verification>

<success_criteria>
- [ ] ValidationHistorySankeyChart.php exists, kind sankey, top-N + Otros curation, Nuevo synthetic source node
- [ ] RejectionReasonsStackedAreaChart.php exists, kind stacked-area, 4 fixed-order series, PHP/Carbon week bucketing
- [ ] Both registered on Admin dashboard alongside all prior widgets
- [ ] Both Browser tests pass with zero JS errors
</success_criteria>

<output>
After completion, create .planning/phases/23-differentiator-visualizations/23-03-SUMMARY.md
</output>
